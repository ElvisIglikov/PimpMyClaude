# ⚪PimpMyClaude — оформление HTML для 👾 Элвиса (20.09.2026)

Правило `SkilZZZ/HTML.md` «Оформление — своего проекта»: файл выглядит как сам Пимп — тёмно-фиолетовый с
неоном. Общее (размеры текста, вопросы наверху, номер файла) — в `HTML.md`. **Светлой версии нет.**
Сверено с темой «Ультрафиолет» (`claude-patch/themes.json`: `#c674fe` / `#381655` / `#4b236e`) — здесь она
чуть темнее и спокойнее.

## Палитра

- `--bg` `#241338` — фон страницы (окно Пимпа — `#2a1a47`…`#3b2560`)
- `--panel` `#32204f` — панели и карточки, чуть светлее фона
- `--line` `#4a3170` — рамка панели, 1 px
- `--ink` `#f3eefb` — текст · `--muted` `#a99cbe` — подписи
- `--accent` `#b48cff` — цифры, кнопки, номера картинок, ссылки
- `--green` `#4ecf8a` — «хорошо» · `--red` `#e5645d` — «нужен ты»
- `--yellow` `#efc65c` — плашка с именем папки проекта в шапке

## Приёмы

- **Неон.** Под шапкой полоска: градиент прозрачный → `--accent` → прозрачный, 3 px,
  `box-shadow:0 0 22px rgba(180,140,255,.55)`. Крупные цифры — `text-shadow:0 0 26px rgba(180,140,255,.45)`,
  кнопки — `box-shadow:0 0 20px rgba(180,140,255,.35)`.
- Светится только акцентное: полоска, цифры, кнопки, номера. Фон, текст и панели — нет: читаемость важнее.
- Скругления 14 px (панели 16 px), рамки 1 px. Цифры и подписи — моноширинным (`ui-monospace,Menlo`).
- Анимация одна: «дыхание» полоски, 6 с. Ничего плавающего и липкого.
