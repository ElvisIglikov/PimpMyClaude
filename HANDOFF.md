# ⚪PimpMyClaude — HANDOFF

Заведён 08.09.2026 10:00 (его требует `SkilZZZ/WORKFLOW.md`, а в проекте файла не было — вход новый чат искал
в `CLAUDE.md`). Порядок чтения для нового чата: `CLAUDE.md` (паспорт и контракты) → этот файл → `docs/status.md`
(счёт) → run-файл прогона в статусе «идёт» → план рядом с ним.

**Разделение с паспортом (08.09, WF44):** в `AGENTS.md` только устройство, контракты и решения, которые не
меняются от прогона к прогону; всё сиюминутное — здесь. Раздел «Текущее» и «Открытые хвосты» из паспорта
переехали в этот файл, чтобы состояние жило в одном месте и не расходилось.

## Где стоим

Обновлено 08.09.2026, 13:10. Заведено 48 воркфлоу, 33 закрыто (счёт — `docs/status.md`); у Элвиса стоит
сборка WF41 из main, версия **1.4.0 (8)**, у команды — 1.3.0, а инструкция у неё вообще от 1.2.0 (релиз
1.4.0 команде = WF17, не сделан).

Идёт **WF43 «Чистка по Ревизору»** (`docs/plan-wf43-2026-09-08.md`, `docs/run-wf43-2026-09-08.json`):
**волна 1 принята проверяющим и слита** (канал и раскладки, окна и экраны, страница; сорок починок),
сейчас идёт **волна 2** — тесты там, где их не было; «Обкэшить» заменяет окно; имя раскладки предлагается
само; дешёвое первое сообщение; документы. Из запущенных разом по слову Элвиса **WF44 (документы),
WF45 (патчер), WF46 (проекты) закрыты и слиты в `wf43`**; **WF47 и WF48 идут** (WF47 упёрся в живую
разведку — она на Элвисе).

Ревизия всего кода закончена (WF42): 12 зон, 56 находок, опровергнуть не удалось ни одной — выжимка
`docs/revizor-wf42-2026-09-08.md` (518 КБ, читать кусками по заголовкам через `grep -n`), полные данные
`docs/revizor-wf42-2026-09-08.json`, страница для Элвиса `docs/revizor-wf42-elvisu.html`.

Живой `inject.js` = репозиторный main. Тесты — один прогон `bash tools/test.sh`; числа тестов берутся из
самого прогона и в документы не переписываются.

## Что ждёт Элвиса

1. **20–30 минут в главном окне на живую разведку первого сообщения** (#5447, WF47): без неё новое окно
   так и стоит хода дорогой модели — разведка считает, чем заменить «Привет». Сказать «давай» или «позже».
2. Ручная проверка раскладок: плитка «5 в ряд» в меню → «⋯ Ещё ▸ 🗂 Раскладки ▸ 💾 Сохранить…» → закрыть
   одно окно → «↩︎ Вернуть эти чаты» (#5620); сказать любому чату «Пимп, пять в ряд» (#5621). Шаги —
   в `docs/CHECKLIST.md`, раздел 1.4б. Плитки мышью и «запомни → верни» живьём не запускались ни разу.
   Тумблер «🗂 Цвет по проекту» для пункта меню «💾 Сохранить…» больше не нужен — он сам просит круг опознания
   чатов и ждёт до 8 с (#5770); та же просьба из чата («Пимп, запомни раскладку») тумблера всё ещё требует.
3. Ручная проверка «Обкэшить» из вынесенного окна (`docs/CHECKLIST.md`, раздел 1.4в): ⇧⌘N в попапе —
   рядом рождается окно с последним ответом и черновиком, донор цел.
4. Ответить на вопросы ревизии — страница `docs/revizor-wf42-elvisu.html` (карта функций и спорные места, #5710).
5. Буквы по макетам WF32 (`docs/mockup-wf32.html`), WF33 (`mockup-wf33.html`), WF28 (`mockup-wf28.html`) —
   без них эти воркфлоу не кодить (правило макета). Спрашивать словами, ответа буквами не просить (#5771).
6. Релиз 1.4.0 команде: у неё стоит 1.3.0, инструкция рядом с ней — от 1.2.0 (#5381).
7. Проверка на своём Маке у Дениса и Эрика — macOS 13–15, ответов пока ноль (#5386): список на 10 минут —
   `docs/CHECKLIST.md`, часть 2; главное — скриншот угла окна с рамкой.
8. Живой «Снять → Поставить» новым Swift-патчером — на его Маке, закрывает чат оркестратора.
9. Нажать «Change directory» в чате и увидеть, что цвет поехал за папкой (#5448): без мыши цепочка
   «чат → папка → цвет» проверяется только тестами.
10. Наведение на «Мои темы» с паузой — шаг гейта WF31, мышью не гонялся.

## Решения Элвиса дословно, с датами

- 08.09.2026 ~11:55: «запускай очень много Workflow» → WF44–WF48 запущены разом с идущим WF43; правило
  WORKFLOW.md 08.09 — прогоны параллельно, если не пересекаются по файлам и сборке.
- 08.09.2026 11:40 (#5771): «А что такое A? Почему 5A? Ничего не понял» → вопросы в HTML-файлах нумеровать
  и задавать словами, ответа буквами («5A 6B») не просить. Правило записано в `SkilZZZ/HTML.md`.
- 08.09.2026 утро: «Сейчас Fable подписка заканчивается. Поэтому временно в этом чате оркестратором будет
  Опус 5 Макс. Фейболов в этом чате не запускай. В остальных чатах действуют старые стандартные правила».
  → в прогонах ЭТОГО чата все роли (критик, кодинг, проверка) идут на Opus 5 max.
- 08.09.2026 утро: «Мне нужен рефакторинг этого программного кода» + «перед рефакторингом мне нужно тебе по
  функциям сказать, какая работает, какая не работает» → карта функций сделана обязательной зоной ревизии.
- 08.09.2026 ночь: ответ Элвису — четыре строки простыми словами, без терминов и номеров задач в тексте;
  «жду» рисуется значком ⚠️, а не ✋ (#5663).
- 08.09.2026 01:15: «делаем, как ты хочешь» → вариант А по раскладкам (WF21 сетка 4/5/5×2 и голос).
- 07.09.2026 (#5637): «ты главный» → чат Пимпа руководит чатами проектов и гоняет много воркфлоу разом.
- 07.09.2026 вечер: порядок ответа «итог → решение → варианты → вопрос»; вопрос всегда со своим вариантом
  «(рекомендую)»; макеты и исследования подписывать «сделал: роль · модель эффорт · дата» (#5570).
- 05.09.2026: отклонено — полноценные окна Claude (#5449), память позиций и «Спрятать все».
- 03.09.2026: лоадер в `app.asar` не трогать без крайней нужды — его смена = перепатч Claude у всей команды.

## Хвосты (задач в Trelvis на них нет — завести при случае)

- Живой «Снять → Поставить» Swift-патчером на Маке Элвиса и на чистом пользователе macOS; отзыв первого из команды.
- Поведение обновлений Claude (Squirrel/ShipIt) после ad-hoc подписи — не проверено; `docs/TEAM.md` обещает
  «Поставить снова».
- **Вес `status.json` и `probe-result.json`** — по ~3,9 МБ: лоадер кладёт в них `url` каждой из четырёх десятков
  страниц, а у артефакта `url` это `data:`-адрес целиком. Лечится только правкой лоадера, то есть перепатчем
  у всей команды.
- StatusFeed: подсказка полоски со сводками только там, где есть `~/_ElvisProjects` (`projectsRoot` в claude.json);
  при 9+ проектах хвост списка по алфавиту отбрасывается (32 КБ).
- **Карта тем на странице растёт вдвое**: на чат теперь два ключа (`id:` и тень `chat:`), у localStorage потолка
  нет, уборки нет. У файла потолок есть (32 КБ + 200 ключей).
- **Позиции окон по id чата**: поле `pos` в записи темы зарезервировано и никем не пишется, владелец
  геометрии — WF21. Оценка отдельной работы +1,5 ч.
- `row` активной строки сайдбара в главном окне = null (`myChatId` берётся по пути); адресом поле `chat`
  служит только у команд `theme` и `cashout` (у `popout-window` это груз — какой чат вынести);
  тень `id:` над более полной `chat:` при `themes-restore` (verify WF35);
  безымянный попап («Claude») уносит ключ `main`.
- **Возврат цвета проекта и живых цветов на перезапуск Claude (WF35)** живым гейтом не проверяется:
  `ClaudeApp.onRestart` висит на смене pid **Claude**, и `pkill` приложения его не вызывает вовсе. Закрыто
  Swift-тестами; один ручной перезапуск Claude — на Элвисе.
- Lua-модули в `hammerspoon/` — архив, только источник логики: тем и полоски они не знают.
- Настройки Элвиса, которые менял агент 06.09 (сказано ему): `projectPaintEnabled` включён (был 0), `themeAll`
  (violet, «всем окнам») снят — оба глушили цвет проекта.
- Задачи, закрытые агентом и ждущие проверки Элвиса, смотреть в 🟣Trelvis (слаг `other`) — списками номеров
  их здесь не держим, они там же и протухают.

## Следующий шаг

Гейт волны 2 WF43 (полный `tools/test.sh`, коммит на батч) → слияние `wf43` в `main` `--no-ff`.
В волне 2: Т (страница и тесты), К1 («Обкэшить» заменяет окно, имя раскладки само) → К2 (команды,
авто-Allow, дешёвое первое сообщение), Д2 (паспорт и чеклист), проверяющий. WF47 ждёт живой разведки
Элвиса. Сторож прогона — крон в чате оркестратора, каждый час в :23.
