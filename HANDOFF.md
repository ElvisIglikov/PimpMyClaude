# ⚪PimpMyClaude — HANDOFF

Заведён 08.09.2026 10:00 (его требует `SkilZZZ/WORKFLOW.md`, а в проекте файла не было — вход новый чат искал
в `CLAUDE.md`). Порядок чтения для нового чата: `CLAUDE.md` (паспорт и контракты) → этот файл → `docs/status.md`
(счёт) → run-файл прогона в статусе «идёт» → план рядом с ним.

**Разделение с паспортом (08.09, WF44):** в `AGENTS.md` только устройство, контракты и решения, которые не
меняются от прогона к прогону; всё сиюминутное — здесь. Раздел «Текущее» и «Открытые хвосты» из паспорта
переехали в этот файл, чтобы состояние жило в одном месте и не расходилось.

## Где стоим

**20.09.2026, 01:10 — WF75 «Голосовой Пимп» закрыт, приложение `1.5.3 (12)` стоит у Элвиса в `/Applications`.** Слово
Элвиса 19.09 ~22:50 (#6540–#6548, приоритет максимальный). `tools/pimp.py say <фраза|->` — мозг: DeepSeek
`deepseek-v4-flash` без рассуждений (0,9–1,4 с, 10 живых фраз Элвиса из 10 верно), белый список шагов, ворота `close` и
`paste_airdrop` в коде, журнал `voice-log.jsonl`; промпт — `tools/voice_prompt.md` (правится без сборки). Канал получил
`new-window chat:last`, `close-window`, `paste`, `hud` (контракт — `docs/CONTRACTS.md`, «Голосовой Пимп (WF75)»). Вживую:
плашка, «что открыто», «открой последний чат по Скиллз», «закрой чат Скиллз». НЕ проверено живьём: вставка (⌘V ушёл бы в
черновик Элвиса). Ключ — `~/Library/Application Support/MyClaude/pimp-voice.env` (вписал Элвис). Команде 1.5.3 НЕ
раздавалась (нотаризации не было). Дальше: Диктатор режет фразу (#6548, `docs/voice-dictator-handoff.md` — делает чат
Диктатора); WF76 — «как вчера» (#6545, снимок дня через `LayoutsStore.mergeDaily`, см. критика WF75), темы/шрифты/«отправь»
голосом. План `docs/plan-wf75-2026-09-19.md`, критик `docs/critic-wf75-2026-09-19.md`, журнал `docs/run-wf75-2026-09-19.json`.

**19.09.2026, 20:15 — сборка `1.5.2 (11)` для Маши** (#6532): `main` как есть, проверки зелёные, нотаризация принята,
`dist/PimpMyClaude-1.5.2.zip` + `TEAM.md` (версия 1.5.2, раздел «Исходники») лежат на рабочем столе Элвиса. Репозиторий
на GitHub ПУБЛИЧНЫЙ — клонировать Маша может и так; право писать (`mashamirrr`, collaborator) выдаёт Элвис сам — агенту
это действие запрещено. У самого Элвиса в `/Applications` сборка не подменялась. Следующая версия — `1.5.3 (12)` или `1.6.0`.

**17.09.2026, 23:15 — план WF72–WF74 «Пимп как приложение» написан, разнесён критиком и ждёт слова Элвиса.** По его
диктовке 22:10 (задачи #6366 значок, #6367 одно меню, #6368 архив отчётов; #6282 статистика от 19:00): чат открыт на
**Opus 5 max** («ты оркестратор пока на Опусе, Fable не хватает») — на этот прогон все роли на Opus max. План —
`docs/plan-wf72-2026-09-17.md` (три воркфлоу: значок со звездой и точкой «патч стоит» + одно меню везде со служебным
«⚙️ Пимп ▸»; счётчик расхода по транскриптам с леджером записей ответов + окно Пимпа со статистикой + цена воркфлоу
на карточке по прогонам; архив HTML-отчётов с просмотром и страницей «Как это работает»). Критик Opus max (7 блокеров,
10 замечаний) — всё внесено: модель денег переписана по прогонам `wf_<runId>.json` (окно времени по mtime врало при
параллельных воркфлоу), леджер — записи ответов с проверкой перезаписи файла, цвет проекта из его темы, `usd` как `%.2f`
(канал пишет только Int — волна 2 добавит `CommandValue.decimal`), Package.swift с заглушками до волны («один build на
волну»), меню под значком через `performClick`. Макет — `docs/mockup-wf72-pimp.html` (картинки первыми: значок, меню,
окно статистики, окно отчётов, страница объяснения, карточка с ценой). Промт Астре на значок и иконку приложения —
`audit/astra-prompt-icon-2026-09-17.md`. Кодинга не было; status.md прошлого чата унесён в `status-archive.md`.
**Ждёт Элвиса:** слово «делай» по макету; ответ про SkilZZZ на GitHub (репозиторий приватный, а страница «Как это
работает» ведёт на него); ответ про деньги (сначала только «если бы по API»); скопировать промт Астре. Кандидат на «то
важное, что забыл» — HEIC-фото в чате (#6365, заведена 17.09 17:07 из другого чата), в план не входит.
**Следующий шаг:** по «делай» — тег `wf72-start`, ветка `wf72`, Package.swift с таргетами `UsageStats`/`PimpPanel` и
заглушками, крон-сторож, волна 1 (A меню/значок, B счётчик, C1 окно Swift, C2 страницы, D карточка), гейт с тремя
проверяющими, волна 2 (стыковка), версия `1.6.0 (11)`, сборка и подмена.

## Что ждёт Элвиса

00000000000. **Сузить обе панели своей рукой** (20.09 03:50, #6625 — первый шаг из #6592 и #6582): левую панель Claude
             тянуть за её же рейку левее прежнего упора — до кнопок «Hide sidebar» и Claude/Code вплотную (190 вместо 242);
             правую колонку ввода — рейкой до 248 (было по строке модели, ~308), подписи внизу при этом сокращаются
             многоточием. Двойной клик по рейке — ширина по умолчанию. Живёт в живой странице `wf76-a-3`, в бандл
             приложения не собрано. Оформление правой как левой, «Medium + кружок», кнопка «поле вниз» — НЕ сделаны.

0000000000. **Попробовать вставку голосом** (20.09 01:10, WF75): встать в чат Claude и набрать в терминале или сказать
            через Диктатор «Пимп, вставь фотки с эйрдропа» / «вставь последнюю диктовку» — живьём не нажималось (#6541, #6542).

000000000. **Сказать «делай» по макету Пимпа** (17.09 23:15, WF72–WF74): `docs/mockup-wf72-pimp.html` — значок со
           звездой и зелёной точкой, одно меню на значке и жёлтой кнопке, окно Пимпа (статистика, отчёты, «Как это
           работает»), цена на карточке. Там же два вопроса: SkilZZZ на GitHub сделать публичным (рекомендую) и
           деньги пока только «если бы по API» (рекомендую). Промт Астре на значок — скопировать ей из
           `audit/astra-prompt-icon-2026-09-17.md` (#6366, #6367, #6368, #6282).

00000000. **Правая колонка по строке модели** (17.09 10:00, WF68): в широком окне колонка с полем ввода
          теперь ровно такая, чтобы «Auto» и «Fable 5.1» стояли рядом; поле стало уже. Глянуть и сказать
          «ок» или «ещё уже / шире» (#6212).

0000000. **«Открыть в Chrome»** (17.09 05:30, WF67): правый клик по карточке html-файла в чате → первым пунктом
         жирный «🌐 Открыть в Chrome» → Chrome показывает файл, меню закрывается. У не-html карточек пункта нет
         нарочно (мост открывает в программе по умолчанию, `.md` ушёл бы в Xcode). Проверено изнутри синтетическим
         кликом; его рукой — сказать «ок» (#6190). Заодно свечение полосок воркфлоу тише (6/2 и 6/3).

000000. **Полосы прокрутки у кромок** (17.09 04:20, WF66): обе полосы, ленты и поля ввода, теперь в двух точках
        от кромки, как у ChatGPT (слово Элвиса). Глянуть с длинным черновиком в поле.

00000. **Шапка влево при свёрнутой панели чатов** (17.09 03:55, WF66): свернуть боковую панель — заголовок
       чата с папкой и кнопками встаёт в полосу под кнопками окна слева, лента под ним, поле ввода справа во
       всю высоту; развернуть панель — шапка снова справа. Сказать «ок» или что не так. Дальше по выбору
       Элвиса 03:25: «Открыть в Chrome» правой кнопкой (#6190, первым), задержка цвета проекта (#6196, «если
       возможно»), ревизор на **Opus 5 max** (слово Элвиса: Fable жалко) перед выпуском команде. Отменено им же:
       компактная лента, ширина колонки на чат; «Обкэшить» он больше не пользуется — живые проверки 1–2 ниже
       снимаются с очереди, функцию не трогать; тема «Вкуснофф» принята (рамки годятся, #6025 закрыта).

0000. **Рейка и полоса прокрутки — последняя редакция** (17.09 03:35, WF66): полоса прокрутки ленты на 4
      левее поля ввода (Элвис: «посередине» не понравилось, «чуть-чуть левее, чем прилеплена»), линия рейки
      проявляется через 0,4 с наведения и гаснет сразу, вполсилы. Сказать «ок» или что сдвинуть. Домашний
      экран без сетки подтверждён probe (#6187, #6188).

000. **Посмотреть третью редакцию поля ввода** (17.09 02:25, WF66): вложения снизу Элвис уже подтвердил
     («встали»); после его слов «рейка огромная, страшная» и «компактненько» рейка стала невидимой зоной с
     линией на наведении (как у Claude), правая колонка подтянута к тексту на 12 точек, зазор между текстом и
     плитками — 4. Сказать, стало ли компактно; открыть домашний экран (новый чат) — там раскладка Claude,
     «Welcome back» на месте (#6187, #6188).

0. **Пожить с широким видом окна** (16.09 вечер): развернуть окно на две трети экрана и шире — три колонки;
   потянуть рейку между лентой и полем ввода, открыть меню модели рядом с ней, сузить окно с текстом в поле и
   расширить обратно. Сказать, что не так: ширина колонки по умолчанию (треть), порог включения, отступы поля
   (#6176, #6185). Отдельно посмотреть попап: имя папки в шапке целиком, полоса прогресса с зазором (#6177, #6178).

0б. **Проверить ⌘V своей рукой** (13.09 вечер): скопировать скриншот, кликнуть в ленту разговора и нажать
   ⌘V — вложение обязано приехать в поле ввода ОДНО. В артефактах и встроенном браузере не заработает
   (#6030).

0. **Проверить низ поля ввода** (13.09 утро): положить в поле несколько скриншотов и длинный текст —
   кнопка отправки и строка модели обязаны остаться видны, вложения прокручиваются внутри своей
   полосы (#5951).

3. **20–30 минут в главном окне на живую разведку первого сообщения** (#5447, WF47): без неё новое окно
   так и стоит хода дорогой модели — разведка считает, чем заменить «Привет». Сказать «давай» или «позже».
5. Ручная проверка цвета: нажать «Change directory» в чате и увидеть, что цвет поехал за папкой (#5448);
   на домашнем экране сменить чип папки и увидеть, что окно перекрасилось (`docs/CHECKLIST.md`, 1.4в, (г)).
   Без мыши цепочка «чат → папка → цвет» проверяется только тестами.
6. Проверка на своём Маке у Дениса и Эрика — macOS 13–15, ответов пока ноль (#5386): список на 10 минут —
   `docs/CHECKLIST.md`, часть 2; главное — скриншот угла окна с рамкой.
7. Релиз 1.4.0 команде: у неё стоит 1.3.0, инструкция рядом с ней — от 1.2.0 (#5381).
8. Ответить на вопросы ревизии — страница `docs/revizor-wf42-elvisu.html` (карта функций и спорные места, #5710).
10. Живой «Снять → Поставить» новым Swift-патчером — на его Маке, закрывает чат оркестратора.
11. Наведение на «Мои темы» с паузой — шаг гейта WF31, мышью не гонялся.

Старое, но не закрытое (Элвис не говорил ни «сделал», ни «отменяю»):

0б. **Положить пять скриншотов в поле узкого окна** (12.09 ночь): они обязаны встать по три в ряд и не
   закрыть черновик; крестик снятия должен быть целым (#5912).

0в. **Посмотреть яркие цвета** (12.09): жёлтая кнопка окна → в меню три списка (Тёмные, Светлые, Яркие) и
   «Мои темы» отдельно выше. Сказать, годятся ли цвета (#5801 закрыта, ждёт его глаза).

4. Ручная проверка раскладок: плитка «5 в ряд» в меню → «⋯ Ещё ▸ 🗂 Раскладки ▸ 💾 Сохранить…» → закрыть
   одно окно → «↩︎ Вернуть эти чаты» (#5620); сказать любому чату «Пимп, пять в ряд» (#5621). Шаги —
   в `docs/CHECKLIST.md`, раздел 1.4б. Плитки мышью и «запомни → верни» живьём не запускались ни разу.
   Тумблер «🗂 Цвет по проекту» для пункта меню «💾 Сохранить…» больше не нужен — он сам просит круг опознания
   чатов и ждёт до 8 с (#5770); та же просьба из чата («Пимп, запомни раскладку») тумблера всё ещё требует.

9. Буквы по макетам WF32 (`docs/mockup-wf32.html`), WF33 (`mockup-wf33.html`), WF28 (`mockup-wf28.html`) —
   без них эти воркфлоу не кодить (правило макета). Спрашивать словами, ответа буквами не просить (#5771).

## Решения Элвиса дословно, с датами

- 19.09.2026 ~23:30 (WF75): «Просто сделай, как я сказал… побыстрее… в режиме Workflow, опусов преимущественно, эффорты
  можешь подснизить»; «нам нужна не Pro, а Flash… та версия, которая справляется, та и нужна»; фраза после «Пимп» уходит в
  DeepSeek — его слово; «чем левее, тем важнее чат», Вкуснофф всегда первым слева (живёт в `tools/voice_prompt.md`).

- 17.09.2026 22:10 (WF72): «ты оркестратор пока на Опусе, Fable не хватает» → на этот прогон все роли (критик, кодинг,
  проверка) на Opus 5 max; «людям нравится, когда везде всё одинаково по интерфейсу… нужно сделать и на минус жёлтый,
  и на иконку — чтоб одинаково всё работало» → одно меню на значке и на жёлтой кнопке; «давай значок Claude…
  чёрно-белую… чтоб самобытное что-то было, общую идею обыграть» → своя звезда с точкой состояния, картинку рисует Астра;
  «в Пимпе нужно хранить все HTML-файлики… в историческом виде… внизу кнопка „как это работает“ с промтом, „скилы должны
  быть подключены“» → окно Пимпа с вкладкой «Отчёты» и страницей объяснения; «админку проработай без меня, потом покажу,
  как в других проектах» → статистика по Радуге, круговая и деньги подписки — после его показа.
- 17.09.2026 09:41 (WF68): «между Авто и Fable 5 ещё есть место… можно его поджать, чтобы они близко друг к
  другу стали… и тогда поле чуть поуже станет… минимальную ширину поля рассчитать так, чтобы эти поля
  сошлись» → нижняя граница колонки считается по строке модели, а не числом 300.
- 16.09.2026 20:33 (WF65): «Как рекомендуешь, делай» — широкий вид включается, «когда тексту хватает места»
  (порог по плитке чата); ширина правой колонки — «сделай, как считаешь нужным, потом скорректирую» + «менять
  ширину справа… иногда поуже, иногда пошире» → рейка; затемнения — «и в узком окне тоже убери, они не нужны»;
  «пусть кодит Fable 5 High, Fable-ы быстрее кодят значительно» → кодинг Fable high на этот прогон; боковые
  плитки (терминал, браузер) — «пока не убирай».
- 16.09.2026 19:50 (WF65): «Fable 5.1 Extra пусть участвует в рисовании… Fable High пусть тоже проверит» →
  макет — оркестратор, проверка — Fable high.
- 08.09.2026 ~11:55: «запускай очень много Workflow» → WF44–WF48 запущены разом с идущим WF43; правило
  WORKFLOW.md 08.09 — прогоны параллельно, если не пересекаются по файлам и сборке.
- 07.09.2026 (#5637): «ты главный» → чат Пимпа руководит чатами проектов и гоняет много воркфлоу разом.
- 05.09.2026: отклонено — полноценные окна Claude (#5449), память позиций и «Спрятать все».
- 03.09.2026: лоадер в `app.asar` не трогать без крайней нужды — его смена = перепатч Claude у всей команды.

## Хвосты (задач в Trelvis на них нет — завести при случае)

- Живой «Снять → Поставить» Swift-патчером на Маке Элвиса и на чистом пользователе macOS; отзыв первого из команды.
- Поведение обновлений Claude (Squirrel/ShipIt) после ad-hoc подписи — не проверено; `docs/TEAM.md` обещает
  «Поставить снова».
- **Вес `status.json` и `probe-result.json`** — по ~3,9 МБ: лоадер кладёт в них `url` каждой из четырёх десятков
  страниц, а у артефакта `url` это `data:`-адрес целиком. Лечится только правкой лоадера, то есть перепатчем
  у всей команды.
- StatusFeed: подсказка полоски со сводками только там, где есть `~/_ElvisProjects` (`projectsRoot` в claude.json);
  при 9+ проектах хвост списка по алфавиту отбрасывается (32 КБ).
- **Карта тем на странице растёт вдвое**: на чат теперь два ключа (`id:` и тень `chat:`), у localStorage потолка
  нет, уборки нет. У файла потолок есть (32 КБ + 200 ключей).
- **Позиции окон по id чата**: поле `pos` в записи темы зарезервировано и никем не пишется, владелец
  геометрии — WF21. Оценка отдельной работы +1,5 ч.
- `row` активной строки сайдбара в главном окне = null (`myChatId` берётся по пути); адресом поле `chat`
  служит только у команд `theme` и `cashout` (у `popout-window` это груз — какой чат вынести);
  тень `id:` над более полной `chat:` при `themes-restore` (verify WF35);
  безымянный попап («Claude») уносит ключ `main`.
- **Возврат цвета проекта и живых цветов на перезапуск Claude (WF35)** живым гейтом не проверяется:
  `ClaudeApp.onRestart` висит на смене pid **Claude**, и `pkill` приложения его не вызывает вовсе. Закрыто
  Swift-тестами; один ручной перезапуск Claude — на Элвисе.
- **Перенос везёт только вложения ПОЛЯ ВВОДА**: картинки и файлы из ленты разговора не переносятся вовсе (в
  ElvisOS переносились). Полка `window.__myclaudeFiles` живёт в памяти окна — перезагрузка страницы её теряет.
- Lua-модули в `hammerspoon/` — архив, только источник логики: тем и полоски они не знают.
- Настройки Элвиса, которые менял агент 06.09 (сказано ему): `projectPaintEnabled` включён (был 0), `themeAll`
  (violet, «всем окнам») снят — оба глушили цвет проекта.
- Задачи, закрытые агентом и ждущие проверки Элвиса, смотреть в 🟣Trelvis (слаг `other`) — списками номеров
  их здесь не держим, они там же и протухают.

## Следующий шаг

По слову «делай» на макет WF72 — точка отката `wf72-start`, ветка `wf72`, Package.swift с таргетами `UsageStats` и
`PimpPanel` (заглушки, зелёный `swift build`), крон-сторож, волна 1 из пяти батчей по `docs/plan-wf72-2026-09-17.md`,
гейт с тремя проверяющими, волна 2 (стыковка), версия `1.6.0 (11)`, сборка и подмена без простоя, живой `inject.js`
`wf72-a-1`. Журнал — `docs/run-wf72-2026-09-17.json`. Дальше — прежнее:

Слово Элвиса по широкому виду и по колонке WF68 (пункты 0 выше) → правки констант (порог 640, зазор
`SIDE_CHIN_GAP` 4, умолчание `clamp(var(--myclaude-side-min, 300px), 36%, 420px)`)
одной волной, если попросит; потом версия `1.5.2 (11)`, `tools/bundle.sh release` и подмена без простоя — чтобы
«Поставить» у команды нёс новую страницу (сейчас в бандле приложения ещё `wf64`). Дальше — прежний список:

---

Старые записи — в `HANDOFF-archive.md` рядом (перенесено 18.09.2026 дословно, ничего не потеряно).
Дописывая своё, уноси всё старше двух последних записей в `HANDOFF-archive.md`: этот файл читает каждый
новый чат, потолок — 30 КБ (`SkilZZZ/WORKFLOW.md`).
