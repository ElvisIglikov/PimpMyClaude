# ⚪PimpMyClaude — памятка агенту (читать первой, до README)

Обновлено 06.09.2026 (ветка `wf35`). Это файл-передача: новый чат открывает папку проекта и продолжает отсюда, ничего не
додумывая. Общие правила общения и воркфлоу 👾 Элвиса — `/Users/elvis/_ElvisProjects/SkilZZZ/AGENTS.md` и
`WORKFLOW.md` (прочитать). Задачник 🟣Trelvis: слаг проекта `other` (⚪ Другое); задачи регистрировать через
`python3 /Users/elvis/_ElvisProjects/TrelvisCom/trelvis.py`, актор `fable`.

## Что это

Прокачка Claude Desktop (macOS) для 👾 Элвиса и его команды (Маша, Алла, Денис, Саша Костина, Эрик, Тимон):
узкие окна, ручка над полем ввода, меню на жёлтой кнопке окна, темы/шрифты/размер/рамка на каждое окно и чат,
полоса прогресса воркфлоу, авто-Allow, блок ⌘Q, кнопка «🚀 Workflow» (вставляет стартовый текст подхода Элвиса).
Бывший MyClaude (переименован 03.09.2026). GitHub `ElvisIglikov/PimpMyClaude`, ветка `main`.

Раздаётся как **PimpMyClaude.app** (Swift/AppKit, Developer ID «ELVIS IGLIKOV (F27N4S4NJ4)», нотаризация через
профиль keychain `pimpmyclaude`). Текущий релиз **1.3.0 (7)**, zip `dist/PimpMyClaude-1.3.0.zip` (копия на рабочем
столе Элвиса вместе с `docs/TEAM.md` — инструкция команде). У Элвиса приложение стоит в `/Applications`, старые
Hammerspoon-модули из его `init.lua` сняты 03.09.

## Как устроено (три слоя)

1. **Патч Claude.app** — `claude-patch/patch-claude.mjs` (Node, дев-инструмент) и его порт `app/Sources/Patcher`
   (Swift, боевой): в `app.asar` дописывается **лоадер v6** (строка `LOADER` в mjs / `Loader.swift`), пересчёт
   `ElectronAsarIntegrity`, переподпись ad-hoc, бэкап в `~/Library/Application Support/MyClaude/backups/<версия>/`.
   Лоадер НЕ ТРОГАТЬ без крайней нужды: его смена = перепатч Claude у всей команды («Поставить снова»).
   Папка `~/Library/Application Support/MyClaude/` — так и называется (лоадер смотрит туда), не переименовывать.
2. **Живые файлы в Application Support/MyClaude/**, лоадер перечитывает их по mtime без перезапуска Claude:
   `claude.json` (minWindowWidth, sidePadding, projectsRoot), `claude.css` (user-стили, `insertCSS`), **`inject.js`**
   (исполняется в каждой странице Claude при dom-ready и при каждом изменении файла; обязан быть идемпотентным —
   `window.__myclaude.dispose()`), `command.json` (команды `{id, action, at, …}`; лоадер читает раз в 500 мс и берёт
   ПОСЛЕДНЮЮ запись — отсюда очередь в Swift), `probe.js` → `probe-result.json` (любой JS во все страницы, ответ в файл —
   главный инструмент гейта; **с WF29 этот канал делят агент и приложение** — правило владения в «Контрактах»),
   `status.json` (диагностика лоадера, имя занято), `themes.json`, `my-themes.json`,
   `installed.json`, `workflow/` (комплект KICKOFF.md + WORKFLOW.md). Рядом лежит **`window-themes.json`**
   (WF35) — копия карты тем окон; лоадер его НЕ читает, это файл самого приложения
   (`app/Sources/ClaudeAX/WindowThemeStore.swift`), см. контракт `themes-restore` ниже. Там же файлы WF36,
   лоадеру тоже неизвестные: каталог **`pimp/`** — канал «Пимп» (`<id>.json` запрос, `<id>.taken` взят
   в работу, `<id>.result.json` ответ; права 0600, файлы старше часа приложение убирает на тике) и
   **`projects.json`** (`{"version":1,"projects":[{name,folder,lastUsed}]}` — свой список недавних проектов
   для меню «🪟 Новое окно ▸» и для канала; переживает переустановку Claude, пропавшие с диска папки
   не показываются, но из файла не удаляются).
   Источник правды — `claude-patch/` в репозитории; «Поставить» и `bundle.sh` копируют оттуда. Живой `inject.js`
   руками не править — только репозиторный, потом `cp` в Application Support (лоадер подхватит).
3. **PimpMyClaude.app** — `app/` (SwiftPM, macOS 13+, Swift 5 mode). Таргеты: `Patcher`, `ClaudeAX`
   (AX-модуль: авто-Allow, меню на кнопке, Carbon-хоткеи, ⌘Q, Расставить/Показать, темы/шрифты/размер/рамка,
   автопокраска, StatusFeed, WorkflowKit, очередь `CommandChannel`, канал чатов `ChatProbe`), `PimpMyClaude` (меню-бар, окно настройки
   разрешений, уведомления, Поставить/Снять/Статус). Протокол `ClaudeAXControlling` в `ClaudeAX.swift` — контракт
   между модулями, менять только осознанно.

## Контракты (побайтно, страница и Swift обязаны совпадать)

- `theme`: `{"id","action":"theme","at","scope":"window"|"all","title":<AX-заголовок окна>,"preview":true|false?,
  "theme":{id,name,type,palette{accent,background,foreground,sidebar,panel,muted}}|null?,
  "font":{id,family,mono}|null?, "size":{answer?,question?}|null?, "frame":true|null?}` — поле есть → слой меняется,
  нет → не трогать, null → сброс; `preview:true` — только на экран, `preview:false` без слоёв — конец примерки.
- `workflow`: `{…,"scope":"window","title","text"}` — вставить текст в поле ввода, не отправлять.
- `status`: `{…,"scope":"all","projects":[{name,text}]}` — сводки status.md проектов для подсказки полоски.
- `new-window` (WF13): `{…,"scope":"window","title","x":<число>,"y":<число>,"text":"Привет"}` — главное окно: ⌘N (жмёт Swift),
  первое сообщение, чат выносится popout-окном (стор claude.ai `openPopout`, ищется поведенчески через `import()`
  чанков modulepreload), главное окно возвращается на прежний чат по строке сайдбара; `status().newWindow`.
- WF16: `new-window` получил поля `folder` (абсолютный путь, "" — не трогать), `name` (имя чата) и слои `theme/font/size/frame`
  как у команды `theme` (порядок: scope,title,x,y,text,folder,name,[theme],[font],[size],[frame]); папка ставится через стор
  Claude (`setLocalSelectedFolder`+`setTrustedSelectedFolder`, поиск поведенчески), имя — через DOM-меню строки с деградацией.
- `popout-window` (WF13): `{…,"scope":"window","title","x","y"}` — вынести текущий чат главного окна в отдельное окно.
- Адресация окна (WF15): у любой команды с `scope:"window"` необязательное поле `match` — путь страницы
  (`/epitaxy/local_<id>`); есть строка → страница сверяет `location.pathname`, поля нет — по `title`/фокусу, как раньше.
  Главное окно так и адресуется (его заголовок «Claude» — заглушка).
- Адресация окна по чату (WF29): у команды `theme` необязательное поле `chat` — id чата (`local_<uuid>`).
  Порядок полей побайтно `scope, title, match?, chat?, preview?` + слои (`ClaudeActions.themeFields`,
  `ClaudeActions.swift:396`). Есть строка → страница сверяет её со своим id (`myChatId()`) и заголовок не смотрит
  вовсе; свой id неизвестен → команда не наша, и приложение пришлёт её заголовком на следующем тике
  (`addressed()`, `inject.js:1268`; порядок проверок `match` → `chat` → заголовок/фокус). Приложение шлёт `chat`
  только той странице, которая сама назвала свой id в последнем круге probe, и `match` с `chat` разом не шлёт
  никогда. Другим командам поле в WF29 не добавлено.
- Настройки проекта (WF15, правило записи WF20): файл `.pimpmyclaude.json` в корне проекта (`{"pimpmyclaude":1,"name","theme","font","size","frame"}`,
  ключ отсутствует → слой не трогать, `false` у frame → сброс), контракт побайтно — `ProjectSettings.swift`; индекс чатов
- WF20: подменю «🗂 Проект ▸» больше нет — остался тумблер «🗂 Цвет по проекту» в «🖥 Всем окнам ▸». Папки без файла
  красятся авто-цветом по имени (FNV-1a → тон, `AutoPaint.projectTheme(folderName:)`, отпечаток `auto:<hue>`, файла не
  заводит). Ручной выбор темы/шрифта/размера/рамки в окне проекта (`remember`, только `scope:"window"`) молча пишется в
  `.pimpmyclaude.json` (`ProjectPaint.noteManualChoice`); «Как у Claude» снимает слой, последний слой удаляет файл; битый
  файл не переписывается и не удаляется (плашка). «Своя тема…» — панель `ThemeEditor` (ручки `ThemeKnobs`: тон/сдвиг
  акцента/сила/тёмная), записи `my-themes.json` получили необязательное поле `knobs`, палитра записи побайтно =
  `ThemeKnobs.palette()`; `"knobs":null` не пишется. Меню открывается слева от минуса (`MinimizeMenu.origin`).
  Claude `~/Library/Application Support/Claude/claude-code-sessions/*/*/local_*.json` → `ProjectIndex.swift` (папка по
  sessionId/заголовку, главное окно по `status.json`). Индекс пропал — молчим, не красим. Покраска не трогает окна с ручной
  темой/автопокраской; файл в репозитории — в `~/.config/git/ignore`.
- **Кто этот чат (WF29, задачи #5448 и #5455)**: окно опознаётся id чата, а не заголовком — заголовок попапа это
  снимок имени чата на момент выноса в окно, и после переименования чата он с индексом Claude не сходится
  (у Элвиса так не опознавались три попапа из четырёх). Три части:
  - **Страница** — раздел «12в. Кто этот чат», `inject.js:4338-4541`. `window.__myclaude.chats({scan,nonce})` отдаёт
    промис `{v:1,nonce,kind:"main"|"popout"|"other",self,path,row,title,popouts:[{id,title}],store,at}` и НИКОГДА
    не бросает; `store` — `ok|cache|busy|none|skip`. Главное окно берёт свой id хвостом `location.pathname`, попап
    (у него путь `blank`) спрашивает окно-родителя: `window.opener.__myclaude.popoutChat(title,{scan})` ищет заголовок
    в карте `popoutWindows` стора claude.ai (тот же поведенческий скан, что у «Нового окна», токен не увеличивается;
    одновременные просьбы складываются в один промис `chatsScanInFlight`). Ноль совпадений заголовка или два и больше
    → `null`, заглушки заголовка не участвуют. Ответ родителя кэшируется в sessionStorage `myclaude-chat-v1`
    (`{id,title}`, годен пока `document.title` тот же) — кэш переживает перезапись `inject.js`, и по нему работает
    синхронный `addressed()`. Слепок без скана — `status().chat` (`kind/self/path/row/popouts/store/at`).
    Своих таймеров, подписок и наблюдателей раздел не заводит ни одного.
  - **Приложение** — `app/Sources/ClaudeAX/ChatProbe.swift`: пишет `probe.js`, читает `probe-result.json` и берёт
    из него только записи со своим `nonce`. Канал общий с агентом на гейте, поэтому владение **вежливое**: свой
    скрипт помечен первой строкой `// myclaude-chats v1 <nonce>`; канал берём, если файла нет, текст побайтно наш,
    начинается с метки или файла не касались дольше 10 мин; чужой свежий скрипт — молчим 60 с, карта помечается
    несвежей. **Тумблер «🗂 Цвет по проекту» выключен — probe не пишется и не читается вовсе.** Пол частоты 15 с,
    неопознанное окно переспрашиваем не чаще 60 с, незакрытый круг ждём 30 с, `scan:true` уходит только при
    неизвестном чате. Свой таймер не заводится — общий тик 2 с `ClaudeAXController`.
  - **Покраска** — цели `ProjectPaint.targets()` берутся из ответов страниц, а не из заголовков окон. Ключ окна
    `main` / `c:<id чата>` / `w:<заголовок>`, и ключ на окно ровно ОДИН: появился id — отпечаток переезжает из
    `w:` в `c:` со слоями, потерялся — обратно и с пустым `digest` (`ProjectPaint.moveMark`). Страница ответила при
    рабочем сторе, а чат назвать не смогла → окно «не определён»: команды нет, отпечаток не трогаем, папка главного
    окна не подставляется НИКОГДА (#5455). Ответа нет вовсе (канал занят, стор не нашёлся) → старый путь по
    заголовку, как до WF29. Чат главного окна: свежий (младше 10 с) ответ страницы сильнее адреса из `status.json`.
  - `ProjectIndex` при этом получил номер состава чатов `revision` (кортеж `sessionId+cwd+title`; `lastFocusedAt`
    в него НЕ входит — Claude переписывает файлы сессий непрерывно) и кэш разбора `status.json` по отпечатку
    (файл весит мегабайты, а спрашивают его дважды за тик). Отпечаток снимается со свежего `URL`:
    значения ресурсов кэшируются внутри объекта, и у долгоживущей ссылки mtime навсегда остался бы первым.
- `live-colors` (WF18): `{…,"scope":"all","on":true,"mode":"sync"|"solo","period":<с>,"epoch":<мс>,"light":true|false|null,
  "titles":[…],"ring":{"dark":[палитры…],"light":[…]}}` — порядок полей побайтно, числа числами, палитра в порядке
  `Theme.paletteOrder`; выключение — `{…,"scope":"all","on":false}`. Страница: раздел 2в inject.js, шаг кольца = 360/длина,
  память `myclaude-live-v1` (localStorage, общая — крутёж живёт и без приложения) и `myclaude-live-phase-v1`
  (sessionStorage по окну, `{key: sessionKey(), phase}`; фаза запирается по первому настоящему заголовку, новый `titles`
  пересчитывает). `ClaudeCommand` не расширяется — команда пишется строкой (`LiveColors.swift`).
- WF19: в `size` половина может быть `null` — снимает только её (`{"answer":null}`), `"size":null` снимает обе; `new-window`/`popout-window`
  получили необязательное `match` после `title` (главное окно) — работают с любого окна; попап на `popout-window` — плашка «уже в окне».
- `themes-restore` (WF35): `{…,"scope":"all","entries":{<ключ>:{theme?,font?,size?,frame?}}}` — порядок полей
  побайтно `id, action, at, scope, entries`; `at` записей в команду НЕ идёт (он бухгалтерия файла), слои — те же
  объекты, что у `theme`, сброс — строкой `"none"`. Страница (`runThemesRestoreCommand`) свою память не заменяет,
  а **доливает недостающие СЛОИ**: что у неё есть, не трогает вовсе — отсюда идемпотентность, и спрашивать окно
  «ты пуста?» не нужно. Ключи только пяти видов (`*`, `main`, `id:`, `chat:`, `w:`; непустой хвост, не длиннее
  200 знаков), не больше 200 штук, мусор отбрасывается. Красит `restoreTheme(false)` и НЕ красит при примерке
  (`themeState.previewing`) и живых цветах (`liveState.on`). Обработчик стоит рядом с `status`/`live-colors` —
  **до** отмены примерки, иначе приезд команды гасил бы подменю под рукой. Диагностика —
  `status().restore = {at, keys, merged, painted}`.
- Кто шлёт `themes-restore` (`WindowThemeStore.swift`): понятие **поколения** — старт приложения
  (`ClaudeAXController.start()`) или перезапуск Claude (`ClaudeApp.onRestart`). В поколении не больше **трёх**
  команд: первая на первом тике, где окна Claude есть на экране, дальше только на смену набора AX-заголовков,
  зазор ≥ 5 с. Файл пуст или Claude не запущен — команд нет вовсе.
- Файл догоняет правду через probe (WF35): к ответу `ChatProbe` добавлено одно поле `themes` — карта
  `myclaude-themes-v1`, и отдаёт её **только главное окно** (`isMainWindow()`), а приложение берёт её только из
  ответа с `url` на `https://claude.ai/`. Непустая карта заменяет `entries` файла ЦЕЛИКОМ; пустая — правда везде,
  кроме ПЕРВОГО ответа в поколении (там это переустановка, файл цел). Каждый закреплённый выбор темы приложение
  дублирует в файл (`ClaudeActions.recordTheme`, три точки: `applyTheme`, «Раскрасить по кругу», цвет проекта);
  примерка не пишется никогда. Потолок файла — 32 КБ тела будущей команды (`StatusFeed.totalLimit`) плюс
  страховочные 200 ключей, вытеснение по `at`.
- **Канал «Пимп» (WF36)** — не `command.json`, а свой каталог `pimp/`: файл на запрос, ответ рядом. Эталоны
  побайтно — `tests/fixtures/pimp/*.json` (их читают ОБА конца: `tools/pimp.py` и `PimpChannel.swift`), там же
  README с порядком ключей. Запрос `{"id","at","action":"new-window"|"arrange"|"projects"|"windows","from",…}`
  (id = `<unix-мс>-<4 цифры>`, `from` — id чата агента из `CLAUDE_CODE_HOST_SESSION_ID`, пусто — «под этим»
  деградирует в `right`); ответ `{"id","at","ok","error","fromResolved","screen",…}`, ошибки `stale`, `busy`,
  `no-windows`, `project-missing`, `window-missing`, `too-small`, `bad-request`. Приложение на общем тике 2 с:
  берёт запросы без ответа по `at`, исполняет младше 30 с и по одному, пишет `<id>.taken`. CLI ждёт ответ до 60 с;
  нет `taken` за 5 с — «Пимп не запущен». Ожидания CLI переопределяются `MYCLAUDE_PIMP_WAIT`/`MYCLAUDE_PIMP_TAKEN`,
  каталог — `MYCLAUDE_PIMP_DIR` (нужно тестам). Правда об исполнении — ответ на диске, не память процесса.
- **`cashout` (WF37)**: `{…,"scope":"window","title":<AX>,"match"?:<путь главного окна>,"chat"?:<id чата>}`, порядок
  `scope, title, match?, chat?`; главному окну уходит `match` (на домашнем экране — `"/epitaxy"`), попапу — `chat`
  (если страница назвала свой id в карте probe; иначе только заголовок), никогда наоборот. Страница адресует через
  `addressed()`. Развилка — чистая `ClaudeActions.cashoutRoute(title:isMainTitle:knownChat:)`; главное окно узнаётся
  сиденьем `isMainWindowTitle` (= `ProjectPaint.windowKey(forTitle:) == mainKey`). Главное окно: команда, потом ⌘N,
  как раньше. Попап: команда, потом **`new-window` с полем `transfer:true`** (после `name`, перед слоями; порядок
  `scope,title,match?,x,y,text,folder,name,transfer?,[слои]`) — новое окно рядом в проекте чата (`projectForChat`,
  папка обязана быть на диске) или «Здесь же»; ⌘N попапу больше не шлётся никогда. Ставит `transfer` только ветка
  «Обкэшить»; ⌥⌘N, «▸ проект» и канал «Пимп» — нет. Эталоны побайтно — `tests/fixtures/cashout/*.json` (читают оба
  конца, README там же).
- **Запись переноса** localStorage `myclaude-cashout` (WF37): главное окно пишет `{at,text}` и заводит сторож
  вставки; попап пишет `{at,text,to:"pending"}` и сторож не заводит; цепочка `new-window` с `transfer` после шагов 5 и
  6б штампует `{at,text,to:<id>,title:<заголовок строки>,stampedAt}`. Вставка: `pending` — никогда; главное окно
  записи с `to` игнорирует всегда; свежесть штампа 60 с (`CASHOUT_STAMP_FRESH_MS`), без штампа — 90 с; «моё» =
  `myChatId() === to` или `document.title === title`; ни того ни другого при неизвестном id — один вопрос родителю
  (`popoutChat`); поле должно быть пустым, `isFreshChat()` не спрашивается. `status().cashout` =
  `{record,to,title,stampedAt}`. Непустой композер ДОМАШНЕГО экрана роняет цепочку с плашкой в главном окне — перенос
  тогда молча протухает (хвост в Trelvis).
- **Папка домашнего экрана** (WF37, #5576): ответ `chats()` получил `folder` (string|null) **после `store`, перед
  `at`** — только главное окно и только на `/epitaxy` (`newWindowAtHome()`), из стора чипа `selectedFolder`
  (`newWindowFolderNow`); стора нет — один фоновый скан (`chatsFolderScanInFlight`, повтор не чаще минуты), в круге
  `null`; во время цепочки «Нового окна» — `null` (чип показывает папку будущего чата). В открытом чате папку
  по-прежнему даёт индекс сессий, `folder` там `null` всегда. Swift: `ChatPage.path/folder`; `ProjectPaint.targets()`
  красит главное окно по этой папке ТОЛЬКО когда `index.mainWindow() == nil` (ключ `main`, `match:"/epitaxy"`, подъём
  к корню `ProjectIndex.root(of:)`); `ChatProbe.homeInterval` = 4 с, пока главное окно на домашнем экране (это же
  повод спросить), иначе 15 с. Цвет домашнего экрана ложится в ключ `main` и возвращается после перезапуска Claude —
  ожидаемо. `statusText` `project=…/home:<папка|—>`.
- **Полоска v3 (WF22)**: сводка `status` (контракт прежний) на странице разбирается в объекты `statusBlocks()` (номер —
  цепочка клавиш 2️⃣9️⃣ → 29; роли `{role, agents, model, effort, running}`, `running` — `💭` в конце строки роли); сегмент
  ↔ блок сводки соединяется **по значку, не по номеру** (два счёта, `docs/PROGRESS.md`): идущий — последний блок с 💭,
  готовый k из M — k-й с хвоста среди ✅, будущий — ⬜ после идущего; заливка идущего сегмента — пройденные этапы из
  четырёх (план/критик/кодинг/проверка; при «шаги 0 из M» без 💭 этапы не считаются), иначе «шаги N из M», минимум 8 %;
  пульс — `element.animate()` на слое свечения (отмена в `track()`/`dispose()`, стоп при `hidden` и `reduced-motion`),
  «готово» — зелёная полоса с пульсом, ✋ жёлтая и 🛑 красная без; новый чат без строки — пустой контур, мышь не ловит.
  Карточка сегмента — `#myclaude-progress-card` внутри `#myclaude-progress-tip` (вариант `PROGRESS_CARD_VARIANT = "3A"`,
  буква Элвиса 07.09 22:50): бейдж «идёт кодинг» / «готов» / «ждёт тебя», «о чём», время, «шаги», четыре этапа с «кто»
  (🔴 **Fable max** жирным), подвал со счётом проекта; ширина `min(400, окно − 12)` у левого края полосы, потолок 60 %
  высоты, без прокрутки, `pointer-events:none` + попадание по прямоугольнику, без `role`/`data-state`. Диагностика —
  `status().progress.tip = {segment, open, variant, pulse, card}`. Swift `StatusFeed`: блоки сжимаются (`compact`),
  режутся **с начала** файла, живые блоки (💭 ✋ 🛑) едут всегда, проекты — по mtime свежими вперёд, `digest` по имени.
- Старые: `collapse`, `expand`, `scroll`.
- Хранилище тем на странице: localStorage `myclaude-themes-v1` — карта `{ключ: {theme, font, size, frame}}`, ключи
  **`id:<id чата>`** (главный ключ чата, WF35), `chat:<заголовок>` (тень — по ней живут окна, которые своего id ещё
  не знают), `main` (главное окно), `*` (всем); sessionStorage `myclaude-theme-v1` по окну (`main` / `w:<title>`).
  **Приоритет по слою: `id:` → `chat:` → session → `main` → `*`.** Ключи считает раздел 2а: `chatIdKey()` (id из
  `myChatId()` WF29; проверка `themable` стоит явно, чужая страница ключей не даёт вовсе), `chatTitleKey()`
  (прежний `chatKey`), `chatKey() = chatIdKey() ?? chatTitleKey()`; карту читают через `chatEntry(map)`. Выбор
  пишется **в оба ключа чата разом** (`writeKeys`), сессия признаётся своей и по id, и по заголовку
  (`sameSessionKey`). Заглушки заголовка («Claude», «New chat») в `chat:` не пишутся, но при известном id ключ у
  безымянного чата **есть** — цвет остаётся, когда имя появится. `migrateChatKey()` копирует запись по заголовку
  на `id:` (старую НЕ удаляет) и зовётся трижды: на инжекте, в `syncChatTheme` (у попапа id появляется ПОЗЖЕ
  инжекта) и первой строкой `writeLayers` — иначе запись `id:` создалась бы с одним слоем и навсегда закрыла бы
  более полную тень. Старые форматы (WF5 тема на верхнем уровне, `w:`-ключи) читаются и переносятся.
  Что чинит: переименование чата больше не сбивает цвет, и два чата с одним именем красятся по отдельности.
  Осечка, названная честно: новый чат, которому достался освободившийся заголовок переименованного старого,
  унаследует его цвет и закрепит на своём `id:` — лечится выбором темы (риск 11 плана WF35).
- Тема = `adoptedStyleSheets` (НЕ `<style>`: Claude зеркалит `<style>` главного окна в попапы). Красятся только
  страницы `claude.ai` и `about:blank` (окна «Open in new window»), артефакты/браузер — нет.

## Сборка, гейт, релиз

- **`bash tools/test.sh`** — один прогон всего (WF24): `node --test tests/` (18 наборов, 222 проверки; страница гоняется в
  `node:vm` через люк `globalThis.__myclaudeTest` и стаб DOM `tests/dom.mjs`, путь к файлу — `MYCLAUDE_INJECT`) + сторожевые
  grep-проверки inject.js + `python3 -m unittest tests/pimp_cli_test.py` (20 тестов CLI «Пимп», WF36; приложение подменяет
  ниткой, каталог — `MYCLAUDE_PIMP_DIR`) + `swift test` (185). Красное = гейт не проходит. Чеклист гейта и «проверка на
  своём Маке» для команды — `docs/CHECKLIST.md`.
- `tools/bundle.sh release` → `app/.build/PimpMyClaude.app` (Developer ID). **Подмена без простоя:** сначала bundle,
  потом `pkill -f PimpMyClaude.app/Contents/MacOS; rm -rf /Applications/PimpMyClaude.app; ditto …; open …` — Элвис
  дважды ловил «минус не работает», когда приложение лежало во время сборки.
- `tools/release.sh` → нотаризация (профиль `pimpmyclaude`) → `dist/PimpMyClaude-<версия>.zip`; версию поднять в
  `app/Resources/Info.plist` (CFBundleShortVersionString + CFBundleVersion) до запуска.
- Живой гейт без клика: `probe.js` → `window.__myclaude.status()` (поля theme/font/size/frame/progress/statusFeed/
  chat/chatKey/sessionKey/preview/workflow), команды руками в `command.json` (id обязан быть новым), скриншот
  `screencapture -x -R…`. Мышью водить через Hammerspoon (`hs -c`) можно, но `hs` иногда виснет — таймаут.
- **`probe.js` с WF29 не только твой**: канал делят агент на гейте и приложение (`ChatProbe`). Положил свой скрипт —
  приложение уступает и молчит (карта чатов замирает, попапы красятся по заголовку); **закончил — удали `probe.js`**,
  канал вернётся за 5–10 с. Кто держит канал, видно по `head -1 probe.js` (`// myclaude-chats v1 …` — приложение)
  и в `statusText` полем `chats=<own|busy|off>/<страниц>/<опознано>`; рядом `pimp=<запросов/ошибок>` — канал «Пимп» (WF36).
- **«Поставить» в приложении закрывает Claude** и с ним чат оркестратора — на Маке Элвиса делает он сам; патч у него
  уже стоит (v6). Живой «Снять → Поставить» новым Swift-патчером Элвис ещё НЕ делал (проверено только на копии
  Claude.app и старым mjs-ридером).
- Git: коммит с явным автором (`GIT_AUTHOR_NAME=Elvis GIT_AUTHOR_EMAIL=elvis@M4.local …`, иначе «Author identity
  unknown»), в конце сообщения `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Ветка на воркфлоу
  (`wfN`) + тег `wfN-start`, слияние в `main` `--no-ff`.

## Решения, о которых легко забыть

- **Главное окно на скриншоте** (слово Элвиса 07.09 17:20): единственное окно Claude, у которого справа от трёх кнопок
  окна стоит кнопка сайдбара (прямоугольничек); у попапов её нет. По ней главное окно опознаётся на любом скрине
  гейта, заголовок «Claude» для этого не нужен.
- Модели (слово Элвиса 03.09 18:00): оркестратор Fable 5.1 high; ВСЕ субагенты (критик, кодинг, verify) — Opus 5 max;
  Fable как субагент только в крайнем случае с пометкой в run-файле.
- Хоткеи только Carbon `RegisterEventHotKey` (NSEvent-монитор не глотает ⌘Q и просит Input Monitoring); в меню
  клавиши через `keyEquivalent` (AppKit рисует справа серым, порядок системный ⌥⌘). ⌘⌥W — клавиша Claude, не наша.
- Примерка в меню (WF31): `PreviewMenuDelegate.shared` с паузой 0,5 с (`defaultDelay`, `MinimizeMenu.swift`) — любое новое
  меню вешает его же и зовёт `cancel()` при закрытии; новый класс тестов, трогающий меню, обязан в `setUp` поставить мгновенное
  расписание (образец `ClaudeAXTests.setUp`). «Мои темы» ставят и примеряют только цвет; страница держит `previewLayers` —
  примерка одного слоя возвращает непокрытые слои.
- `NSApp.activate(ignoringOtherApps: true)` перед `popUp` обязателен (кооперативный activate на macOS 14+ не работает —
  подменю закрывалось).
- Разрешений у приложения два: Accessibility и «Управление приложениями» (App Management); уведомления — третье,
  необязательное. Dev-сборки подписывать Developer ID с первого билда (стабильный DR → AX просится один раз).
- Претензии к цветам решались так: палитры «заметные» (фон явно окрашен, S 45–60 %), акцент в полосе контраста
  4,5–6 (тёмная) / 4,5–7 (светлая); `--cds-*` семантические переменные обязательны (попапы красятся через
  `bg-surface-1`); блок Epitaxy (Claude Code) обязателен для поля ввода.
- **Блоки кода (WF34)**: коробка, подсветка и рамка идут из палитры прямыми правилами `codeCss` (`pre`, `.hljs`,
  `code.hljs`, `.code-block__code` + восемь групп `.hljs-*`; рамка только у внешнего `pre` — иначе двойная обводка
  вокруг счётчика строк). Инлайн-код красится ТОЛЬКО тремя код-только переменными Claude —
  `--cds-prose-code-color`, `--code-chip-ink`, `--cds-editor-code-ink` — плюс одно правило на старый рендер
  (`.ReactMarkdown code`, `div.ProseMirror > p > code`): своих фонов и рамок у инлайна не писать никогда, иначе
  затаптываются исключения самого Claude (чип-ссылка и её ховер, `th`, `table :not(pre) > code`, `code code`).
  **Не переопределять:** `--cds-alpha-*` (на них все ховеры и заливки страницы), `--cds-bg-neutral` (44 правила:
  `kbd`, утилиты `.bg-neutral`, бабл), `--cds-text-danger` и `--danger-000` (общая красная семантика, 27 правил).
  У каждого правила `codeCss` хвост `:not(:is(.artifact-markdown-preview, .channel-artifact-markdown-preview) *)` —
  превью артефактов остаётся сегодняшним (защита Claude прикрывает там только `background` у `pre`, на наш цвет и
  рамку её не хватает). Вывод `codeCss` дописывается В СТРОКУ `themeCss`: своя `CSSStyleSheet` = красный
  `tests/idempotent.test.mjs` (число таблиц стилей не должно расти). Тон и насыщенность One Dark не трогаем —
  правится только светлота (цель 4,5, комментарий 3,5).
- Полоска прогресса читает строку состояния ПОСЛЕДНЕГО ответа (`WF N из M · P%`, значки 💭✋✅🛑) — формат из
  SkilZZZ/AGENTS.md п. 5; окна-попапы — через текст всего окна (приметы ответов там не совпадают). Сегменты —
  пул узлов с переходами; подсказка — по клику на сегмент.
- Автопокраска не ставит галки тем и стирает старые; окна с одинаковым заголовком получают один цвет (адресация по
  `document.title`); «Случайно» режим берёт из памяти приложения (по умолчанию тёмный).
- Откат `themeScope` (выключатель темы-на-чат) снят: страница файлов не читает, тащить флаг = перепатч.
- **`status().raw` после WF35 показывает запись по `id:`**, а не по заголовку: он считается по `themeKey()`, а тот
  теперь отдаёт `id:local_…`. Тень `chat:<имя>` смотреть прямым чтением `myclaude-themes-v1` — иначе первый же
  читатель решит, что тень не пишется.
- **`probe.js` держит не только гейт**: пока он лежит, `window-themes.json` не догоняет правду страницы (файл
  живёт одним зеркалом закреплённых выборов). Забыл удалить — приложение молчит бесконечно.

## История воркфлоу (планы `docs/plan-wf*.md`, состояние `docs/run-wf*.json`, сводка `docs/status.md`)

WF1–3 (03.09) ручка/меню/⌘Q в Hammerspoon → WF4 PimpMyClaude.app 1.0.0 → WF5 темы окон 1.1.0 → WF6 24 темы,
шрифты, «Мои темы» → WF7 полоска → WF8 предпросмотр → WF9 полоска v2, кнопка Workflow, клавиши, тема на чат,
категории шрифтов 1.2.0 → WF10 автопокраска → WF11 плавная полоска → WF12 размер, рамка, клик по сегменту 1.3.0.
Все 12 закрыты (`docs/status.md`: 12 из 12). Незавершённых воркфлоу и сторожей-кронов нет.

## Текущее (07.09 23:26, чат «PimpMyClaude баги и улучшения» на Fable xhigh — WF37 и WF22 слиты, приложение у Элвиса — сборка WF22)

- **WF22 «Полоска v3» (07.09 19:46–23:20, план `docs/plan-wf22-2026-09-07.md` после критика Opus max, макет карточки
  `docs/mockup-wf22-cards.html` Fable xhigh, буква Элвиса 3A, verify `docs/verify-wf22-2026-09-07.md`)**: сделано,
  контракт выше. Живой `inject.js` = main, VERSION `wf22-p-1`. Волна падала по лимиту сессии (сброс 22:00) — поднята
  сторожем, огрызок P откачен. Хвосты: правило `💭` у идущей роли и «о чём» человеческим языком — в AGENTS.md через
  чат Max (#5632); значок ✋ → ❓ (#5633); оглавление в макетах (#5634); открытие макетов из чата (#5635).
- **WF37 «Ремонт Пимпа» (07.09, план `docs/plan-wf37-2026-09-07.md` после критика Opus max, run `docs/run-wf37-2026-09-07.json`,
  verify `docs/verify-wf37-2026-09-07.md`)**: сделаны части B («Обкэшить» из попапа → новое окно рядом, #5575) и C (цвет
  домашнего экрана по чипу папки, #5576) — контракты выше. **Часть A («Привет» без хода модели, #5447) НЕ сделана**: ждёт
  разведки при Элвисе (раздел «Разведка» плана WF37 = WF30), потом мини-волна P2+S2. Живой `inject.js` = main, VERSION
  `wf37-p-1`. Проверки Элвиса: «Обкэшить» в попапе → новое окно с текстом; ⌘N → домашний экран → сменить чип папки →
  цвет окна. Хвосты — задачи в Trelvis (см. run-файл).
- **Программа этого чата (9 прогонов, слово Элвиса «хочу много воркфлоу»)**: 37 → 38 (inject.js на модули, #5585, чтобы
  страницу кодили 3–4 агента разом) → волна «22 полоска v3 + 32/33 меню и цвета + 39 хвосты Пимпа» → 21 сетка → 28 чаты в
  подменю (+#5548) → 40 голосовой Пимп (#5555) → 17 релиз. Ждёт Элвиса: «сейчас/потом» на разведку A; буквы по макетам
  WF32/33/21/28 (высланы 07.09 18:00); скрин лимитов. Отменено им 05.09 и закрыто в Trelvis: #5449 (полноценные окна).
- Правила SkilZZZ переписаны чатом Max 07.09 17:42: критик/проверка — Fable xhigh/high при «запасе», без цифр баланса —
  Opus max; макеты — Fable high; строка баланса `⚖️` над строкой состояния, пока идёт прогон; норма оркестратора xhigh.
- Примета главного окна на скриншотах — кнопка сайдбара справа от светофора (см. «Решения»). Ниже — состояние на 07.09 17:20.


- **WF36 «Пимп, открой окно» (07.09) сделано, слито в main**: канал `pimp/` + CLI `tools/pimp.py` + скилл `skills/pimp`
  (симлинк `~/.claude/skills/pimp` стоит), #5544 (чип папки — пометка), авто-цвет в команде нового окна, `projects.json`,
  `minCellWidth` из `claude.json`, рамка окна ставится с проверкой и повтором (`ClaudeActions.setFrame`: Electron глотал
  позицию у свежего окна — так «Расставить» и «сужал, не двигая»). Живой `inject.js` = main, VERSION `wf36-a-1`.
  Живьём из чата: `pimp.py open Dictator --at right` → окно четвёртым, зелёное. Ответ `layers` канала = `"ok"`/`""` —
  положило ли приложение слои в команду, а не встали ли они на странице. Хвосты verify — задачи в Trelvis (см. run-wf36).
- **Следующее по слову Элвиса (07.09)**: #5447 «Привет» без Fable (WF30 ч.1: первое сообщение — локальная команда
  `/rename`, разведку делает оркестратор руками при Элвисе), потом #5555 голосовой Пимп (Диктатор слушает →
  «Пимп …» → дешёвая модель → канал; нужен action `rename`), #5548 список фоновых задач под сообщением (макет).
- **Передача 07.09 ~17:45 (Элвис сжимает контекст и переводит чат на Fable 5.1 xhigh)**: все пожелания Элвиса
  за день зарегистрированы в Trelvis (#5531–#5566, слаг `other`; Диктатору #5538 #5539; SkilZZZ #5545 #5552).
  Открыто по приоритету Элвиса: (1) #5447 «Привет» без Fable — план `docs/plan-wf30-2026-09-06.md` ч.1, разведка
  оркестратором руками при Элвисе; (2) WF22 полоска v3 по новым словам (#5565 пульс идущего этапа + заливка по шагам
  WORKFLOW.md, #5566 подсказка в ширину окна с ✅, ролями «N агентов · модель эффорт», историей) — макет
  `docs/mockup-wf22.html` переделан агентом Opus max 07.09 17:40 (буквы 1x 2x 3x ждут Элвиса), план WF22 писать заново
  после букв (старый `plan-wf22-2026-09-04.md` устарел по формату строки состояния); (3) #5555 голосовой Пимп;
  (4) #5548 список фоновых задач. Сторожей и открытых run-файлов в `идёт` нет. Правила WORKFLOW.md переписаны 07.09
  (сторож — `CronCreate` в чате, дисковый `scheduled-tasks` не использовать; проверка на гейте — Fable high).
- **Буквы Элвиса по макету WF22 (07.09 17:10, макет делал агент Opus 5 max по заданию оркестратора)**: 1A мягкий пульс
  свечения; «готово» красить ЗЕЛЁНЫМ и тоже с мягким пульсом (готово = сигнал продолжать); 2A четыре этапа поровну,
  по клику на идущий сегмент — этапы (план/критик/кодинг/проверка) и КТО на каждом (модель · эффорт); 3A карточка на
  сегмент, но дизайн переделать (плохо выделены, переносов в узком окне не хватает), на карточке — этапы и кто делал.
  Макет НЕ подписан, кто его делал, — новое правило Элвиса (#5570): макеты/исследования сверху несут «сделал: роль ·
  модель эффорт · дата»; вписать в WORKFLOW.md, согласовав с чатом Max, который его правит. План WF22 писать по этим буквам.
- **Вопрос Элвиса «можно ли много воркфлоу разом, чтобы быстрее»** — ответ и текст для чата Max: `docs/for-max-2026-09-07.md`.
- Открыто ещё: #5569 Диктатор режимы вечеринки (делает Диктатор), #5571 масштаб HTML-макетов в просмотре Claude.
- Ниже — состояние на 06.09 11:10 (марафон), очередь и вопросы Элвису в силе.

- **Марафон чата «Pimp» (05–06.09)**: Элвис выбрал 14 воркфлоу из списка 20 (порядок и журнал —
  `docs/run-marathon-2026-09-05.json`, сводка `docs/status.md`). **Сделано и слито в main, запушено, приложение у Элвиса —
  сборка WF35 (1.4.0 (8), версию в Info.plist не поднимали)**: WF20 (своя тема, меню слева от минуса, авто-цвет проекта),
  WF29 (чат по id: страница отдаёт свой чат через канал probe, приложение красит по id, попап не путает проект, смена папки
  перекрашивает), WF34 (блоки кода в цвет темы), WF31 (пауза примерки 0,5 с, «Мои темы» ставят только цвет, слои примерки),
  WF35 (ключи тем `id:<chatId>`, зеркало `window-themes.json` в Application Support/MyClaude, команда `themes-restore`
  после перезапуска Claude). Живой `inject.js` = репозиторный main, VERSION `wf35-a-1`. Тесты: `bash tools/test.sh` —
  16 наборов / 187 JS + 20 Python + 165 Swift на 06.09; после WF37 — 17 / 205 + 20 + 179; после WF22 — 18 / 222 + 20 + 185, зелёный.
- **Счёт в строке состояния нового чата** — свой (по правилу «WF N из M — только по этому чату»); в марафоне осталось 9:
  WF30, 32, 33, 21, 22, 28, 17, 25, 27.
- **Ждёт Элвиса (спросить в первом же ответе, вопросы уже заданы 06.09 ~11:00)**:
  1. Буквы по пяти макетам — `docs/mockup-wf32.html` (подменю у минуса), `mockup-wf33.html` («🎨 Цвета ▸»),
     `mockup-wf21.html` (сетка окон как в Moom, окна местами, раскладки), `mockup-wf22.html` (полоска v3),
     `mockup-wf28.html` (чаты в подменю). Без букв эти воркфлоу не кодить (правило макета).
  2. WF30 («Привет» без Fable + «Недавние проекты» в Пимпе, план `docs/plan-wf30-2026-09-06.md`, после критика):
     начинается с РАЗВЕДКИ — приложение создаст 3 пробных чата через command.json и подёргает главное окно Claude ~2 мин;
     Элвис должен сказать «сейчас» или «ночью». Ветку wf30 брать от main.
  3. Ревизия Fable xhigh по WF29 (канал probe уходит команде; всё кодили и проверяли Opus max) — предложена, ждёт «да/нет».
     Без «да» Fable субагентом не звать.
  4. Перезапуск Claude Элвисом — единственная живая проверка возврата тем WF35 (решение 8 плана); и наведение на «Мои темы»
     с паузой (шаг 0 гейта WF31 мышью не гонялся).
- **Очередь после ответов**: WF30 → WF32 → WF33 → WF21 → WF22 → WF28 → WF17 (релиз 1.4.0 команде: поднять версию,
  `tools/release.sh`, нужен Элвис) → WF25 (своё меню-панель) → WF27 (Sparkle). Планы WF32/33/21/22/28 ещё НЕ написаны —
  после букв: планировщик → критик → правки (Opus max), потом кодинг. Отклонено Элвисом 05.09: полноценные окна Claude (#5449),
  память позиций/«Спрятать все», кикофф WF23, автоперепатч WF26 не назван.
- **Хвосты (задач в Trelvis нет, завести при случае)**: `row` активной строки сайдбара в главном окне = null (myChatId по
  пути); `status.json` 7,5 МБ из-за ~60 data:-страниц макетов (вес — лоадер, перепатч); поле `chat` только у команды `theme`;
  тень `id:` над более полной `chat:` при `themes-restore` (verify WF35); безымянный попап («Claude») уносит ключ `main`;
  живой «Снять → Поставить» Swift-патчером на Маке Элвиса; обновления Claude после ad-hoc подписи.
- **Настройки Элвиса, которые я менял 06.09 (сказано ему)**: `projectPaintEnabled` включён (был 0), `themeAll` (violet, «всем
  окнам») снят — оба глушили цвет проекта.
- Сторож `watchdog-pimpmyclaude-0509` снят при паузе; новому чату — поставить свой (`watchdog-pimpmyclaude-<ДДММ>`).
- Задачи Trelvis, закрытые агентом и ждущие проверки Элвиса: #5358–#5386 (старые), #5382 #5416 #5419 (WF20), #5474 (полоска),
  #5475 (скрипт), #5448 #5455 (WF29), #5365 (WF34), #5452 #5453 (WF31), #5473 (WF35). Открыты: #5447 #5451 (WF30),
  #5454 #5395 (WF32), #5442 (WF33), #5393 #5392 #5397 #5398 (WF21), #5384 #5434 #5391 (WF22), #5433 (WF28), #5381 (WF17),
  #5430 #5416 (WF25), #5432 (WF27), #5449 (отклонено).

## После переустановки Claude (05.09.2026, Элвис переустанавливал вечером)

Переустановка Claude.app стирает патч (лоадер в `app.asar`) и localStorage страниц (темы окон, `myclaude-themes-v1`,
живые цвета). Живые файлы в `~/Library/Application Support/MyClaude/` остаются — **с WF35 там же лежит копия карты
тем (`window-themes.json`), поэтому шаг 3 больше не ручной**. Порядок:
1. Элвис сам: PimpMyClaude в меню-баре → «Поставить» (Claude закрывается и открывается с патчем; проверка —
   `status.json` свежий, `loader: 7`, `installed.json.version` = версии Claude).
2. `'/Users/elvis/Не удалять/после установки.command'` (двойной клик) — проверяет патч по версии, обновляет
   живой `inject.js`/`claude.css` из репозитория, напоминает про темы.
3. Темы окон **возвращаются сами** (WF35, задача #5473): приложение шлёт `themes-restore` на первом же тике
   нового поколения (старт приложения или перезапуск Claude), до трёх раз. Цвет проекта и живые цвета на том же
   перезапуске Claude тоже возвращаются (`ProjectPaint.forget()` + `resendLiveColors()` на `onRestart`).
   Проверить: `python3 -m json.tool "$SUP/window-themes.json" | head -40` и кусок `themes=<ключей>/<команд>`
   в `statusText`. Не вернулось (Claude поднимался дольше поколения) — `pkill` + `open` приложения открывает
   новое поколение. Ручной путь остался на крайний случай: команды `theme` в `command.json` (по одной, с паузой
   ≥ 0,7 с, `id` новый; палитры из `claude-patch/themes.json`; главное окно адресуется `title:""` + `match` по
   пути `/epitaxy/local_<id>` из `status.json`, попапы — `document.title`) — так делалось 05.09 23:45 для пяти окон.
4. Полоска прогресса: если в окне `status().progress.reason` = «нет блока композера» — это #5474, не переустановка.
5. Скилл «Пимп» (WF36): переустановка Claude сносит `~/.claude/skills/` — вернуть симлинк
   `bash /Users/elvis/_ElvisProjects/PimpMyClaude/tools/install-skill.sh` (идемпотентно, стоит — молчит).
   Проверка: `python3 tools/pimp.py projects` отвечает строкой, а не «Пимп не запущен». Список недавних проектов
   (`projects.json`) переустановку переживает сам — он лежит в MyClaude, а не в Claude.

## Открытые хвосты (не сделано, задач в Trelvis на них нет — завести при старте)

- Живой «Снять → Поставить» Swift-патчером на Маке Элвиса и на чистом пользователе macOS; отзыв первого из команды.
- Поведение обновлений Claude (Squirrel/ShipIt) после ad-hoc подписи — не проверено; TEAM.md обещает «Поставить снова».
- StatusFeed: подсказка полоски со сводками только там, где есть `~/_ElvisProjects` (`projectsRoot` в claude.json);
  при 9+ проектах хвост списка по алфавиту отбрасывается (32 КБ).
- Lua-модули в `hammerspoon/` — только источник логики, тем/полоски не знают; README про них честно говорит.
- Задачи в Trelvis #5312–#5343 закрыты агентом, ждут проверки Элвиса.
- **Вес `status.json` и `probe-result.json`** — по ~3,9 МБ: лоадер кладёт в них `url` каждой из четырёх десятков
  страниц, а у артефакта `url` это `data:`-адрес целиком. WF29 убрал лишние разборы (кэш по отпечатку, чтение
  раз в круг), но сам вес лечится только правкой лоадера, то есть перепатчем у всей команды.
- **#5448 живьём проверяет Элвис** (нажать «Change directory» и посмотреть, что цвет поехал за папкой): без мыши
  цепочка «чат → папка → цвет» проверяется только тестами и переключением строки сайдбара.
- **Возврат цвета проекта и живых цветов на перезапуск Claude (WF35, решение 8) живым гейтом не проверяется**:
  `ClaudeApp.onRestart` висит на смене pid **Claude**, и `pkill` приложения его не вызывает вовсе. Закрыто
  Swift-тестами; один ручной перезапуск Claude — на Элвисе (он же закрывает чат оркестратора).
- **Позиции окон по id чата** (вторая половина пункта 20 списка 05.09) в WF35 сознательно не делались: поле `pos`
  в записи зарезервировано и никем не пишется, владелец геометрии — WF21. Оценка отдельной работы +1,5 ч.
- **Карта тем на странице растёт вдвое**: на чат теперь два ключа (`id:` и тень `chat:`), у localStorage потолка
  нет, уборку WF35 не делает. Распухнет — отдельная задача. У файла потолок есть (32 КБ + 200 ключей).

## Кто чем владеет при параллельных батчах (правило одного писателя)

`claude-patch/inject.js` — всегда ОДИН агент на волну (5250 строк на старте WF35, 5425 после него, 5429 после
WF36, 5615 после WF37, 6025 после WF22; растёт каждой волной, сверяй `wc -l`; разделы 2а темы и ключи чата, 2б полоска, 2в живые цвета,
12 «Обкэшить», 12а Workflow, 12б новое окно, 12в «кто этот чат», 16 подписки). Swift — второй агент;
`themes.json`, `bundle.sh` — у Swift-батча; `tools/pimp.py`, `skills/`, `tests/*.py` — у батча страницы.
Эталоны канала `tests/fixtures/pimp/*.json` пишет один батч до старта, дальше они не меняются: на них стоят
оба конца. Контракт команды фиксируется в плане до старта, менять — только на гейте.
