# ⚪PimpMyClaude — HANDOFF, архив

Перенесено из `HANDOFF.md` 18.09.2026 дословно, ничего не правлено и не сокращено. Порядок как был —
свежее сверху. Живое состояние проекта — в `HANDOFF.md` рядом.

## Где стоим — записи прошлых дней

**17.09.2026 — AirDrop для Dictator, задача6256 (Codex).** Пользователь выбрал первый адаптер Claude через Pimp. Изолированный патч поверх завершённого WF70/WF71 добавляет только opaque attachment metadata внутри точного composer: count всех известных карточек, session/generation/revision/members. Внешних команд, чтения текста/имён/URL, отправки и смены фокуса со стороны Pimp нет. VERSION wf70-b-1; ordinary /chat UUID, /new и Code проверены. Unknown/loading/смена чата закрывают старый ticket. Canonical JS406+CLI42 PASS; независимый Astra High review выявил обычный /chat и исправление внесено. Swift, loader и бандл не менялись. Подробности docs/dictator-airdrop-2026-09-17.md. Native AX и живая вставка с телефона пока не подтверждены; дальнейшую установку/проверку фиксирует Dictator HANDOFF. Прежние пункты проверки меню WF70/WF71 остаются.

**17.09.2026, 16:30 — хвост WF71 по слову Элвиса над живым меню (#6273–#6275).** Три поправки, все стоят:
«Как у Claude» первым пунктом как тема по умолчанию (пустой кружок, галка когда цвета нет); «Тема проекта <Имя>»
с кружком палитры вместо папки («кажется, что папка откроется»); и баг — «Всё как у Claude» в окне проекта красило
окно бирюзовым: сброс удалял последний слой из `.pimpmyclaude.json`, файл исчезал и возвращался авто-цвет по имени
папки. Теперь сброс цвета пишется как `"theme": null` (`ProjectPaint.mergedTheme`), файл живёт, авто-цвет не
приходит; битый файл проверяется до записи. Файл проекта PimpMyClaude восстановлен с `"theme": null` (его удалил
клик Элвиса). Гейт зелёный (JS 406, CLI 42, Swift 267), приложение подменено. Его слово по остальному меню:
«в принципе, неплохо, нормально».

**17.09.2026, 15:55 — WF70/WF71 готовы и стоят у Элвиса.** По его диктовке 12:50 и слову по макету 14:20
(задачи #6247–#6254). WF70 — карточка воркфлоу: вместо счёта проекта внизу «Завершился 15 мин назад · занял 50 мин»
/ «Идёт 25 мин · начался в 13:05 · закончит в 16:00» / «Ждёт тебя · идёт 40 мин» / «Ещё не начат» (относительные слова
только по сегодняшней сводке, иначе «вчера в 10:35»), строки «09:45 → 10:35 · шаги 1 из 1» нет, блок карточки ищется
ПО НОМЕРУ (#5884, #6130 закрыты), открытая карточка тикает раз в минуту; #5915 закрыта замером (208 px при потолке 461,
в 280×394 потолок 236 — влезает; «кто» в две строки может перевалить на ~15 px). WF71 — меню на минусе про
оформление: сверху «🗂 <Проект>» (тема проекта, без записи файла), свои темы с «Применить ▸ (Только этому окну ·
Окнам проекта · Всем окнам) · Изменить… · Удалить», «💾 Сохранить тему» одним кликом (авто-имя, первой в списке),
Цвет/Шрифт/Размер шрифта/Рамка/Поля, «Всё как у Claude», «⋯ Ещё ▸» со всем остальным и «Всем окнам» последним;
примерка снимается при уходе в другой раздел (правило предка, подъём к верхнему меню серии); шрифты: с засечками
в конец, Noteworthy/Bradley Hand/Snell Roundhand убраны. Критик Fable xhigh (7 блокеров, 10 замечаний), кодинг
Opus max ×2, проверка Fable high ×2 — оба «сделано как описано»; гейт добавил шесть правок по замечаниям. Гейт
зелёный (JS 390, CLI 42, Swift 267), приложение пересобрано и подменено, живой inject `wf70-a-1`, слияние в main.
Версия по-прежнему 1.5.1 (10) — команде со следующим выпуском (у них ещё старое меню; TEAM.md уже описывает новое).
**Ждёт Элвиса:** глазами пройти новое меню (порядок, «Применить ▸», уход с примерки на «Шрифт ▸», быстрый заход в
«Матрица ▸» → «Изменить…» — окно должно остаться в Матрице) и кликнуть сегмент воркфлоу.
Хвосты в Trelvis: галка 🗂 после «Только этому окну» остаётся у своей темы; безымянный попап «Claude» получает
пункт проекта главного окна (#5534); мёртвый код после снятия «Своя тема…» (appearanceIcon, themeEditorIcon,
ветка ThemeEditorModel.save без editing) — на /revizor. Новое правило макетов в SkilZZZ/HTML.md: картинки первыми.

**17.09.2026 — Back в Dictator126 через Pimp (Codex, #6207).** После завершения чужого WF69 применён проверенный отдельный патч, live inject теперь `wf69-c-1`; loader, бандл Claude и Swift Pimp не менялись. Единственный живой composer получает восстановимую DOM id границу `myclaude-image-undo-composer-v1`; контейнер без role временно становится group. Штатная Remove последней картинки получает `myclaude-remove-last-composer-image-v1`. Dictator126 читает только этот контейнер точного окна: если после снимка изменён текст, отдаёт Back обычному CmdZ; после попытки AXPress второй отмены не шлёт. Pimp сам ничего не нажимает, не отправляет и не переводит фокус. Неоднозначные/скрытые/disabled/modal случаи не дают capability.

Независимый Astra High review PASS; `LC_ALL=C bash tools/test.sh --js`: 377 JS и42 CLI, 0ошибок, включая16новых тестов. Это устраняет прежнее временное красное из-за теста, который появился до своей реализации. Swift Pimp неизменён с5daf909, заново не запускался. Live файл побайтно совпадает с source; backup `/tmp/pimp-inject-before-undo-126.js`. CUA проверил доступность Claude и сохранность исходного пользовательского черновика после возврата из пустого нового черновика; пользовательский текст/буфер/вложения не менялись, сообщений не отправляли. Физическая отмена мышью и DOM→AX mapping пока не подтверждены: не выдавать совпадение файла за end-to-end. Программный доступ ChatGPT/Codex был запрещён CUA, обхода нет.

**17.09.2026, день — «📋 Копировать в буфер» правой кнопкой по файлу в чате (WF69, #6236).** Слово Элвиса
12:00: «Открыть в Хром — заебись команда, я пользуюсь; сделай ещё ниже „Копировать в буфер“: сам файл
скопирует, потом в Telegram правой кнопкой „вставить“ — и он вставит файл, а то приходится Finder открывать».
Мелкая правка мимо конвейера, кодил и проверял оркестратор (Opus 5 max). Второй пункт в том же меню, под
«Открыть в Chrome», у любого файла (не только .html). Устройство: страница умеет класть в буфер только текст,
поэтому кладёт путь и метку в HTML-слое, а приложение (новый `CopyFileRelay.swift`, таймер 0,25 с по
`changeCount`) подменяет содержимое буфера самим файлом — `public.file-url` + `NSFilenamesPboardType` + путь —
и говорит плашкой «📋 Файл в буфере: имя — вставляй». Контракт побайтно — в паспорте. Живой `inject.js` —
`wf69-a-1`; приложение пересобрано и стоит в «Программах» (версия по-прежнему 1.5.1 (10), выпуска команде
не было — они получат со следующей сборкой). Проверено живьём 12:25–12:27: копия из страницы Chromium →
в буфере файл (типы как у Finder), плашка на экране; в главном окне Claude по правому клику на карточке файла
меню: «🌐 Открыть в Chrome», «📋 Копировать в буфер», разделитель, пункты Claude. Клик по пункту самим Элвисом
и вставка в Telegram — его глазами (в буфере у него сейчас лежит файл памятки Дархану — след проверки).
Полный `tools/test.sh` красный ТОЛЬКО из-за чужого незакоммиченного `tests/composer-image-undo.test.mjs`
(параллельный агент, задача про вставку скриншотов, 12:30 — файл ещё пишется, фичи в inject.js пока нет; не
трогал); без него JS 361/361, guards, CLI 42, Swift 261 — зелёные. Тем же днём отдана памятка Дархану
`docs/darkhan-pimp-chatgpt-2026-09-17.html` (#6234): как патч влезает в Claude и как повторить с ChatGPT
(его приложение 26.908 — тоже Electron, fuses открыты, риски — права подписи и DeviceCheck).

**17.09.2026, утро — правая колонка широкого вида не шире строки модели (WF68, #6212).** По слову Элвиса
09:41 со скриншотом: «между Auto и Fable 5.1 ещё есть место — поджать, чтобы сошлись, и поле станет поуже;
минимальную ширину рассчитать так, чтобы эти поля сошлись». Мелкая правка мимо конвейера (одна задача,
inject.js + его тест), кодил оркестратор (Opus 5 xhigh), проверка — два независимых проверяющих Opus max
через Workflow. Нижняя граница колонки теперь не 300, а замер строки модели под полем: обе группы («+ 🎤 ⌄
Auto» и «Fable 5.1 · Extra · ◑») + её поля + 4 px зазора + обвязка; у Элвиса вышло 268 (панель 716), между
группами 4 px, редактор 228. Граница считается каждый проход и ходит за именем модели в обе стороны;
ключ ширины в хранилище стал `myclaude-wide-side-v2` — под старым у Элвиса лежало ровно 300 (он тянул рейку
до упора), и без этого новая граница осталась бы невидимой. Живой `inject.js` — `wf68-a-1`, замер probe и
скриншот 09:56: «Auto» и «Fable 5.1» рядом. Проверяющие не опровергли; по их слову пол колонки поднят
до 248 (редактору 208, запас 8 над порогом узнавания), правило «группы не ужимаются» сужено до доки, три
сторожа-по-литералам из теста сняты. Их же находка, не этого прогона: захват рейки прибавляет колонке 12 px
ещё до движения (с WF66) — задача #6214, не чинилось. Сборки и версии приложения не было (команда — со
следующим выпуском). Его глазами: сказать «ок» или «ещё уже/шире» — зазор `SIDE_CHIN_GAP` одно число.

**16.09.2026 — интеграция с Dictator #6186 (Codex).** В inject `wf65-c-1` добавлены Control+Option+Command+[ / ]: меняется только шрифт живого поля ввода, 11–32 px с шагом 1 px. Размер хранится отдельно в `myclaude-composer-font-size-v1`, настройки `size.question/answer` не меняются. Dictator 119 распознаёт верхнюю F6 вместе с боковым колесом Options+ и отправляет команды в точное окно Claude. Самостоятельный разворот верхней кнопкой в Claude ждёт отпускания; колесо отменяет снимок и разворот. Остальные приложения и обычный масштаб сохраняют прежнее поведение. Контракт описан в README.

Установленный inject совпадает с исходным, резервная копия `/tmp/pimp-inject-before-6186-20260916.js`. Loader и бандл не менялись, версия приложения остаётся 1.5.1 (10). Независимый review PASS; узкие проверки 37/37, полный JS 344/344 и CLI 42 прошли. Полный Swift-набор не прошёл в `testMyThemePreviewsOnlyPalette`, `testNewWindowFirstMessageAsksToWait`, `testNewWindowPressesKeyAfterCommandLandsOnDisk`: тесты создают настоящий ClaudeApp и зависят от живого AX-окна. Каталог `app/` не менялся относительно e75ca68; отдельного прогона исходной версии не было, измеренного сравнения с baseline нет. Не относящийся к задаче Swift не исправляли. Лог `/tmp/pimp-composer-6186-full.log`; запуск с `LC_ALL=C` обходит ошибку Bash 3 в разборе Unicode около переменной old.

CUA-проверка шрифта прерывалась изменениями окна пользователем; видимое изменение не подтверждено. Физическая проверка верхней кнопки с колесом запрошена у Элвиса. Текст и буфер в Claude не меняли, сообщений не отправляли. Параллельный чужой WF66 (79d2a9a), его план, макет и статус сохранены; наш коммит включает только inject, тест, README и этот блок HANDOFF.

**16.09.2026, вечер — широкий вид окна: три колонки (WF65).** По слову Элвиса того же вечера («хедер и футер
перенести и поле ввода перенести вправо… центральное окно максимально высокое… без фейдов»). Плитка чата
от 640 CSS px раскладывается сеткой: лента разговора от верха до низа слева, шапка чата и поле ввода во всю
высоту правой колонкой, ширину колонки тянет рейка у её левой кромки (двойной клик — треть); ýже — всё как
раньше. Во всех окнах: строка «ветка · +N −M · Create PR» не показывается, затемнения ленты сверху и снизу
убраны, имя папки в шапке никогда не сворачивается в значок, полоса прогресса на 2 px ниже рамки и вдвое
мягче. Кодил Fable high (слово Элвиса — ради скорости), критик Opus max, проверка Fable high. Живой `inject.js`
у Элвиса — `wf65-b-1`, проверено probe в главном окне (1214 px: сетка, поле 713 в высоту, рейка тянется
синтетической тягой 420 → 540 → сброс) и в попапе 292 (узкий вид, ступень поля сохранена, имя папки видно).
На гейте вскрылось: поля доки держит `sidePadding` из `claude.json` через USER-стиль `claude.css` с `!important`,
и перебить их нельзя ничем авторским — поле ввода само выезжает в них отрицательным `margin`. По первому
взгляду Элвиса (21:18: «сверху не используется место», «снизу обрезь») место под кнопки окна над лентой
стало считаться по положению панели (36 px только у левого края окна, с открытой боковой панелью — 0), а
пустой блок Claude под последним сообщением был ужат с 48 до 8 px — и это оказалось ошибкой: блок —
хвост виртуальной ленты, лента при прокрутке «телепортировалась» (слово Элвиса 17.09 00:05); правило
снято в ночь на 17.09 в дереве и в живом файле, закоммичено отдельно поверх правки Астры. Версия
приложения НЕ поднималась и сборки не было: у команды новая раскладка появится со следующим выпуском.
Что не проверено рукой Элвиса: тяга рейки мышью, меню модели рядом с рейкой, сужение окна с текстом в поле
(`docs/CHECKLIST.md`, 1.4г).

**13.09.2026, вечер — Claude в цветах тёмного Вкусноффа и ⌘V из любого места (WF64).** По двум словам
Элвиса того же вечера. Первое: «добавь тему, чтобы был вот этот тёмный Вкуснов» — в списке «🌙 Тёмные»
появилась тема «Вкуснофф», палитра взята из самого сайта (`VkusnoffKz/app/globals.css`, блок тёмного
режима), а не подобрана на глаз. Второе: «можно, я в любом месте Command V буду нажимать» — раньше
скриншот из буфера приезжал, только если курсор стоял в поле ввода. Теперь ⌘V работает из ленты
разговора, из пустого места и из боковой панели.

Живая проверка сделана руками агента 13.09 21:40: клик в ленту разговора, ⌘V — приехало РОВНО одно
вложение, фокус вернулся в поле; тестовая картинка убрана, буфер Элвиса возвращён, `probe.js` удалён.
Проверка (Opus max) нашла настоящую поломку: клавиша сверялась по `event.key`, а он зависит от раскладки
— на русской та же клавиша отдаёт «м», и у Элвиса первая дорога не срабатывала бы никогда. Починено на
гейте: примет теперь две (`key` или `code`), сторож в тестах стоит.

Собрана и подменена версия **1.5.1 (10)**, живой `inject.js` — `wf64-c-1`. Ветка `wf64` слита в `main`.
Что осталось незакрытым: в артефактах и встроенном браузере ⌘V так и не работает (это другие документы);
на латинской раскладке ⌘V мимо поля уводит в черновик и текст из буфера — оставлено осознанно, отправки
нет и ⌘Z работает.

Пришло тем же вечером и зарегистрировано, но НЕ делалось: два окна разных проектов одного цвета (#6035 —
диагноз готов, лежит в задаче: у обоих чатов одинаковое имя «Ожидание задачи», и они делят общую тень
темы по заголовку) и превью скриншота в Диктаторике (#6034 — дольше жить, крестик, смахивание, кнопка
«скопировать в буфер», перетаскивание в окно).

**13.09.2026, утро — поле ввода перестало уезжать за низ окна (WF62, #5951).** Элвис прислал окно, где
девять вложений и длинный черновик утащили низ блока ввода за край экрана: не видно ни конца текста, ни
кнопки отправки, прокрутка внутри текста до низа не доводит. Замер живьём (окно 653×784): блок вложений
386 точек без потолка плюс область текста 384 — низ оказался на 853. Теперь у коробки вложений потолок
`min(30vh, 240px)` со своей прокруткой, у области текста — `min(384px, 40vh)`, и оба потолка работают во
ВСЕХ окнах, а не только в узких. Проверено живьём на шести вложениях и длинном черновике в окнах 259 и
425: низ блока ввода 776 и 775 при окнах 785 и 784, строка модели видна. Живой `inject.js` — `wf62-a-1`,
слито в `main`.

**12.09.2026, ночь — карточка воркфлоу заговорила словами Claude Code, узкие окна перестали тонуть в
скриншотах (WF60 и WF61).** По сообщению Элвиса вечером: в карточке полоски эффорты теперь пишутся как на
ползунке Claude Code (Extra вместо xhigh), «1 агент»/«2 агента» и «×1» ушли — число стоит за моделью
(«Opus Max 2», «Fable Extra 1, Fable High 1»), длинный список режется в «и ещё N», «примерно» из времени
убрано, низ карточки собирается из чисел чужой шапки и переносится на две строки вместо обрыва, слово
«воркфлоу» везде русское, «из 5 · этот чат» стало «3-й из 5 в этом чате». Всё это — ТОЛЬКО на показе:
файлы `status.md` не переписываются (их пишет и Codex). В узком окне вложения поля ввода теперь 56×56 и
по три в ряд, блок вложений не выше 25 % окна и прокручивается внутри себя.

Живой замер 12.09 23:37–23:44 (вставляли картинки в служебное окно «Ожидание задачи» и снимали разметку):
плашка вложения в композере — `div[data-cds=MessageAttachmentsImage][data-cds-attachment]` с классом
`size-[120px]`, `[data-cds-tile-media]` в поле ввода НЕ встречается. Первая версия правила целилась в
него и не меняла ничего — поймал проверяющий, починено на гейте; после правки замер показал плашки 56×56
в один ряд и целый крестик «Remove» (у коробки поля 12 точек под его вылет). Ветка `wf60`, план
`docs/plan-wf60-2026-09-12.md`, журнал `docs/run-wf60-2026-09-12.json`, живой `inject.js` — `wf60-a-1`.
Хвосты гейта: карточка не влезает в низкое окно 280×394 (#5915), подвал теряет строки шапки без цифры
впереди (#5916). Отложено по слову Элвиса: смотреть скриншоты крупно поверх окна (#5913).

**12.09.2026, день — поле ввода доведено и появились яркие цвета (WF57 и WF59).** По словам Элвиса того же
дня: свёрнутая полоска ушла с кнопки Claude «вниз к последнему сообщению» и села на строку модели, полоса
прогресса при свёрнутом поле переехала на низ блока; клик по полоске открывает поле на три строки, а не «во
сколько хочет Claude»; после смены высоты лента доскручивается (низ разговора больше не прячется); файл,
вставленный в свёрнутое поле, сначала открывает его. Цвета: девять ярких палитр, меню разрезано на три
набора (Тёмные, Светлые, Яркие), «Мои темы» отдельным списком выше. Собрана версия **1.5.0 (9)**. Ветка
`wf57`, план `docs/plan-wf57-2026-09-12.md`, журнал `docs/run-wf57-2026-09-12.json`.

**Вечером 12.09 жёсткую высоту клика сняли** (слово Элвиса 19:00): клик открывал поле на три
фиксированные строки, то есть держал подмену высоты постоянно, и с пятью скриншотами в поле блок ввода
перестал влезать в окно — подрезка резала высоту, Claude перерисовывал вложения, и низ окна запрыгал
вверх-вниз вместе с полосой прогресса. Клик снова открывает поле обычной высотой; «чуть-чуть» надо делать
потолком (max-height), а не подменой height — задача #5886. Живой `inject.js` — `wf58-e-1`.

Тем же прогоном закрыты ещё два (WF58): в новом чате полоска внизу больше не показывает чужие воркфлоу
(#5855 — запасной путь разбора читал текст всего окна вместе с сайдбаром и собственной карточкой) и убрана
тонкая тёмная полоса у левого края главного окна (#5871 — наш цвет боковой панели попадал на рейку
«Resize sidebar», у Claude она прозрачная). Живой `inject.js` метки `wf58-b-1` подменён, приложение
**1.5.0 (9)** стоит в `/Applications`.

**12.09.2026, утро — поле ввода снова сворачивается (#5857).** Claude обновил разметку низа окна:
`.epitaxy-prompt` держит теперь не рамку поля, а весь низ вместе со строкой модели. Из-за этого блок ввода
совпадал с рамкой, сворачивать было нечего — одинарный клик по полоске молча не делал ничего, и поле
занимало до трёх четвертей окна (замер живьём: 566 точек из 784). Заодно в узких окнах (280) поле шириной
194 не проходило голый порог в 200 и полоски там не было вовсе. Правка — в странице (`claude-patch/inject.js`,
метка `wf53-b-1`): рамка ищется внутри `.epitaxy-prompt`, порог ширины считается от окна, всплывающая
подсказка у полоски убрана по слову Элвиса. Перепатчивать Claude не нужно — живой `inject.js` подменён,
лоадер перечитал его сам. Цвет левого и правого угла поля не трогали — Элвис отложил.

Обновлено 08.09.2026, конец дня. Заведён 51 воркфлоу, 37 закрыто (счёт — `docs/status.md`); у команды
стоит 1.3.0, а инструкция у неё вообще от 1.2.0 — релиз 1.4.0 команде (WF17) так и не сделан.

Всё сегодняшнее лежит в ветке **`wf43`**, и в `main` она ещё НЕ слита (`git log main..wf43` — весь день).
Закрыты: **WF43** «Чистка по Ревизору» (обе волны), **WF44** документы, **WF45** патчер, **WF46** проекты,
**WF48** «Обкэшить» заменяет окно и имя раскладки предлагается само, **WF49** разбор старого проекта
ElvisOS, **WF50** перенос не теряет ни текста, ни вложений. **Идут два**: **WF47** (первое сообщение
нового окна перестаёт стоить хода дорогой модели — упёрся в живую разведку, она на Элвисе) и **WF51**
(хвосты, которые назвал проверяющий WF50).

Сегодня по переносу («Обкэшить»): донора закрываем ТОЛЬКО по подтверждению доезда текста, сама вставка
подтверждается сверкой, вложения поля ввода едут ссылкой через память главного окна, у авто-Allow
появился список исключений. Контракты побайтно — в `AGENTS.md`: «Доезд переноса — слово страницы»,
«Вложения переноса», авто-Allow в «Решениях».

Что у Элвиса на экране прямо сейчас — вчерашняя ночная сборка (**1.4.0 (8)** от 08.09 04:01, WF41) и живой
`inject.js` метки `wf21-a-1`, тогда как в ветке страница уже `wf51-a-1`. Сегодняшних починок он не видит,
пока гейт не подменит приложение и не скопирует живой файл (`docs/CHECKLIST.md`, разделы 1.3 и 1.4).

Ревизия всего кода закончена (WF42): 12 зон, 56 находок, опровергнуть не удалось ни одной — выжимка
`docs/revizor-wf42-2026-09-08.md` (518 КБ, читать кусками по заголовкам через `grep -n`), полные данные
`docs/revizor-wf42-2026-09-08.json`, страница для Элвиса `docs/revizor-wf42-elvisu.html`. Разбор ElvisOS
(что взято по «Обкэшить», окнам и вложениям) — `docs/elvisos-razbor-2026-09-08.md`.

Тесты — один прогон `bash tools/test.sh`; числа тестов берутся из самого прогона и в документы не
переписываются.

## Что ждёт Элвиса — закрытое, снятое и старое

00. **Сказать «делай» по макету поля ввода** (16.09 ~22:25): `docs/mockup-wf66-composer.html` — вложения снизу
    у кнопки отправки целиком и без прокрутки, текст во всю ширину (#6187, #6188). И сказать, когда Астра сдаст
    свою правку размера шрифта поля (#6189): в рабочем дереве `main` лежат её незакоммиченные строки в
    `claude-patch/inject.js`, `README.md` и новый `tests/composer-font.test.mjs`; живой `inject.js` уже её
    `wf65-c-1`. **Обновление 17.09 00:20:** Астра сдала — коммиты 22:28 (`ab46747`, слияние `52c13dd`,
    хоткеи `⌃⌥⌘[`/`]` для Диктатора, #6186); откат нашего блока `transcript-spacer` закоммичен следом.
    Дерево чистое, кодинг WF66 можно начинать по слову «делай».

0я. **Включить тему «Вкуснофф»** (13.09 вечер): жёлтая кнопка окна → 🌙 Тёмные → Вкуснофф. Сказать, годятся
   ли коричнево-оранжевые рамки и подсветка строк: палитра сайта их не задаёт, цвет линии программа
   считает от оранжевого. Макет с этим предупреждением — `docs/mockup-wf64-vkusnoff.html` (#6025).

0а. **Сказать, годится ли вид строк в карточке** (12.09 ночь): клик по полоске внизу окна → карточка;
   строки ролей теперь «Opus Max 2» и «Fable Extra 1, Fable High 1». Макет — `docs/mockup-wf60-card.html`
   (#5908).



Первые два — про сегодняшний перенос: его руками не проверял никто, а цена промаха — потерянный текст.

1. **«Обкэшить» со скриншотами из вынесенного окна**: положить в поле два-три скриншота и черновик, нажать
   ⇧⌘N — новое окно обязано встать НА МЕСТО старого, с текстом И вложениями, и только после этого старое
   закрывается. Шаги — `docs/CHECKLIST.md`, раздел 1.4в, пункты (а) и (а3) (#5773).
2. **Неудача переноса**: то же ⇧⌘N, но новое окно сразу закрыть (⌘W) — старое ОБЯЗАНО остаться на экране со
   своим текстом, а плашка через две минуты — сказать, что перенос не доехал. Шаги — 1.4в, пункт (а4) (#5779).

## Решения Элвиса дословно — прошлые записи

- 08.09.2026 11:40 (#5771): «А что такое A? Почему 5A? Ничего не понял» → вопросы в HTML-файлах нумеровать
  и задавать словами, ответа буквами («5A 6B») не просить. Правило записано в `SkilZZZ/HTML.md`.
- 08.09.2026 утро: «Сейчас Fable подписка заканчивается. Поэтому временно в этом чате оркестратором будет
  Опус 5 Макс. Фейболов в этом чате не запускай. В остальных чатах действуют старые стандартные правила».
  → в прогонах ЭТОГО чата все роли (критик, кодинг, проверка) идут на Opus 5 max.
- 08.09.2026 утро: «Мне нужен рефакторинг этого программного кода» + «перед рефакторингом мне нужно тебе по
  функциям сказать, какая работает, какая не работает» → карта функций сделана обязательной зоной ревизии.
- 08.09.2026 ночь: ответ Элвису — четыре строки простыми словами, без терминов и номеров задач в тексте;
  «жду» рисуется значком ⚠️, а не ✋ (#5663).
- 08.09.2026 01:15: «делаем, как ты хочешь» → вариант А по раскладкам (WF21 сетка 4/5/5×2 и голос).
- 07.09.2026 вечер: порядок ответа «итог → решение → варианты → вопрос»; вопрос всегда со своим вариантом
  «(рекомендую)»; макеты и исследования подписывать «сделал: роль · модель эффорт · дата» (#5570).

## Следующий шаг — прошлая запись

Гейт WF51 (полный `tools/test.sh`, коммит на батч) → слияние `wf43` в `main` `--no-ff` → сборка и подмена
приложения без простоя + копия живого `inject.js` (`docs/CHECKLIST.md`, 1.3 и 1.4): пока это не сделано,
у Элвиса на экране вчерашняя ночная сборка и старая страница, и живые проверки переноса ставить бессмысленно.
Дальше — живые проверки его руками по списку выше. WF47 ждёт двадцати минут разведки на его Маке.
Сторож прогона — крон в чате оркестратора, каждый час в :23.


## Унесено 20.09.2026 из «Где стоим»

**17.09.2026, вечер — разведка под следующие темы; кодинга не было.** Слово Элвиса ~19:00: карточка нравится;
хочет (1) **цену воркфлоу в долларах по API на карточке и статистику по проектам** — графики, кнопка «Статистика»,
панель справа (#6282); (2) воскресить отчёты Радуги (#6283) — разведка сделана: `docs/raduga-stats-2026-09-17.md`
(устройство, формулы, грабли, откуда брать данные сегодня) + скриншоты `docs/raduga-stats/01–07`; (3) сравнение
китайских подписок для кодинга (#6284) — `docs/china-coding-models-2026-09-17.html` отдан ему («подписка за $70» =
GLM Coding Plan Pro, сегодня $80/$56 за год; рекомендация — GLM Pro, Kimi Pro как вариант). Он же сказал: «проверь
остальные баги сам» — открытые хвосты Пимпа (#6269, #5534, #6196, #6214 и очередь `list --project other`) разбирать
без вопросов ему. **Следующий шаг (новый чат):** план WF72 «цена воркфлоу и статистика» по `raduga-stats` (счётчик по
транскриптам с дедупом по message.id, панель справа, цена на карточке из `workflows/wf_<runId>.json`) — с макетом
по HTML.md (картинки первыми); параллельно — сметание багов из очереди.

