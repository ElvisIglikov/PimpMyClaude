# План WF25 — «Своё анимированное меню»: панель вместо NSMenu, вылет с минуса, влёт обратно

Дата: 05.09.2026 19:40. Проект ⚪PimpMyClaude. Задача 🟣Trelvis [#5430](https://vlusnoff-production.up.railway.app/#t/5430)
(одобрено Элвисом 05.09 18:35: «Бери первое — своё анимированное меню»). Ветка `wf25`, тег `wf25-start` — от `main`
**после слияния WF20, WF17, WF21, WF22, WF23**: порядок марафона (`docs/run-marathon-2026-09-04.json`) —
15, 16, 18, 19, 24, 20, 17, 21, 22, 23, **25**, 26, 27, 28. WF21 и WF23 добавляют в меню свои пункты и подменю, WF20
переносит меню влево и заводит панель редактора — брать ветку раньше значит переписывать `MinimizeMenu.swift` дважды.
Run-файл `docs/run-wf25-2026-09-05.json`, крон сторожа `watchdog-pimpmyclaude-wf25`; перед стартом снять сторожей
прошлых прогонов и вывести их run-файлы из `идёт` (WORKFLOW.md, «Финиш — проверяемое состояние»).

Макет (обязателен до кодинга, слово Элвиса 04.09 19:30): **`docs/mockup-wf25.html`** — живая анимация вылета/влёта,
три вида панели, каскад со всеми видами строк, схема ступеней и четыре вопроса с «(рекомендую)».

**Модели: оркестратор — Fable high (норма WORKFLOW.md); все субагенты и гейт — Opus 5 `max`** (04.09 18:40: Fable
экономим, Opus ниже `max` не брать) — батчи A1, A2, A3, D, V, гейт.
**Версию не поднимаем** — `Info.plist` в WF25 не трогается, релиз собирает WF17 (или следующий за ним).

**Один писатель.** WF25 — чистый Swift: `claude-patch/inject.js`, лоадер v7 и контракты `command.json`
не трогаются ни на байт, «Поставить снова» команде не нужно. Внутри волны писатель по `MinimizeMenu.swift`
тоже ровно один: A1 → A2 → A3 идут **цепочкой**, параллельных батчей нет ни в одну секунду.

## Слова Элвиса

> (05.09 10:40) «Меню на кнопке — слева от окна, правый край чуть левее и чуть ниже значка „свернуть“.
> **И чтобы вылетало с минуса, а обратно влетало.**»

> (05.09 18:35) «Бери первое — своё анимированное меню.» (из четырёх идей, задачи #5430–#5433)

Формулировка задачи #5430: «Своя панель-меню вместо NSMenu: все подменю, галки, примерка, ползунки;
анимация вылета/влёта. WF25.»

В WF20 анимация была разобрана и отложена сюда же (решение 2.2, вариант В): «Своя панель-меню — отдельный
будущий воркфлоу „своё меню“; втискивать его в WF20 нельзя: он один по объёму больше всех трёх частей вместе».

## Что выяснено по коду (только чтение, 05.09; агенту заново не копать)

1. **`NSMenu` анимировать нельзя, это подтверждено.** Показ — блокирующий вложенный цикл событий внутри
   `popUp` (`MinimizeMenu.swift:290`), окно меню рисует сам AppKit (`NSCarbonMenuWindow`), доступа к его слою нет,
   API появления/исчезновения нет, `menuWillOpen` зовётся, когда меню уже на экране. Разобрано в WF20, п. 13
   «Что выяснено»; заново не проверять.
2. **`MinimizeMenu.build(config:) -> NSMenu` (`:441`) — чистая сборка без AX и без `popUp`**, и именно её проверяют
   все тесты меню. Отсюда главное решение WF25: **`build` не трогаем вовсе, а рисуем готовое дерево `NSMenu`
   своей панелью** (решение 1). Ни один существующий тест меню при этом не краснеет и не переписывается.
3. **Что должен уметь рендерер — инвентарь по файлу** (`MinimizeMenu.swift`, показания на 05.09):
   - **16 подменю, глубина 4.** Верхний уровень: «🪟 Новое окно ▸», «🎨 Оформление ▸», «⋯ Ещё ▸». Внутри
     «Оформления»: Цвет, Шрифт, Размер ответов, Размер вопросов, Всем окнам, Изменить мою тему, Удалить мою тему.
     Внутри «Всем окнам ▸»: Цвет, Шрифт, два Размера, Раскрасить по кругу, Живые цвета. Самая глубокая цепочка —
     меню → Оформление → Всем окнам → Цвет (четыре панели на экране одновременно). WF21 добавит «▦ Расставить ▸»,
     WF23 — «🚀 Workflow ▸»: станет 18, глубина та же.
   - **Семь видов строк:** команда с иконкой и клавишей справа (`commandItem`, `:462`); строка с галкой
     (`state = .on`, темы/шрифты/размеры/рамка/тумблеры); строка с картинкой-кружком палитры 14×14
     (`swatch`, `:925`); строка с `attributedTitle` — имя шрифта нарисовано этим же шрифтом (`fontList`, `:854`);
     **погашенная шапка секции** (`header`, `:908`, `isEnabled = false`: «МОИ ТЕМЫ», «ТЁМНЫЕ», «СВЕТЛЫЕ»,
     «ВСЕМ ОКНАМ», «НЕДАВНИЕ ПРОЕКТЫ», «С ЗАСЕЧКАМИ»…); разделитель; строка-подменю со стрелкой.
     Плюс **погашенные обычные пункты** (в «🌈 Раскрасить по кругу ▸» всё гаснет, пока крутятся живые цвета,
     `:629`) и `toolTip` (полный путь папки проекта, `:493`).
   - **Два пункта с `NSMenuItem.view`** — `SliderMenuView` (`:1031`): «↔️ Поля по бокам» (`:897`) и «⏱ Скорость»
     живых цветов (`:696`). Это обычные `NSView` с `NSSlider`; в своей панели они станут просто подвидом строки.
   - **Клавиши** — `keyEquivalent` + `keyEquivalentModifierMask` (`:465-466`), рисует их сегодня AppKit;
     в своей панели рисуем сами (порядок системный ⌃⌥⇧⌘, как в `KeyMods.appKit`).
4. **Клик и наведение уже развязаны от `NSMenu`.** Нажатие — `BlockMenuItem` (`:1101`): `target = self`,
   `action = #selector(fire)`, внутри — замыкание. Значит рендерер зовёт `NSApp.sendAction(item.action, to:
   item.target, from: item)` и своей логики выбора не заводит. Предпросмотр по наведению — `NSMenuDelegate`
   `menu(_:willHighlight:)` у `PreviewMenuDelegate.shared` (`:1011`), который дёргает `BlockMenuItem.preview`.
   **Рендерер обязан звать делегата меню (`menuWillOpen`, `willHighlight`, `menuDidClose`), а не примерку напрямую** —
   тогда правило «примерка только в списке окна, у „Всем окнам ▸“ её нет» соблюдается само собой.
5. **Порядок «примерка → закрепление → конец примерки» держится на том, что `popUp` блокирующий**
   (`:182-186`, `:290-299`): флаги `previewed`/`committed` живут до конца показа, `endPreview` уходит **синхронно
   сразу после** `popUp`, а действие выбранного пункта отложено `DispatchQueue.main.async` и срабатывает
   следующим ходом. С асинхронной панелью этот кусок переезжает в замыкание закрытия, и **порядок обязан
   остаться прежним** (решение 5): лоадер читает `command.json` раз в 500 мс и берёт ПОСЛЕДНЮЮ запись.
6. **Флаг «меню открыто» читают соседи.** `isMenuOpen` (`:50`) → `ProjectPaint.isMenuOpen`
   (`ClaudeAXController.swift:68`, `ProjectPaint.swift:180`): пока меню открыто, фоновая покраска по проекту
   молчит. Он же гасит наведение (`hoverPaused`, `:59`). Панель обязана держать `menuOpen = true` **всё время
   жизни панели и её подменю**, а не только на миг открытия, — иначе через 2 с проект перекрасит окно
   прямо под открытым меню и собьёт примерку.
7. **Панель у нас уже есть, с граблями.** `ThemeEditor.swift` (WF20, 311 строк): `NSPanel`,
   `.nonactivatingPanel`, `isReleasedWhenClosed = false`, **сильная** `static var current` (критик В2 WF20:
   слабая освободила бы контроллер сразу после выхода из обработчика пункта), сторож окна раз в секунду
   (окно Claude закрыли — панель уходит следом), `Screens.flip`, чистая `origin(window:size:area:)`.
   Оттуда же главный урок про клавиатуру: **у неактивной панели LSUIElement-приложения клавиатуры нет**
   (критик В1 WF20 — поле имени в панели набирать нельзя, буквы уезжают в Claude).
8. **Щит и анимация тоже есть с чего списать.** `HUD.swift` (74 строки): borderless `NSWindow`,
   `level = .statusBar`, `collectionBehavior = [.canJoinAllSpaces, .stationary, .ignoresCycle,
   .fullScreenAuxiliary]`, `ignoresMouseEvents`, `orderFrontRegardless()`, гашение через
   `NSAnimationContext` — ровно те кирпичи, из которых собираются щит-окно и вылет.
9. **Позиция уже вынесена в чистую функцию и покрыта тестом.** `MinimizeMenu.origin(button:menuWidth:area:)`
   (`:321`, тест `testMenuOriginSitsLeftOfMinimizeButton`, `ClaudeAXTests.swift:549`): правый край на
   `rect.minX − 2`, верх на `rect.maxY + 2`, у левого края экрана — под левым краем окна. **Её не трогаем:**
   панель встаёт в ту же точку, только ширину даёт наш замер строк, а не `NSMenu.size`. Границы 120…600
   и запасные 280 остаются — они нужны фолбэку на системное меню.
10. **Списки длинные, прокрутка обязательна.** Тем в `claude-patch/themes.json` — 24 (15 тёмных, 9 светлых),
    плюс «Как у Claude», две шапки и два разделителя: 29 строк ≈ 640 px при высоте строки 22. Шрифтов
    до ~30 (четыре секции, моно ограничены десятью, `FontCatalog.monoLimit`). На экране Элвиса
    (usable 1055) влезает, на 13" ноутбуке команды (usable ~800) — уже нет. `NSMenu` прокручивает сам,
    наша панель обязана уметь (решение 7).
11. **Приложение — `LSUIElement`** (`app/Resources/Info.plist:23`), и `NSApp.activate(ignoringOtherApps: true)`
    перед показом уже зовётся (`:284`) — без него `popUp` не получает событий. Значит «активируем ради
    клавиатуры» — **не новая цена, а ровно сегодняшняя** (решение 6).
12. **Флаг отката ставить некуда, кроме `UserDefaults`.** Протокол `ClaudeAXControlling` (`ClaudeAX.swift:12`) —
    контракт между модулями, «менять только осознанно» (AGENTS.md); меню-бар приложения (`AppDelegate.swift`)
    в WF25 не трогаем, чтобы не драться за файл с WF17/WF21. Ключ `ownMenu` в `UserDefaults` домена
    `com.elvis.pimpmyclaude` (решение 8) — так же живут `autoPaint.last` и `liveColors.*`.
13. **Тесты меню сегодня — все против `build()`**, ни один не касается показа: `testAppearanceMenuStructure`
    (`ClaudeAXTests.swift:593`), `testMenuOriginSitsLeftOfMinimizeButton` (`:549`), `testMenuKeysMatchReadme` (`:64`),
    `testMenuItemsCarryKeyEquivalents` (`:121`), `testHoverPreviewsThemeAndFont` (`:759`),
    `testMenuHasSizeSubmenusWithChecksAndPreview` (`:822`), `testMenuFrameToggleFlipsAndPreviewsOn` (`:889`),
    `testNewWindowSubmenuListsProjects` (`:1333`), `testAutoPaintMenuHasPresetsRandomAgainAndReset` (`:1930`),
    `testLiveColorsMenuHasOffModesSpeedAndTones` (`:2363`), `testMenuHasProjectColorToggle`
    (`ProjectTests.swift:1131`). **Наш подход не трогает ни один** — это и есть главный аргумент за решение 1.
14. **Соседи по меню.** WF21 («▦ Расставить ▸» с раскладками, «Спрятать все») и WF23 («🚀 Workflow ▸»,
    «💤 /skilzzz») добавляют пункты в `build()`. Раз мы рисуем готовое дерево, **их пункты появятся в панели
    сами**, без единой правки в WF25. Обратной зависимости нет: они пишут в `MenuModel`/`build`, мы — в показ.

## Решения

### 1. Рисуем готовый `NSMenu`, а не переписываем меню (главное)

`MinimizeMenu.build(config:)` остаётся единственным местом, где меню задаётся: пункты, порядок, галки,
иконки, клавиши, замыкания. Новый код умеет ровно одно — **показать дерево `NSMenu` своей панелью**:
обойти `menu.items`, нарисовать строки, водить каскад, звать `NSApp.sendAction` по клику и делегата
по наведению.

Что это даёт:
- 11 существующих тестов меню остаются зелёными и продолжают проверять состав (в этом весь смысл);
- пункты WF21/WF23 приезжают в панель даром (п. 14);
- откат — одна строка: `usesOwnPanel = false`, и снова `popUp`, поведение до байта прежнее;
- галки, значки, погашенные пункты, `toolTip`, `attributedTitle`, ползунки-`view` берутся из `NSMenuItem`
  как из модели — своей копии структуры меню не заводим (иначе она разъедется с `build` в первом же WF).

Чего это стоит: `NSMenuItem` — не самая приятная модель (свойства читаются, а не строятся), и один
`NSView` нельзя показать в двух панелях сразу (для ползунков это не важно — дерево строится на каждый показ).

**Что не пытаемся повторить:** VoiceOver-доступность системного меню, системные стрелки прокрутки,
поведение при перетаскивании файлов на пункт, «моргание» выбранного пункта перед закрытием (macOS его
и сама уже почти не делает).

### 2. Три ступени, каждая — рабочее состояние

| Ступень | Что на экране | Готово, когда |
|---|---|---|
| **1. Свой верхний уровень + вылет** | Панель вместо `NSMenu` на верхнем уровне: строки, иконки, клавиши справа, подсветка под курсором, вылет с минуса и влёт обратно, закрытие по Esc и клику мимо. **Подменю пока системные**: клик по строке-подменю открывает обычное `NSMenu` через `popUp` в точке строки — каскад, прокрутка и клавиатура там работают как сегодня | «жжик» видно глазами, ничего не сломано; флаг `ownMenu` по умолчанию **выключен** |
| **2. Свои подменю** | Весь каскад своими панелями: 16 подменю, глубина 4, галки, кружки палитр, имена шрифтов своим шрифтом, шапки секций, погашенные пункты, ползунки прямо в панели, предпросмотр по наведению, прокрутка длинных списков | меню целиком в одном стиле; флаг всё ещё выключен |
| **3. Клавиатура и доводка** | ↑↓ по строкам, → открыть подменю, ← закрыть, ⏎ выбрать, Esc закрыть всё; анимация подменю; уважение «Уменьшить движение» | гейт живьём прошёл; флаг по умолчанию **включён**, системное меню остаётся запасным путём |

Ступень 1 самоценна: если после неё Элвис скажет «хватит», проект остаётся в рабочем виде — свой
анимированный верхний уровень и системные подменю (вопрос В1 внизу).

### 3. Анимация «вылет с минуса / влёт обратно»

Панель — своя, значит анимируем её слой, а не окно (двигать `frame` окна рывками нельзя — дрожит).

- Контейнер панели: `wantsLayer = true`; `layer.anchorPoint` — **угол, ближний к жёлтой кнопке**:
  обычно правый верхний (1, 1), а когда панель ушла под левый край окна (правило `origin` у края экрана) —
  левый верхний (0, 1). Точку старта считает чистая функция (тестируется).
- **Открытие:** `transform` от `CATransform3DMakeScale(0.08, 0.08, 1)` к единичному, `opacity` 0 → 1,
  **0,16 с**, `CAMediaTimingFunction(0.16, 1.0, 0.3, 1)` — резкий старт с мягким доводом («жжик»).
- **Закрытие:** обратно, **0,11 с**, ease-in, по завершении `orderOut`. Команда выбранного пункта уходит
  **до** анимации (решение 5) — задержки на глаз нет.
- **Подменю:** та же анимация, масштаб от 0,35, якорь — левый верхний угол у строки-родителя, 0,11 с.
- **Тень рисует слой**, у окна `hasShadow = false`: тень окна не масштабируется вместе со слоем и во время
  вылета висела бы прямоугольником в полный размер.
- **Фон панели непрозрачный** (решение 9 и вопрос В3): `NSVisualEffectView` с материалом `.menu` даёт
  системную вибрансь, но блюр «за окном» считает WindowServer по форме вьюхи, и при масштабировании слоя
  он отстаёт и мылит. Красиво в статике, грязно в движении — берём сплошной фон.
- **«Уменьшить движение»**: `NSWorkspace.shared.accessibilityDisplayShouldReduceMotion` → показываем сразу,
  без анимации. Три строки, зато правило системы.

### 4. Закрытие: щит, Esc, выбор пункта

- **Щит** — одно borderless-окно на объединение `NSScreen.screens.map(\.frame)`, прозрачное,
  `ignoresMouseEvents = false`, уровнем ниже панели, `collectionBehavior` как у HUD. Любой клик по щиту
  закрывает меню и **не доходит до приложения под ним** — ровно так ведёт себя и `NSMenu`.
- **Esc** — `keyDown` панели (ступень 3) либо, пока клавиатуры нет, тот же щит по правому клику.
- **Выбор пункта** — закрываем всю цепочку панелей.
- **Сторож окна** (как у `ThemeEditor`): раз в секунду проверяем, живо ли окно Claude; исчезло — панель
  уходит и примерка не залипает.
- **Порядок на выходе — байт в байт сегодняшний** (п. 5 «Что выяснено»):
  ```
  onClose(committed:)  →  menuOpen = false
                       →  if previewed && !committed && !MinimizeMenu.editorOpen { actions.endPreview(window:) }
                       →  app.focus(window: window)
  ```
  действие пункта при этом уже отложено `DispatchQueue.main.async` внутри `MenuConfig`-замыканий и
  срабатывает следующим ходом — как и при `popUp`.

### 5. Что делает `show()` после правки

`show(for:at:)` делится надвое: сборка `MenuConfig` (сегодняшние строки 150–278, **не трогаются**) и показ.
Показ — развилка:

```swift
if MinimizeMenu.usesOwnPanel {
    MenuPanel.open(menu: menu, at: rect, anchor: .minimizeButton) { committed in … onClose … }
} else {
    NSApp.activate(ignoringOtherApps: true)
    menu.popUp(positioning: nil, at: Screens.flip(point: point), in: nil)
    … сегодняшний хвост …
}
```

`previewed`/`committed` из локальных `var` переезжают в поля контроллера панели (их пишут те же замыкания
`MenuConfig`). `shows += 1` и `menuOpen = true` — до показа, как сейчас.

### 6. Активация и клавиатура — вариант А

**Активируем приложение (`NSApp.activate(ignoringOtherApps: true)`), как делает `popUp` сегодня** — цена та же,
что сейчас: пока меню открыто, заголовок окна Claude сереет, после закрытия фокус возвращает
`app.focus(window:)`. Панель при этом `canBecomeKey = true` и ловит `keyDown` сама, без глобальных мониторов
и без лишних разрешений.

Вариант Б (не активировать): Claude не сереет, панель `.nonactivatingPanel` мышью работает, но клавиш
у неё нет вовсе — Esc пришлось бы ловить глобальным монитором, а он **не съедает** событие, и тот же Esc
уехал бы в Claude. Отвергнут; вынесен вопросом В2 на случай, если Элвису важнее «Claude не сереет».

### 7. Каскад и прокрутка

- **Открытие подменю** — по наведению с задержкой 0,12 с (как система), клик открывает сразу.
- **Закрытие соседнего** — с задержкой 0,3 с и «коридором»: пока курсор движется в сторону открытого
  подменю (в треугольнике «строка → угол панели подменю»), соседние строки подсветку не перехватывают.
  Без этого меню бесит на первой же диагонали — это главный «незаметный» кусок работы каскада.
- **Положение подменю** — справа от строки, `+2` по x, верх по строке; не влезло справа — слева от панели;
  не влезло снизу — поднимаем, прижимая к нижнему краю рабочей области. Чистая функция, тест без экрана.
- **Прокрутка**: список выше `usable.height − 16` кладём в `NSScrollView` (`scrollerStyle = .overlay`,
  `autohidesScrollers = true`), колесо и трекпад работают штатно. Системных стрелок-«крышечек» не рисуем.
- **Экран считаем тот, на котором окно Claude**, а не главный: у `MinimizeMenu.origin` сегодня
  `Screens.mainUsableFrame` — его оставляем как есть (сужать правило не наша задача), но панели подменю
  считают по экрану панели-родителя.

### 8. Флаг отката

`MinimizeMenu.usesOwnPanel` = `UserDefaults.standard.object(forKey: "ownMenu") as? Bool ?? <дефолт ступени>`.
Дефолт: ступени 1–2 — `false`, на гейте ступени 3 — `true`. Вернуть системное меню:
`defaults write com.elvis.pimpmyclaude ownMenu -bool false` — строка в README и `docs/TEAM.md`.
Ни `ClaudeAXControlling`, ни меню-бар приложения при этом не трогаются (п. 12).

### 9. Внешний вид панели

Сплошной фон под системное меню (тёмная тема: `#2c2c2e` с рамкой `rgba(255,255,255,.09)`; светлая:
`NSColor.windowBackgroundColor`), скругление 10 (macOS 13–15) / 12 (macOS 26), строка 22 px, подсветка —
скруглённый прямоугольник цветом `NSColor.selectedContentBackgroundColor` с отступом 5 px по бокам,
шрифт `NSFont.menuFont(ofSize: 0)`. Колонки: галка 15 · значок 19 · заголовок · клавиша серым · стрелка ▸.
Ровно та же геометрия, что в макете. Третий вариант — **красить панель в тему окна** (акцент и фон из
палитры) — вынесен вопросом В3: технически это одна функция, но это уже «не как у macOS».

### 10. Не делаем в WF25

Меню-бар приложения и окно настроек; новые пункты меню (это WF21/WF23); правку `build()`, `MenuModel`,
`inject.js`, лоадера и контрактов `command.json`; перенос меню в другое место (положение — из WF20);
доступность VoiceOver; анимацию системных диалогов (`askThemeName`, `warn` — остаются `NSAlert`);
подъём версии и релиз; замену `NSMenu` где-либо ещё (меню-бар статуса — системное и остаётся системным).

## Батчи

| Батч | Что | Файлы | Модель |
|---|---|---|---|
| **A1** | **Ступень 1.** `MenuPanelLayout.swift` (чистая часть: `rows(for:)` из `NSMenu`, замер строки и панели, `keyHint(item:)`, точка якоря анимации, положение подменю, порог прокрутки); `MenuPanelRow.swift` (вьюха строки: галка, картинка, заголовок/`attributedTitle`, клавиша, стрелка, подсветка, погашенность, хост для `item.view`); `MenuPanel.swift` (окно панели, щит, анимация вылета/влёта, `menuWillOpen`/`menuDidClose`, `NSApp.sendAction` по клику, делегат по наведению, сторож окна, Esc); строка-подменю на этой ступени открывает системное `NSMenu` через `popUp`. Правка `MinimizeMenu.show`: развилка `usesOwnPanel`, флаг `UserDefaults`, переезд `previewed`/`committed` в замыкание закрытия. **Сборка: `bash tools/test.sh`** | `app/Sources/ClaudeAX/{MenuPanelLayout,MenuPanelRow,MenuPanel}.swift` (новые), `MinimizeMenu.swift`, `app/Tests/ClaudeAXTests/ClaudeAXTests.swift` | Opus max |
| **A2** | **Ступень 2.** Свои подменю: стек панелей, открытие по наведению 0,12 с, закрытие 0,3 с с «коридором», положение справа/слева/вверх, прокрутка длинных списков (`NSScrollView`), кружки палитр и `attributedTitle`, шапки секций и погашенные пункты, ползунки-`view` внутри панели, предпросмотр через делегата меню. `popUp`-ветка для подменю снимается. **Сборка: `bash tools/test.sh`** | `app/Sources/ClaudeAX/{MenuPanel,MenuPanelRow,MenuPanelLayout}.swift`, тесты | Opus max |
| **A3** | **Ступень 3.** Клавиатура (↑↓→←, ⏎, Esc, Home/End, пропуск шапок/разделителей/погашенных), анимация подменю, `accessibilityDisplayShouldReduceMotion`, дефолт флага `true`, мелкая доводка по гейту ступени 2. **Сборка: `bash tools/test.sh`** | те же три файла + `MinimizeMenu.swift`, тесты | Opus max |
| **D** | README (раздел «Меню на кнопке „Свернуть“»: панель вместо системного меню, что умеет, чем отличается, как вернуть системное — `defaults write … ownMenu -bool false`), `docs/TEAM.md` (одной строкой: меню теперь своё, вылетает с минуса; если что-то не так — как вернуть старое), `docs/CHECKLIST.md` (живые проверки меню в чеклист гейта). Стартует ПОСЛЕ A3, в код не пишет | `README.md`, `docs/TEAM.md`, `docs/CHECKLIST.md` | Opus max |
| **V** | verify по диффу, **только чтение**: `build()` и `MenuModel` не изменились ни на строку; `inject.js`, лоадер и контракты не тронуты; порядок `endPreview`/`committed`/`focus` совпадает с прежним; `menuOpen` держится всё время жизни панели и подменю (иначе проект перекрасит окно под меню); делегат меню зовётся, а `preview` напрямую — нет (иначе «Всем окнам ▸» начнёт красить по наведению); `NSApp.sendAction` не срабатывает у погашенных строк, шапок и разделителей; панель не остаётся в памяти после закрытия (`isReleasedWhenClosed = false` + обнуление `current`), щит снимается на всех выходах; при `usesOwnPanel = false` ветка `popUp` жива и не изменена; ни один старый тест меню не удалён и не ослаблен | | Opus max |
| **гейт** | `bash tools/test.sh` (счёт тестов в `AGENTS.md` и `CHECKLIST.md` поднять по факту); `tools/bundle.sh release`; подмена без простоя (bundle → `pkill` → `rm -rf /Applications/PimpMyClaude.app` → `ditto` → `open`); **версию в `Info.plist` НЕ поднимать**; живьём — список ниже; коммит после каждого батча, слияние в `main` `--no-ff` | | Opus max |

Сборку запускают только A1, A2 и A3, и не одновременно (идут по очереди): D пишет документы, V только читает.
Гейт живьём после A1 и после A2 — короткий (три-четыре пункта), полный список — на финише.

## Тесты (поимённо)

Всё, что можно проверить без экрана, живёт в `MenuPanelLayout` — чистой части (как `MinimizeMenu.origin`
в WF20). Три группы, **не меньше 14 новых проверок**:

**Ступень 1 (6):**
- `testMenuPanelRowsMirrorMenuTree` — `rows(for:)` на настоящем меню из `MinimizeMenu.build(config:)`:
  число строк совпадает с `menu.items`, разделители помечены разделителями, шапки — погашенными и
  некликабельными, у строк-подменю выставлен признак стрелки, у «Новое окно» — подменю, у «Workflow» — нет.
- `testMenuPanelKeyHintMatchesMenuItem` — «⇧⌘N», «⌥⌘↓», «⌘N» и пустая строка у «Workflow»; порядок
  модификаторов системный (⌃⌥⇧⌘), стрелки рисуются глифами ↑↓, а не кодами приватной зоны.
- `testMenuPanelSizeFitsWidestRow` — ширина панели считается по самой длинной строке с учётом колонок
  (галка 15 · значок 19 · заголовок · клавиша · стрелка), не меньше 120 и не больше 600; строка с `view`
  (ползунок 280) не даёт панели схлопнуться уже своей вьюхи.
- `testMenuPanelAnchorFollowsButtonSide` — якорь анимации: обычное положение → правый верхний угол (1, 1);
  панель ушла под левый край окна (кнопка у края экрана) → левый верхний (0, 1).
- `testMenuPanelUsesOwnPanelFlagDefaults` — `usesOwnPanel` читает `ownMenu` из подставленных `UserDefaults`:
  ключа нет → дефолт ступени; `false` → системное меню; мусор (строка) → дефолт, не падаем.
- `testMenuPanelFiresOnlyEnabledItems` — «сработать» отдаёт `false` на разделителе, шапке и погашенном
  пункте, `true` на обычном; у строки-подменю действие не шлётся (как в AppKit).

**Ступень 2 (5):**
- `testSubmenuOriginPrefersRightThenLeft` — подменю справа от строки; не влезло справа → слева от панели;
  не влезло ни там, ни там → прижимается к правому краю рабочей области; верх выравнен по строке.
- `testSubmenuFlipsUpWhenBottomIsShort` — длинный список у нижнего края экрана поднимается, но не уезжает
  выше верхней кромки.
- `testLongListScrolls` — 29 строк «Цвета» при рабочей области 800 → включена прокрутка, высота панели
  зажата рабочей областью минус поля; при 1055 прокрутки нет.
- `testCorridorKeepsSubmenuOpen` — чистая проверка «коридора»: курсор идёт по диагонали от строки к углу
  панели подменю → соседняя строка подсветку не перехватывает; ушёл вбок мимо коридора → перехватывает.
- `testPreviewGoesThroughMenuDelegate` — при наведении зовётся `menu(_:willHighlight:)` делегата меню;
  в списке «Всем окнам ▸» (делегата нет) примерка не уходит вовсе.

**Ступень 3 (3):**
- `testKeyboardSkipsSeparatorsAndHeaders` — ↓ по списку «Цвет ▸» перескакивает разделители, шапки
  «ТЁМНЫЕ»/«СВЕТЛЫЕ» и погашенные пункты; ↑ с первой строки уходит на последнюю.
- `testArrowRightOpensSubmenuAndLeftCloses` — → на строке-подменю открывает его и встаёт на первую живую
  строку; ← закрывает и возвращает подсветку родителю; на верхнем уровне ← ничего не делает.
- `testReduceMotionSkipsAnimation` — при поднятом флаге системы длительности обеих анимаций = 0.

**Обязаны остаться зелёными без единой правки** (это и есть приёмка решения 1): все 11 тестов меню из п. 13
«Что выяснено», включая `testAppearanceMenuStructure`, `testMenuOriginSitsLeftOfMinimizeButton`,
`testMenuKeysMatchReadme`, `testHoverPreviewsThemeAndFont`, `testMenuHasProjectColorToggle`. Красное здесь —
признак, что кто-то полез в `build()`.

## Гейт живьём

После A1: **(а)** навёл на жёлтую кнопку — панель **вылетает с минуса** за ~0,16 с и встаёт там же, где
стояло меню (правый край чуть левее кнопки, верх чуть ниже); **(б)** клик мимо и Esc — панель **влетает
обратно** в кнопку, Claude получает фокус; **(в)** «💬 Новый чат» и «⬇️ Свернуть» работают, клавиши справа
нарисованы; **(г)** «🎨 Оформление ▸» открывает системное подменю (ступень 1) и всё в нём работает как вчера.

После A2: **(д)** каскад до глубины 4 («Оформление → Всем окнам → Цвет»), ни одна панель не уезжает за
экран; **(е)** галки, кружки палитр и имена шрифтов своим шрифтом на месте; **(ж)** наведение на тему красит
окно, уход без выбора возвращает прежнюю (`probe.js` → `status().theme`, `preview:false`), клик закрепляет;
**(з)** «Всем окнам ▸ → Цвет ▸» по наведению **не красит** ничего; **(и)** ползунки «↔️ Поля по бокам»
и «⏱ Скорость» тащатся прямо в панели и не закрывают её; **(к)** список «Цвет ▸» на маленьком экране
прокручивается; **(л)** диагональ мышью от строки к подменю его не закрывает; **(м)** пока панель открыта,
цвет проекта окно не перекрашивает.

После A3: **(н)** ↑↓→←, ⏎ и Esc водят по меню и открывают подменю; **(о)** «Уменьшить движение» в системных
настройках — панель появляется мгновенно; **(п)** `defaults write com.elvis.pimpmyclaude ownMenu -bool false`
и перезапуск приложения возвращают системное меню целиком; **(р)** окно Claude закрыли при открытой панели —
панель ушла, примерка не залипла; **(с)** два окна Claude: панель на втором окне открывается после закрытия
первой, чужое окно не перекрашено; **(т)** живые цвета и цвет проекта при открытой панели молчат.

## Реалистичная оценка (честно)

- **Объём.** Три новых файла ≈ **900–1100 строк** Swift (`MenuPanel` ~380–450, `MenuPanelRow` ~230–280,
  `MenuPanelLayout` ~150–200) плюс ~120 строк правок в `MinimizeMenu.swift` и ~300 строк тестов. Это самый
  крупный по коду воркфлоу проекта после WF4/WF6 — для сравнения, весь `MinimizeMenu.swift` сегодня 1116 строк.
- **Время.** Ступень 1 — 50–80 мин работы агента, ступень 2 — 70–110 мин (каскад и прокрутка — самая
  «мышиная» часть), ступень 3 — 40–60 мин, документы и verify — 40 мин, гейт живьём — 40–60 мин.
  Итого **4–6 часов** прогона с тремя коммитами и двумя короткими гейтами по дороге.
- **Что реально может не влезть:** «коридор» каскада с первого раза почти никогда не выходит идеальным
  (система его подбирала годами) — закладываем доводку по гейту; прокрутка длинных списков без системных
  стрелок будет выглядеть чуть иначе; вибранси не будет (решение 3).
- **Ставка на откат.** Флаг `ownMenu` — не формальность: пока он выключен, вся волна не может сломать
  Элвису рабочее меню, и любую ступень можно оставить недоделанной до следующего воркфлоу.

## Вопросы Элвису (они же в макете, с рекомендациями)

1. **Как далеко идём.** Все три ступени сразу — своё меню целиком, включая подменю *(рекомендую)*;
   или ступень 1 (свой анимированный верхний уровень, подменю системные) — смотрим глазами и решаем дальше?
2. **Активация.** Как сейчас: пока меню открыто, приложение активно и заголовок окна Claude сереет, зато
   работают стрелки и Esc *(рекомендую)*; или не активировать — Claude не сереет, но клавиш в меню нет?
3. **Вид панели.** Как системное меню, серо-тёмная *(рекомендую)*; полупрозрачная с блюром (красивее
   в статике, мылит в движении); или **в цвет темы окна** — панель берёт фон и акцент у темы окна?
4. **Скорость «жжика».** 0,16 с вылет / 0,11 с влёт *(рекомендую)*; быстрее (0,10/0,08 — почти мгновенно);
   медленнее (0,24/0,16 — заметнее, но начнёт раздражать на десятом открытии).

## Открытые риски

- **Пиксель-в-пиксель системное меню не получится.** Нет вибранси (решение 3), нет системных стрелок
  прокрутки, скругления и отступы на macOS 26 отличаются от 13–15. Смягчение: флаг отката и вопрос В3.
- **«Коридор» каскада** — главный источник «меню бесит»: закрылось подменю на диагонали, перехватила
  соседняя строка. Смягчение: задержки 0,12/0,3 с, тест на коридор, доводка на гейте, откат флагом.
- **Клавиатура требует активации** — окно Claude сереет, пока меню открыто. Это ровно сегодняшнее
  поведение (`popUp` активирует), но раньше его никто не называл вслух; в вопросе В2 сказано прямо.
- **Щит глотает первый клик мимо** (как и `NSMenu`) — если сделать иначе, клик уедет в Claude и, например,
  отправит сообщение. Оставляем как у системы, в README отдельной строкой.
- **`menuOpen` на всё время жизни панели.** Забыть — значит получить перекраску окна проектом прямо
  под открытым меню (`ProjectPaint` ждёт 2 с) и сбитую примерку. Отдельный пункт verify.
- **Порядок `endPreview`.** С асинхронным показом легко получить «конец примерки» ПОСЛЕ закрепляющей
  команды — лоадер возьмёт последнюю за 500 мс, и окно вернётся к старой теме. Отдельный пункт verify
  и живая проверка (ж).
- **Утечки и зомби-панели**: `isReleasedWhenClosed = false`, сильная `static current`, снятие щита и
  сторожа на ВСЕХ выходах — грабли WF20 (критик В2), повторяем их защиту дословно.
- **Ползунки в панели**: клик по слайдеру не должен закрывать меню и не должен считаться выбором пункта;
  сегодня это обеспечивал сам AppKit (`NSMenuItem.view`).
- **Мультимонитор и фуллскрин**: панель и щит — `.canJoinAllSpaces, .fullScreenAuxiliary`; подменю считают
  по экрану родителя, верхний уровень — по `Screens.mainUsableFrame`, как сегодня (правило не трогаем).
- **Пересечение по файлам.** `MinimizeMenu.swift` берут WF20, WF21, WF23; ветку резать только после их
  слияния, а батчи обязаны перечитывать файл перед правкой, а не идти по номерам строк из «Что выяснено».
- **Прогон разрешений**: новых внешних действий у WF25 нет (`swift build/test`, `tools/bundle.sh`,
  `codesign`, `pkill`, `rm -rf /Applications/PimpMyClaude.app`, `ditto` уже проходили). Новых системных
  разрешений тоже нет: панель, щит и `keyDown` активного приложения не требуют ни Input Monitoring,
  ни чего-либо ещё сверх нынешних Accessibility и App Management.
- **Критика этого плана ещё не было** — прогнать критика (Opus max) перед стартом или разнести самому
  и написать об этом в презентации (WORKFLOW.md, п. 2).

## Дополнение Элвиса (05.09 19:20, задача «Цвета для людей» — максимальный приоритет)

Живые цвета (WF18) и «Раскрасить по кругу» (WF10) у Элвиса не нашлись — они внутри «Оформление ▸ Всем окнам ▸».
Требование: управление цветами простое, «для всех». В новом меню-панели (и **до него — быстрой правкой NSMenu сразу
после WF20**): пункт верхнего уровня **«🎨 Цвета ▸»**: 🌊 Живые цвета (галка вкл/выкл) · ⏱ Скорость ▸ (медленно /
обычно / быстро + ползунок) · 🎞 Перетекание ▸ (радуга · закат · океан · лес · ягоды — наборы автопокраски как кольца
живых цветов) · 🌈 Раскрасить окна (одним кликом, гармонично) · 🔁 Ещё раз · ─ · «Как у Claude (все окна)».
«Оформление ▸» остаётся для темы/шрифта/размера/рамки/полей. Проверить глазами гармонию случайных палитр (Элвис:
«чтобы мой эстет внутренне радовался»).
