**Вердикт: fix-needed** (1 блокер, 2 важных, 2 мелочи). Правок не вносил, боевые файлы не трогал (`git status` по `claude-patch/`, `tools/`, `app/` чист).

## Находки

**1. [БЛОКЕР] Сторожевого теста идемпотентности нет вовсе — `tests/idempotent.test.mjs` не написан**
Решение 4 плана («сердце воркфлоу», 9 проверок) не выполнено ни одним батчем: A-js сузили до люка и `VERSION`, A-js2 написал каркас и девять наборов, но идемпотентность не покрыл никто. В `tests/` файла нет; grep по `tests/` на «идемпот» — 0 совпадений.
Доказал ровно ту поломку, что план просил ловить: на копии в scratchpad снял `track(() => target.removeEventListener(type, handler, options))` у `on()` (`/Users/elvis/_ElvisProjects/PimpMyClaude/claude-patch/inject.js:57-60`) — `tools/test.sh --js` **зелёный, exit 0, 113 из 113**, а подписки в окне растут `17 → 34 → 51` за три прогона и после `dispose()` остаются 51. Это и есть «окно с двумя полосками через час работы».
Единственное, что рядом есть, — `tests/live-colors.test.mjs:176` («повторный инжект не удваивает интервалы») с ослабленным `assert.ok(count <= after)` и `tests/theme-store.test.mjs:131` (сессия переживает `reload()`): узлов, подписок, наблюдателей, `dispose()`, `__myclaudeUndo`, `__myclaudeFailure` и осиротевшего `<style id="myclaude-theme">` не проверяет ничто.
Сторожевые grep-проверки в `tools/test.sh` (пп. 4) — компенсация только от удаления трёх примет целиком, эту поломку они пропускают (проверено).
**Точная правка:** написать `tests/idempotent.test.mjs` по решению 4 (9 проверок). Инфраструктура готова и работает — проверил вручную через `loadInject({html:"composer"})`: после 1/2/3 прогонов `{listeners:17, observers:3, intervals:3}` и ровно по одному `#myclaude-input-handle`, `#myclaude-progress-bar`, `#myclaude-progress-tip`, `#myclaude-window-frame`; после `dispose()` — все счётчики 0, узлов 0, `window.__myclaude === undefined`, `window.__myclaudeUndo` — пустой массив (К16 сходится). На сломанной копии те же проверки краснеют. Заодно: `README.md` (рабочее дерево, раздел «Чего тесты не покрывают») сейчас записывает эту дыру как задуманную («полная сверка — живой двойной инжект на гейте») — строку переписать, когда тест появится.

**2. [важно] Коммит `aa96cde` подписан «WF20: план и макет…», а несёт всю работу WF24**
`claude-patch/inject.js` (люк + `VERSION`), `tests/dom.mjs`, `tests/load.mjs` и 5 наборов, `tools/test.sh`, 2 Swift-теста лежат в одном коммите вместе с `docs/plan-wf20-2026-09-04.md` и `docs/mockup-wf20.html`. Нарушены «коммит после каждого батча» и сверка «сделано vs описано по диффу» (WORKFLOW.md). **Правка гейта:** переписать историю ветки по батчам либо, если переписывать не хочется, честным сообщением следующего коммита и записью в `docs/run-wf24-2026-09-04.json`, что A-js/A-js2/B-swift лежат в `aa96cde`.

**3. [важно] Половина волны не закоммичена**
Untracked: `docs/CHECKLIST.md` (весь батч D), `tests/addressed.test.mjs`, `tests/live-colors.test.mjs`, `tests/size-half.test.mjs`, `tests/wf16-new-window.test.mjs` (46 из 113 случаев). Изменены и не закоммичены: `tests/dom.mjs`, `tests/load.mjs`, `tests/new-window.test.mjs`, `README.md`. Умри прогон сейчас — resume поднимет ветку без батча D и без 46 проверок. **Правка:** закоммитить на гейте.

**4. [мелочь] В run-файле нет числа Swift-тестов на старте ветки**
`docs/run-wf24-2026-09-04.json` — поля нет, хотя шапка плана (К6) требует записать N при создании ветки, и `docs/CHECKLIST.md:21` на него ссылается («не меньше, чем записано в run-файле волны»). Фактически сходится: `swift test` → **87 тестов, 0 падений**; функций `func test` 64 → 66 в `ClaudeAXTests` + 6 в `PatcherTests`, удалённых нет (в диффе только добавления). **Правка:** дописать `"swift_tests_start": 85, "swift_tests_now": 87`.

**5. [мелочь] Пункт V «сторожевой тест умеет краснеть» выполнить в заявленном виде нельзя** — команда из плана `MYCLAUDE_INJECT=… node --test tests/idempotent.test.mjs` не существует (см. п. 1). Механизм подмены при этом рабочий: проверил на копии со сломанным `statusKey` — `node --test tests/progress.test.mjs` exit 1, `tools/test.sh --js` на копии репозитория exit 1 («✗ node --test: красное»). Боевой `inject.js` не трогал.

## Что проверил (команды → результат)

- `git diff wf24-start..HEAD -- claude-patch/inject.js` → **ровно два ханка**: `VERSION` `wf19-a-1 → wf24-a-1` и люк. Постороннего в `inject.js` нет.
- Люк (`claude-patch/inject.js:4767-4774`): один оператор `if`, **без try/catch**, стоит между `window.__myclaude = api;` + его `track(...)` и хвостовым `try {` — точно по К4/К3. `grep -n __myclaudeTest` по репозиторию: в `inject.js` **одна строка**, в бою функции нет; остальные вхождения — `tests/load.mjs:53`, план, `README.md:543`.
- Все **32** имени люка объявлены в файле ровно один раз (скрипт по списку). `node --check claude-patch/inject.js` — чисто (node v26.5.0).
- Наборы против решения 3 (по именам случаев): theme-store 19 ≥ 12 ✅, theme-css 12 ≥ 10 ✅, progress 16 ≥ 12 ✅, workflow-insert 9 ≥ 6 ✅, new-window 11 ≥ 6 ✅ — все заявленные случаи присутствуют поимённо (приоритет слоёв, заглушки, перенос `w:`, `null` как сброс, битый JSON; `--cds-*`, Epitaxy, 19 серых, 60 знаков, `sizeCss`, фолбэки, `frameShadow`; образцы AGENTS.md, «2 из 2» мимо, последнее совпадение, зажим 0…100, имя из markdown-ссылки, `progressShares`; пустой текст / нет редактора / черновик / повтор / 64000; `newWindowSegment/SessionId/AtHome/StoreOk`, `busy`). Плюс 4 набора сверх плана (addressed 5, live-colors 19, size-half 6, wf16-new-window 16) = **113**.
- `bash tools/test.sh --js` → зелёный, exit 0, сторожевые строки на месте: node 26.5.0 · `node --check` · «статического ESM и require нет; динамический import(...) — 1» · «снятие прошлой установки на месте» · «claude.json и themes.json разбираются» · «VERSION: main «wf19-a-1» → «wf24-a-1»».
- `bash tools/test.sh` целиком → `✅ JS: 9 файлов, 113 проверок · Swift: 87 тестов`, exit 0. `--help` → exit 0, `--oops` → **exit 2**.
- Красный код на копии репозитория в scratchpad (боевые файлы не трогал): убрал `window.__myclaude?.dispose?.()` → exit 1 с внятным текстом; убрал `dispose,` из api → exit 1; сломал `statusKey` → exit 1 «node --test: красное».
- `cd app && swift build && swift test` → **Executed 87 tests, 0 failures**. Диффом: `testFrameRadiusMatchesMacOSMajor` через константы `LiveStyle` (+ ключ `frameRadius` из claude.json, + сверка с `WINDOW_FRAME_RADIUS` в inject.js) и `testCommandContractMatchesInjectJS` с двумя списками (`axOnly` = newChat/arrange/show, `pageOnly` = theme/status/live-colors) — ровно по К1/К7.
- `docs/CHECKLIST.md` — обе части на месте: 9 блоков гейта (тесты → версия → bundle → `ls …/Contents/Resources` без `tests` → подмена без простоя с `rm -rf` отдельной строкой → probe → живой двойной инжект → claude.css → документы → git) и 10 пунктов «Проверка на своём Маке» + что прислать + пустая таблица ответов. `tools/bundle.sh:75` копирует четыре файла поимённо + `workflow-kit` — каталог `tests/` в бандл попасть не может.

## Не сделал и почему

- Не проверял живьём (запрещено) и не чинил найденное (V только читает).
- Проверку «сторожевой тест краснеет» в заявленном плане виде — нечего запускать (п. 1); механизм `MYCLAUDE_INJECT` проверил на другом наборе.

## Follow-up

1. Дописать `tests/idempotent.test.mjs` (решение 4) — без него главный смысл WF24 не доставлен; счёт станет 122 проверки, строку сводки в `README.md` и `docs/CHECKLIST.md:20` подправить.
2. Гейт: закоммитить хвост A-js2 и D, разобраться с именем коммита `aa96cde`, дописать N в run-файл.
3. На будущее: сторожевые grep-проверки в `tools/test.sh` ловят только удаление примет целиком — после появления `idempotent.test.mjs` это перестаёт быть дырой, но пометить в README, что grep не заменяет тест.