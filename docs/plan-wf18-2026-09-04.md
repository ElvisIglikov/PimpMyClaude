# План WF18 — «Живые цвета»: окна плавно едут по цветовому кругу

Дата: 04.09.2026. Проект ⚪PimpMyClaude. Задача 🟣Trelvis [#5371](https://vlusnoff-production.up.railway.app/#t/5371)
(одобрено Элвисом 04.09 20:40: «все 10 делай»). Ветка `wf18`, тег `wf18-start` — **от `main` ПОСЛЕ слияния WF17**
(порядок прогона 15→16→17→18; WF15 и WF16 переписывают те же четыре файла меню — `MenuModel`, `MinimizeMenu`,
`ClaudeActions`, `ClaudeAXController`; ветка от старого main = ручное слияние этих четырёх файлов).
Run-файл `docs/run-wf18-2026-09-04.json`. Макет меню и живой показ скорости: `docs/mockup-wf18.html` (открыть файлом).
Модели: оркестратор Fable high; **все батчи, критик, verify и гейт — Opus 5 max** (слово Элвиса 04.09 18:40: Fable
экономим, Opus ниже `max` не брать; гейт Opus max — слово Элвиса 04.09 19:30, зафиксировано в WF14).
**Сторож-крон `watchdog-pimpmyclaude-0409` обязателен до волны**; `script_path` и `run_id` — в
`docs/run-wf18-2026-09-04.json` сразу после запуска. Версию в `Info.plist` WF18 **не трогает**, релиз — вместе со
следующим воркфлоу (как в WF19). **Запуск — только после того, как Элвис откроет макет и ответит на вопросы внизу него.**

## Слова Элвиса (дословно, задача #5371)

> «Ты можешь сделать плавное изменение цвета? Чтобы можно было скорость изменения цветов, и они по цветовому кругу,
> разные светлые, тёмные, зациклить плавненькое изменение. Чтобы все чаты меняли цвета, можно каждый по отдельности,
> можно все синхронно — у всех одинаковый цвет плавно менялся. Чтобы я скорость регулировал.»

## Что выяснено (чтение кода 04.09; агенту заново не копать)

1. **Генератор цвета уже есть и проверен** — `app/Sources/ClaudeAX/AutoPaint.swift` (WF10): `AutoPaint.palette(hue:light:strength:)`
   отдаёт шесть hex (accent, background, foreground, sidebar, panel, muted), фон HSL с потолком насыщенности 60 %
   (`maxBackgroundSaturation`), текст подтянут до контраста 7 (`pulled`), акцент уложен в полосу 4,5–6 (тёмная) / 4,5–7
   (светлая) — `banded`, `accentContrastCap`. `AutoPaint.hues(scheme:count:start:)` уже умеет раскладывать N окон по кругу
   (`.wheel`), `AutoPaint.theme(preset:hue:light:)` даёт готовую `Theme` с id `auto-<набор>-<hue>`.
   **Для WF18 новой цветовой математики писать не нужно** — нужна только выборка палитр по кругу.
2. **Как тема попадает на экран.** `claude-patch/inject.js`, раздел 2а: `applyTheme(theme, source)` (строка 990) —
   `themeSheet.replaceSync(themeCss(clean))` + `adoptSheet(themeSheet)`; `themeCss` (строка 435) собирает ≈200 строк CSS
   (переменные `--bg-*`, `--text-*`, `--accent-*`, `--cds-*` и блок Epitaxy для Claude Code, строка 593) — **всё с
   `!important` в `adoptedStyleSheets`**, не `<style>` (Claude зеркалит `<style>` главного окна в попапы).
   `applyTheme` **в хранилище НЕ пишет** — запись делает отдельная `writeLayers` (строка 1134). Значит крутить цвет
   можно прямо через `applyTheme(..., "live")`, и localStorage от этого не пухнет.
3. **Выключить = вернуть прежнее.** `restoreTheme(clear, onlyLayers)` (строка 1061) поднимает слой из хранилища по
   приоритету `chat:` → сессия окна → `main` → `*`. Стоп живых цветов — это ровно `restoreTheme(true, ["theme"])`:
   ни отдельного «запомнить, что было», ни своей ветки отката писать не надо.
4. **Кто ещё смотрит на цвет** (исправлено по критику В3). `accentColor()` (строка 937) читает **вычисленный**
   `--accent-brand` со страницы и зовётся ровно в одном месте — `paintFrame` (строка 967). **Рамка едет сама**:
   `applyTheme` вызывает `paintFrame()` на каждом применении темы. **Полоска прогресса сама НЕ едет**: она берёт
   акцент через алиас `progressAccent = accentColor` (строка 1632), но перекрашивается по своему пульсу
   `PROGRESS_IDLE_MS = 10000` (строка 1375) и по мутациям ленты — то есть сегменты до 10 секунд держали бы старый
   цвет рядом с уже уехавшим окном. Значит A-js обязан звать перекраску полосы сам — на грубом шаге (раз в 30°),
   а не на каждом тике.
5. **Новую команду лоадер пропустит.** Лоадер v6 (`patch-claude.mjs:203`, `Loader.swift:79`) рассылает `command.json`
   во все страницы без белого списка `action` (проверено в WF13 на `new-window`). **Перепатча у команды не будет.**
6. **Канал командами крутить нельзя.** `CommandChannel` (`CommandChannel.swift`): зазор между записями
   `minInterval = 0.6 с`, лоадер опрашивает файл раз в 500 мс и берёт ТОЛЬКО последнюю запись. Значит «Swift шлёт цвет
   раз в N секунд» упирается в 0,6 с × число окон на один круг раскраски (6 окон = 3,6 с), а всё, что чаще, теряется;
   плюс `command.json` переписывался бы сотни раз в минуту и душил бы очередь меню и `StatusFeed` (тик 60 с,
   приоритет `.status` — он и так уступает всем). **Вывод: анимация живёт на странице, Swift шлёт только команду
   «включить/выключить/скорость».**
7. **Окна синхронизируются без переговоров.** `localStorage` у окон Claude общий (контракт в AGENTS.md), а `document.title`
   у каждого окна свой — этого хватает: цвет считается от стенных часов (`Date.now()`), а не накоплением тиков, поэтому
   даже придушенное фоновое окно Electron (backgroundThrottling режет таймеры до 1 Гц) после пробуждения оказывается
   на правильном цвете, а не отстаёт.
8. **Меню.** После WF14 верхний уровень — вариант А, оформление живёт в «🎨 Оформление ▸», общее — в «🖥 Всем окнам ▸»
   (там же «🌈 Раскрасить по кругу ▸»), ползунок в меню уже сделан (`NSMenuItem.view` + `NSSlider`, пункт «↔️ Поля по бокам»
   в WF14) — **компонент ползунка переиспользуется, второй раз не изобретать**.
9. **Что WF18 НЕ трогает, потому что это чужие воркфлоу:** хвосты меню (галки, половина размера, тумблер рамки) — WF19;
   редактор своей темы ползунками — WF20; настройки на проект — WF15. Дублировать нельзя.

## Решения

### 1. Где считается цвет: страница, по стенным часам (рекомендую)

Swift присылает ОДНУ команду; дальше страница крутит цвет сама:

```
hue(t) = (start + 360 * ((Date.now() - epoch) / (period * 1000)) + phase) mod 360
```

- `epoch` и `period` одинаковы у всех окон → в режиме «синхронно» все окна независимо считают ОДИН и тот же цвет,
  без единого лишнего байта в канале.
- `phase` — сдвиг окна по кругу в режиме «каждое своим цветом»: `360 * индекс / число окон`; окно, которого в списке
  нет (открылось позже), берёт фазу из устойчивого хэша своего `document.title`.
- **Фаза защёлкивается один раз** (критик В5): вычислили при первом приёме команды — записали в `sessionStorage`
  под ключом `sessionKey()` (`inject.js:789`, `main` / `w:<title>`) и дальше от заголовка не зависим. Иначе у ГЛАВНОГО
  окна заголовок меняется на каждый чат (`chatKey`/`watchChatTitle`, строка 779) и окно перескакивало бы на другую
  точку круга при каждой смене чата. Список `titles` задаёт только начальное распределение.
- **Окна с одинаковым `document.title` получат одну фазу** (критик М6) — ровно как в автопокраске, где адресация тоже
  по заголовку. Это не баг, на гейте так и записать.
- Тик — `setInterval`; шаг по кругу ≤ 1,5°, частота **не выше 4 Гц** (см. риск про нагрузку). Отсюда же нижняя
  граница шкалы: 1,5° при 4 Гц = минимум 60 с на круг (критик В4). Окно не видно
  (`document.hidden`) — тик пропускаем, цвет догоняется на `visibilitychange`.

Отвергнуто: **Swift командами раз в N секунд** — упирается в очередь канала (п. 6 «что выяснено»); годится только для
«цвет раз в минуту», а Элвис просит «плавненькое».
Вариант на будущее (не в WF18): чистый CSS — зарегистрировать `@property --myclaude-hue` и крутить `@keyframes` без JS
вовсе. Нулевая нагрузка и идеальная гладкость, но насыщенность и светлота при этом фиксированы на весь круг,
то есть теряется вся коррекция контраста WF10 (жёлтый начинает кричать, синий проваливается) — ровно та претензия,
которую Элвис уже предъявлял к цветам.

### 2. Откуда берутся палитры: кольцо опорных точек из Swift (рекомендую)

Один генератор на проект — тот, что уже проверен тестами. Swift кладёт в команду **кольцо из 12 палитр** (шаг 30°),
страница между соседними интерполирует в sRGB покомпонентно:

- `ring.dark` и `ring.light` — по 12 объектов `{accent, background, foreground, sidebar, panel, muted}` в hex,
  считает `AutoPaint.palette(hue:light:strength: 0.5)`. Обе всегда, чтобы работал режим «как окно сейчас».
- **Сериализовать только через `Theme.paletteOrder` / `Theme.paletteValue`** (`ThemeCatalog.swift:15–21`) — критик В6:
  `AutoPaint.palette(hue:light:strength:)` возвращает `[String: String]` (`AutoPaint.swift:225`), а обход словаря
  в Swift непредсказуем, и побайтный контракт кольца плыл бы от запуска к запуску. Словарь напрямую не обходить.
- Размер команды ≈ **3,2–3,7 КБ** (критик М1: 24 палитры × 6 hex, не 1,6 КБ) — канал этого не замечает
  (файл и так перечитывается раз в 500 мс).
- Между опорными точками контраст лежит между контрастами соседей — на 30° это заметно глазу не больше, чем сама
  смена цвета.

Отвергнуто: **порт генератора в JS** — вторая копия формул контраста, которая через месяц разъедется со Swift.

### 3. Контракт команды `live-colors` (новый, побайтно)

`CommandChannel.write(action:fields:)` пишет поля В ТОМ ЖЕ ПОРЯДКЕ, что и собраны (алфавит и «всё строками» — только у
`write(action:extra:)`), числа даёт `CommandValue.number`, списки — `CommandValue.array`.

```json
{"id":"…","action":"live-colors","at":"…","scope":"all","on":true,"mode":"sync","period":300,"epoch":1757000000000,
 "light":false,"titles":["Trelvis","Dictatorik"],"ring":{"dark":[{…},…],"light":[{…},…]}}
```

- `on:false` — выключение; кроме `scope` и `on` полей нет, страница делает `restoreTheme(true, ["theme"])`.
- `mode`: `"sync"` (все одним цветом) | `"solo"` (каждое своим).
- `period` — секунды на полный круг, целое.
- `epoch` — миллисекунды, общая точка отсчёта; чтобы при смене скорости цвет не прыгал, Swift пересчитывает `epoch`
  так, чтобы текущий hue сохранился (`epoch' = now - (hue/360) * period' * 1000`).
- `light`: `true` — светлые, `false` — тёмные, `null` — «как окно сейчас» (страница смотрит `themeState.theme?.type`,
  нет темы — тёмная).
- `titles` — AX-заголовки окон слева направо, порядок как у «Расставить» (`paintableWindows()` в `ClaudeActions.swift`
  уже отдаёт ровно этот список — переиспользовать).
- `ring` — п. 2. Дальше `float`-полей нет: всё, что дробное, страница считает сама.

Команда одна на всё (`scope:"all"`) — ни в одном режиме N команд по окнам не пишем.

### 4. Как живые цвета уживаются с остальным

- **Хранилище не трогают вообще.** Идут через `applyTheme(theme, "live")`; `writeLayers` не зовётся; выключили —
  вернулась тема чата/окна, как будто ничего не было.
- **Предпросмотр сильнее.** `themeState.previewing === true` (мышь в подменю тем) — тик молчит, иначе примерка
  затиралась бы через 250 мс. Меню закрылось — крутёж продолжился.
- **Смена чата.** `applyChatEntry` покрасит окно в тему нового чата, следующий тик (≤ 250 мс) вернёт живой цвет —
  ничего не ломается, отдельного кода не нужно.
- **Порядок таблиц.** `applyTheme` сам двигает `themeSheet` в конец `adoptedStyleSheets` — приоритет живого цвета
  обеспечен без новой таблицы стилей. Четвёртый sheet не заводим.
- **«🌈 Раскрасить по кругу» на время живых цветов гасится** (критик В13: «взаимоисключающи» — неверное слово,
  автопокраска не режим, а одноразовое действие `autoPaint(preset:) -> Int`, `ClaudeActions.swift:452`, и выключать
  там нечего — исполнитель начал бы изобретать несуществующий тумблер). Пока живые цвета включены, пункты подменю
  «🌈 Раскрасить по кругу ▸» стоят `isEnabled = false`, в заголовке подменю подсказка «сначала выключи живые цвета».
  Обратно несимметрично: включение живых цветов у автопокраски ничего не спрашивает. Иначе автопокраска пишет темы
  окнам, а живой слой их немедленно перекрывает — Элвис видит, что кнопка «не работает» (вопрос 5 Элвису).
- **Цвет проекта (WF15) на время живых цветов молчит** (критик Б2 — блокер). WF15 идёт РАНЬШЕ (порядок 15→16→17→18)
  и красит окно при смене чата (`ProjectPaint`, «за ≤4 с»); живой слой это тут же перекрывает, и Элвис видит, как окна
  мигают между цветом проекта и живым. Поэтому `ProjectPaint` спрашивает `LiveColors.isOn` и не шлёт команду; выключили
  живые — следующая смена чата красит как обычно.
- **Память.** Состояние (`on`, `mode`, `period`, `light`, `epoch`) — в `UserDefaults` приложения (`liveColors.*`),
  плюс страница кладёт его в `localStorage` под своим ключом `myclaude-live-v1` — **общий у всех окон Claude**, поэтому
  окно, открытое во время крутёжа, подхватывает цвет само, без команды. Приложение при старте перешлёт команду заново.

### 5. Скорость (ползунок)

- 8 делений, круг за: **30 · 20 · 15 · 10 · 5 · 3 · 2 · 1 мин**. По умолчанию **5 минут**.
- Пункт «⏱ Скорость» — тот же `NSMenuItem.view` + `NSSlider`, что WF14 сделал для полей: слева заголовок, справа
  подпись «круг за 5 мин». Дебаунс 150 мс, на изменение — одна команда с новым `period` и пересчитанным `epoch`.
- **Быстрее минуты не даём** (исправлено по критику В4: прежняя шкала с делениями 40 с и 20 с противоречила
  собственному ограничению плана «шаг ≤ 1,5°, не выше 4 Гц» — 20 с на круг при 4 Гц дают 4,5° за тик, втрое больше;
  1,5° при 4 Гц = минимум 60 с на круг). Дальше это мигание, а не смена цвета.

### 6. Меню (вариант А — рекомендую)

Внутри «🖥 Всем окнам ▸», сразу под «🌈 Раскрасить по кругу ▸»:

```
🌊 Живые цвета ▸
    ⏹ Выключить                     ✓
    ──────────────
    🔗 Все окна одним цветом
    🎭 Каждое окно своим цветом
    ──────────────
    ⏱ Скорость   ——●——   круг за 5 мин
    ──────────────
    🌑 Тёмные                        ✓
    ☀️ Светлые
    🪟 Как окно сейчас        (гаснет при «🔗 Все окна одним цветом»)
```

**«🪟 Как окно сейчас» доступен только в режиме «🎭 Каждое окно своим цветом»** (критик М2): команда одна на всех
(`scope:"all"`), окна возьмут разные кольца — и «все одним цветом» перестанет быть одним цветом. В «🔗 Все окна одним
цветом» пункт `isEnabled = false`; в вопросе 2 макета это оговорено.

Вариант Б: тумблер «🌊 Живые цвета ✓» прямо в «🎨 Оформление ▸», а режим и скорость — в подменю рядом (на клик меньше,
но верхний уровень оформления пухнет — Элвис его только что просил разгрузить).
Вариант В: отдельное окно-панель с ползунками — отвергнут, в WF20 и так планируется редактор темы.

### 7. Чего в WF18 НЕ делаем

Живые цвета на одно окно (только «всем»); свою скорость каждому окну; крутёж светлых и тёмных вперемешку (мигание);
плавную смену шрифта/размера; `@property`-вариант из п. 1; правку лоадера; хвосты меню (WF19) и редактор темы (WF20).

## Батчи

| Батч | Что | Файлы | Модель |
|---|---|---|---|
| A-js | Раздел 2в inject.js «Живые цвета»: разбор команды `live-colors`, кольцо палитр + интерполяция в sRGB, hue от `Date.now()`, **фаза окна защёлкивается один раз в `sessionStorage` под `sessionKey()`** (строка 789) при первом приёме команды и дальше от заголовка не зависит — список `titles` задаёт только начальное распределение (В5); тик ≤4 Гц с паузой на `document.hidden` и на `themeState.previewing`, старт из `localStorage` `myclaude-live-v1` при загрузке окна, выключение через `restoreTheme(true,["theme"])`, **после применения живого цвета звать перекраску полосы прогресса — на грубом шаге (раз в 30°), не на каждом тике** (В3); **обработчик ставится в `onCommand` (строка 3818) рядом с `theme` и `status` — `if (action === "live-colors") { try { runLiveCommand(detail); } catch {} return; }` ДО гашения примерки (3836) и ДО `if (!state.editor?.isConnected) return;` (3843), сам проверяет `themable`** (В7); **`VERSION` → `wf18-a-1`** (сейчас `wf13-a-2`, строка 37 — по нему на гейте видно, доехал ли живой файл) (В11); поля `live` в `__myclaude.status()` (on, mode, period, phase, hue, source). **Один агент на файл. Сборку не трогает: только `node --check claude-patch/inject.js`** (Б1) | `claude-patch/inject.js` | Opus max |
| A-swift | Swift: `LiveColors.swift` (кольцо 12 палитр из `AutoPaint.palette`, **сериализация только через `Theme.paletteOrder`/`Theme.paletteValue`, словарь напрямую не обходить** (В6), шкала скоростей 30·20·15·10·5·3·2·1 мин (В4), пересчёт `epoch`, память в `UserDefaults`), **запись команды строкой: `commands.write(action: "live-colors", fields: LiveColors.fields(…))` — `ClaudeCommand.liveColors` НЕ заводить** (В1: enum — это пункты верхнего уровня и слоты хоткеев, `MenuModel.swift:89`; аналоги `theme` и `status` тоже пишутся строкой, `ClaudeActions.swift:274, 441, 498`); подменю «🌊 Живые цвета ▸» в «Всем окнам ▸» с галками режима и режима света («🪟 Как окно сейчас» гаснет в режиме «все одним цветом», М2), ползунок скорости (компонент WF14), **гашение пунктов «🌈 Раскрасить по кругу ▸» при включённых живых** (В13), **флаг `LiveColors.isOn` в `ProjectPaint` — не шлёт команду покраски проекта, пока живые включены** (Б2), пересыл команды при старте приложения; тесты, в т. ч. **«середины всех 12 хорд обоих колец: контраст `foreground`/`background` ≥ 4,5, акцент в полосе 4,5–6 (тёмные) / 4,5–7 (светлые)»** (В14 — не уложились, кольцо из 24 точек, шаг 15°, это одна константа). **Сборка волны у этого батча: `swift build && swift test`** (Б1) | `app/Sources/ClaudeAX/{LiveColors,MenuModel,MinimizeMenu,ClaudeActions,ClaudeAXController,ProjectPaint}.swift`, `app/Tests/ClaudeAXTests/ClaudeAXTests.swift` | Opus max |
| D | README (раздел «Меню» — «Живые цвета», скорость, режимы) и `docs/TEAM.md` (одна страница «как включить, как выключить, что это ест батарею»; **отдельной строкой: страница поднимает крутёж из `myclaude-live-v1` даже когда приложение не запущено — выключить можно только запустив его**, М5). Стартует после A-swift. Сборку не трогает (Б1) | `README.md`, `docs/TEAM.md` | Opus max |
| V | verify по диффу: контракт команды побайтно (порядок полей, числа не строками), **порядок ключей внутри каждой палитры кольца = `accent, background, foreground, sidebar, panel, muted`** (В6), живой слой не пишет в `localStorage` карту тем, выключение возвращает прежнюю тему, пауза на предпросмотре и на скрытом окне, **полоска прогресса перекрашивается вместе с темой** (В3), **`ProjectPaint` не шлёт команд при включённых живых цветах** (Б2), `inject.js` идемпотентен (`dispose` снимает интервал), лоадер и `WINDOW_FRAME_RADIUS` не тронуты, тесты не удалены, а переписаны. Ничего не пишет, ничего не собирает (Б1) | | Opus max |
| гейт | **`tools/bundle.sh release` и подмена без простоя** (В8: `pkill -f PimpMyClaude.app/Contents/MacOS` → `rm -rf /Applications/PimpMyClaude.app` → `ditto` → `open` — без этого нового подменю живьём просто не будет); `cd app && swift build && swift test` (51+); `node --check claude-patch/inject.js`; `cp inject.js` в Application Support; живьём: включить «каждое своим цветом» на 3 окнах — цвета разные и едут; «все синхронно» — цвета совпадают; ползунок скорости меняет темп без прыжка цвета; выключение возвращает тему чата; **замер нагрузки скриптом** (`ps -Ao %cpu,comm \| grep "Claude Helper (Renderer)"`, три замера до и три во время; число — в run-файл), порог — см. риски (М3); окно, открытое во время крутёжа, подхватывает цвет; предпросмотр темы в меню не мигает. **Дописать контракт `live-colors` (поля, порядок, `on:false`) в `AGENTS.md` → «Контракты», ключ `myclaude-live-v1` — в абзац про хранилище тем** (В9). Коммит после каждого батча, слияние в `main --no-ff` | | **Opus max** (В2) |

## Открытые риски

- **Нагрузка.** Каждый тик — `themeCss()` (≈200 строк, ~9 КБ) + `replaceSync` + полный пересчёт стилей страницы.
  На 4 Гц × 6 окон это заметно. **Порог гейта: рост CPU рендерера ≤ 10 % на окно.** Не уложились — план Б:
  разделить таблицы (большая с литеральными цветами — раз в 30°, маленькая только с переменными — на каждый тик),
  либо срезать потолок частоты до 2 Гц. Решение принимает гейт по замеру, не батч по догадке. **Замер — скриптом**
  (`ps -Ao %cpu,comm | grep "Claude Helper (Renderer)"`, три замера до и три во время), не глазами по Activity Monitor;
  числа кладутся в run-файл (М3).
- **Мигание и глаза.** Минута на круг — это уже заметное «дыхание» экрана. Если Элвис на гейте скажет «режет глаза» —
  режем нижний конец шкалы до 2 минут, это одна константа.
- **Батарея.** Крутёж не даёт окну простаивать; на ноутбуке это реальные проценты. `document.hidden` спасает только
  спрятанные окна. В TEAM.md — честная строка «жрёт батарею», в меню состояние видно галкой.
- **Фоновые окна Electron.** backgroundThrottling режет таймеры видимых-но-перекрытых окон до 1 Гц. Цвет от стенных
  часов это переживает (окно догоняет), но плавность там будет хуже — проверить на гейте на перекрытом окне.
- **Два писателя.** `inject.js` — один агент (A-js); Swift — второй (A-swift). Пересечение только через контракт
  команды, он зафиксирован в п. 3 ДО старта. WF15/WF16/WF19/WF20/WF21 трогают те же файлы меню —
  **параллельно с WF18 их не запускать**; WF18 ветвится от main со слитым WF17, WF19/WF20 — от main со слитым WF18.
- **Тесты меню** (переписано по критику В12; прежняя формулировка называла тесты наугад).
  `testAutoPaintMenuHasPresetsRandomAgainAndReset` (`ClaudeAXTests.swift:1427`) ищет подменю по заголовку на ВЕРХНЕМ
  уровне — его и так переписывает WF14. WF18 сломает не его, а **новый тест точного списка пунктов «🖥 Всем окнам ▸»,
  который напишет WF14** (имя уточнить `swift test` на старте): ожидание переписывается, не удаляется.
  `testSkeleton` и `testMenuKeysMatchReadme` НЕ трогаются — верхний уровень и хоткеи WF18 не меняет, а
  `ClaudeCommand.allCases.count == 10` (`ClaudeAXTests.swift:19`) остаётся нетронутым, потому что новый enum-кейс
  не заводится (В1). Verify обязан проверить, что тесты переписаны, а не удалены.
- **Смена чата на быстрой скорости.** Между `applyChatEntry` и следующим тиком окно на ≤250 мс мигнёт темой чата.
  Некритично, но на гейте посмотреть; если бросается в глаза — тик после смены заголовка зовётся немедленно.

## Решение (05.09 01:35): вопросы закрыты по рекомендациям — круг по умолчанию 5 мин, минимум 1; «как окно сейчас» только в режиме «каждое своим»; старт после WF15–16 (WF17 отложен на день). VERSION сейчас `wf16-a-1` (не `wf13-a-2`), тестов 74, inject.js 4400 строк — брать из файлов.

## Вопросы Элвису — в макете `docs/mockup-wf18.html` (внизу страницы, с «(рекомендую)»)

1. Режим по умолчанию: «каждое окно своим цветом» (рекомендую) или «все синхронно»?
2. Светлые/тёмные: держим тёмные (рекомендую), светлые, или «как окно сейчас» (только в режиме «каждое своим цветом»)?
3. Скорость по умолчанию 5 минут на круг, шкала **30 мин…1 мин** (рекомендую)?
4. Место в меню: «🖥 Всем окнам ▸ → 🌊 Живые цвета ▸» (рекомендую) или тумблером в «🎨 Оформление ▸»?
5. Живые цвета и «🌈 Раскрасить по кругу» — взаимоисключающие (рекомендую)?
6. Пауза на спрятанных окнах ради батареи (рекомендую)?

Правки по критику внесены 04.09 (Opus max)

## Критик (Opus max, 04.09)

Разнёс по WORKFLOW.md: нужность, подход по коду, модели, пересечения по файлам с WF14 и WF15–WF22,
ломающиеся тесты, владелец сборки, граница перепатча лоадера. Читал `inject.js`, `Loader.swift`,
`CommandChannel.swift`, `AutoPaint.swift`, `ThemeCatalog.swift`, `ClaudeActions.swift`, `MenuModel.swift`,
`ClaudeAXTests.swift` и планы WF15–WF22. **Нужность: задача одна (#5371), лишних батчей нет; выкинуть стоит
только третий режим света (см. М2).** «Что выяснено» по `inject.js` сверено построчно — номера строк 435/938/965/990/1061/1134
верны, п. 1–3, 5–8 подтверждаю. Неверен п. 4 (см. В3).

### Блокеры

**Б1. Сборкой волны никто не владеет.** WORKFLOW.md «Один build на волну» требует назвать батч; у соседей
(WF16/19/20/21) строка есть, здесь нет — два батча пойдут собирать одновременно.
Правка: в таблицу батчей, в ячейку A-swift дописать «**Сборка волны у этого батча**: `swift build && swift test`»;
в ячейку A-js — «только `node --check claude-patch/inject.js`, сборку не трогает»; D и V не собирают ничего.

**Б2. Драка с WF15 «Цвет проекта», который идёт РАНЬШЕ (порядок 15→16→17→18).** WF15 красит окно при смене чата
(`ProjectPaint`, «за ≤4 с»), живой слой это тут же перекрывает — Элвис увидит, как окна мигают между цветом проекта
и живым. В плане подавление описано только для автопокраски.
Правка: в решение 4 после абзаца про автопокраску добавить: «**Цвет проекта (WF15) на время живых цветов молчит**:
`ProjectPaint` спрашивает `LiveColors.isOn` и не шлёт команду; выключили живые — следующая смена чата красит как обычно»;
в батч A-swift в список файлов добавить `ProjectPaint.swift` (правка в одну строку — флаг), в V — пункт «ProjectPaint
не шлёт команд при включённых живых цветах».

**Б3. Точка ветвления устарела.** «от `main` ПОСЛЕ слияния WF14» верно было до порядка «15,16,17,18». WF15 и WF16
правят ровно те же `MenuModel/MinimizeMenu/ClaudeActions/ClaudeAXController`; ветка от старого main = ручное слияние
четырёх файлов меню.
Правка: в шапке заменить на «Ветка `wf18`, тег `wf18-start` — **от `main` ПОСЛЕ слияния WF17** (порядок прогона
15→16→17→18; WF15 и WF16 переписывают те же четыре файла меню)».

### Важно

**В1. `ClaudeCommand.liveColors` не нужен и вреден.** `ClaudeCommand` — это пункты ВЕРХНЕГО уровня меню и слоты
хоткеев (`MenuEntry(command:…)`, `MenuModel.swift:89`; `refreshHotkeys` перебирает `entries`). Ближайшие аналоги —
`theme` и `status` — в enum не входят и пишутся строкой: `commands.write(action: "theme", fields:)`
(`ClaudeActions.swift:274, 441, 498`).
Правка: в батче A-swift `ClaudeCommand.liveColors` заменить на `commands.write(action: "live-colors", fields: LiveColors.fields(…))`;
в рисках вычеркнуть «счёт команд в `testSkeleton`» — `ClaudeCommand.allCases.count == 10` (`ClaudeAXTests.swift:19`)
остаётся нетронутым.

**В2. Гейт стоит на Fable high** — против решения Элвиса 04.09 19:30 (гейт Opus max, зафиксировано в WF14) и против
WF16/19/20/21/22, где гейт Opus max.
Правка: в таблице батчей строка «гейт» — модель **Opus max**; в шапке «оркестратор Fable high; все батчи, критик,
verify и **гейт** — Opus 5 max».

**В3. Полоска прогресса за цветом НЕ поедет — п. 4 «Что выяснено» неверен.** `accentColor()` зовётся ровно в одном
месте — `paintFrame` (`inject.js:967`). Полоска берёт акцент через алиас `progressAccent = accentColor`
(строка 1632), но перекрашивается по своему пульсу `PROGRESS_IDLE_MS = 10000` (строка 1375) и по мутациям ленты —
то есть сегменты будут до 10 секунд держать старый цвет рядом с уже уехавшим окном.
Правка: п. 4 переписать («рамка едет сама — `applyTheme` зовёт `paintFrame`; **полоска сама не едет**»); в батч A-js
дописать «после применения живого цвета звать перекраску полосы — на грубом шаге (раз в 30°), а не на каждом тике»;
в V — пункт «полоска перекрашивается вместе с темой».

**В4. Шкала скорости противоречит собственному ограничению.** Объявлено «шаг по кругу ≤ 1,5°, частота не выше 4 Гц»,
но 20 с на круг при 4 Гц = **4,5° за тик** — втрое больше. 1,5° при 4 Гц дают минимум 60 с на круг.
Правка: решение 5 — «8 делений, круг за: 30 · 20 · 15 · 10 · 5 · 3 · 2 · 1 мин, по умолчанию 5 минут; быстрее минуты
не даём — при 4 Гц это уже больше 1,5° за шаг, то есть мигание». Из макета убрать деления 40 с и 20 с.

**В5. В режиме «каждое своим» цвет прыгает при смене чата.** Фаза считается от `document.title`, а у ГЛАВНОГО окна он
меняется на каждый чат (`chatKey`/`watchChatTitle`, `inject.js:779`) — окно перескочит на другую точку круга.
Правка: в батч A-js — «фаза защёлкивается один раз в `sessionStorage` под ключом `sessionKey()` (`inject.js:789`,
`main` / `w:<title>`) при первом приёме команды и дальше от заголовка не зависит; список `titles` задаёт только
начальное распределение».

**В6. Порядок ключей палитры не определён.** `AutoPaint.palette(hue:light:strength:)` возвращает `[String: String]`
(`AutoPaint.swift:225`) — обход словаря в Swift непредсказуем, побайтный контракт кольца поплывёт от запуска к запуску.
Правка: в решение 2 и в батч A-swift — «палитры кольца сериализуются через `Theme.paletteOrder` / `Theme.paletteValue`
(`ThemeCatalog.swift:15–21`), словарь напрямую не обходить»; в V — «порядок ключей внутри каждой палитры кольца
= `accent, background, foreground, sidebar, panel, muted`».

**В7. Место обработчика в `onCommand` не указано — по умолчанию он умрёт.** В `onCommand` (`inject.js:3818`) всё, что
ниже строки 3843 `if (!state.editor?.isConnected) return;`, не выполняется на окне без поля ввода; выше стоит гашение
примерки (3836).
Правка: в батч A-js — «`if (action === "live-colors") { try { runLiveCommand(detail); } catch {} return; }` ставится
рядом с `theme` и `status` — ДО гашения примерки и ДО проверки `state.editor`; обработчик сам проверяет `themable`».

**В8. Гейт не подменяет приложение.** Есть `cp inject.js`, нет «bundle → `pkill` → `rm -rf /Applications/PimpMyClaude.app`
→ `ditto` → `open`» — нового подменю живьём просто не будет (AGENTS.md, «подмена без простоя»).
Правка: в ячейку гейта первой строкой добавить `tools/bundle.sh release` и подмену без простоя.

**В9. Контракт не попадает в память проекта.** Все соседние планы дописывают новую команду в `AGENTS.md` → «Контракты».
Правка: в ячейку гейта — «дописать контракт `live-colors` (поля, порядок, `on:false`) в `AGENTS.md` → «Контракты» и
ключ `myclaude-live-v1` — в абзац про хранилище тем».

**В10. Сторож не назначен.** WORKFLOW.md: один сторож на каждый прогон, ставить ДО волны.
Правка: в шапку — «сторож-крон `watchdog-pimpmyclaude-0409` обязателен до волны; `script_path`/`run_id` — в
`docs/run-wf18-2026-09-04.json` сразу после запуска».

**В11. `VERSION` в `inject.js` не поднимается** (сейчас `wf13-a-2`, строка 37) — по нему на гейте видно, доехал ли живой файл.
Правка: в батч A-js — «`VERSION` → `wf18-a-1`».

**В12. Ломающиеся тесты названы наугад.** `testAutoPaintMenuHasPresetsRandomAgainAndReset` (`ClaudeAXTests.swift:1427`)
ищет подменю по заголовку на ВЕРХНЕМ уровне — его и так переписывает WF14. WF18 сломает не его, а НОВЫЙ тест точного
списка пунктов «🖥 Всем окнам ▸», который WF14 напишет.
Правка: риск «Тесты меню» переписать: «ломается тест точного списка «🖥 Всем окнам ▸» из WF14 (имя уточнить `swift test`
на старте) — ожидание переписывается, не удаляется; `testSkeleton` и `testMenuKeysMatchReadme` не трогаются:
верхний уровень и хоткеи WF18 не меняет».

**В13. «Взаимоисключающи с автопокраской» — нечего исключать.** Автопокраска не режим, а одноразовое действие
(`autoPaint(preset:) -> Int`, `ClaudeActions.swift:452`); исполнитель начнёт изобретать несуществующий тумблер.
Правка: в решение 4 — «пока живые цвета включены, пункты «🌈 Раскрасить по кругу ▸» **гасятся** (`isEnabled = false`)
с подсказкой в заголовке подменю «сначала выключи живые цвета»; обратно — включение живых цветов ничего у автопокраски
не спрашивает».

**В14. Гарантия контраста WF10 держится только в 12 точках.** Между опорными палитрами интерполяция идёт по хорде в
sRGB, а `pulled`/`banded`/`capped` нелинейны по тону — просадка возможна ровно в середине шага.
Правка: в батч A-swift — тест «середины всех 12 хорд обоих колец: контраст `foreground`/`background` ≥ 4,5, акцент
в полосе 4,5–6 (тёмные) / 4,5–7 (светлые)»; не уложились — кольцо из 24 точек (шаг 15°), это одна константа.

### Мелочи

**М1.** Размер команды в решении 2 занижен: 24 палитры × 6 hex ≈ **3,2–3,7 КБ**, не 1,6 КБ. Канал переживёт, но цифру поправить.

**М2.** Режим света «🪟 Как окно сейчас» ломает смысл «все окна одним цветом»: команда одна на всех (`scope:"all"`),
и окна возьмут разные кольца. Правка: пункт доступен только при «🎭 Каждое окно своим цветом», в «🔗 Все окна одним
цветом» гасится; в вопросе 2 макета оговорить.

**М3.** Замер нагрузки на гейте — не Activity Monitor, а скриптом
(`ps -Ao %cpu,comm | grep "Claude Helper (Renderer)"`, три замера до и три во время), число — в run-файл.

**М4.** Версия и релиз не оговорены. Правка: в шапку — «версию в `Info.plist` не трогаем, релиз вместе со следующим
воркфлоу» (как в WF19).

**М5.** Страница поднимает крутёж из `myclaude-live-v1` даже когда приложение не запущено — выключить можно только
запустив его. Строкой в `docs/TEAM.md` (батч D).

**М6.** Окна с одинаковым `document.title` получат одну фазу — как и в автопокраске (адресация по заголовку). Записать
в решение 1, чтобы на гейте не сочли багом.

### Пересечения по файлам и границы

- **Внутри волны:** `claude-patch/inject.js` — только A-js; Swift — только A-swift; `README.md` + `docs/TEAM.md` — только D
  (стартует после A-swift); V ничего не пишет. Правило одного писателя соблюдено.
- **С WF14:** WF18 стартует после его слияния и переиспользует ползунок и «🖥 Всем окнам ▸» — дублирования нет.
- **С соседями:** `MenuModel/MinimizeMenu/ClaudeActions/ClaudeAXController` трогают WF15, WF16, WF19, WF20, WF21;
  `AutoPaint.swift` — WF19 (снимает `prefersLightWindows`, меняет «Случайно») и WF20 (`palette(accentHue:)`);
  `README.md`/`docs/TEAM.md` — все. Порядок последовательный, ветка на каждый — **параллельно ни один из них с WF18
  не запускать**, и WF19/WF20 обязаны ветвиться уже от main со слитым WF18.
- **Перепатч лоадера не нужен:** `runCommand` (`Loader.swift:79`) рассылает ЛЮБОЙ `action` без белого списка, `inject.js`
  перечитывается по mtime, `myclaude-live-v1` — ключ localStorage, а не файл. **Нельзя** (это перепатч у всей команды):
  заводить новый живой файл вроде `live.json`, менять интервалы опроса (500/1000/2000 мс), `isClaudePage`, `cssOrigin`
  и сам блок `LOADER`. Если на гейте захочется хранить состояние файлом — это отдельный воркфлоу, не правка WF18.
