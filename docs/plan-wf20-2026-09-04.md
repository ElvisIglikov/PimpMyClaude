# План WF20 — «Редактор своей темы»: акцент, фон и шрифт ползунками с предпросмотром

Дата: 04.09.2026 21:00. Проект ⚪PimpMyClaude. Задача 🟣Trelvis [#5382](https://vlusnoff-production.up.railway.app/#t/5382)
(одобрено Элвисом 04.09 20:40: «все 10 делай»). Ветка `wf20`, тег `wf20-start` (от `main` после слияния WF14),
run-файл `docs/run-wf20-2026-09-04.json`, сторож `watchdog-pimpmyclaude-0409`.
Макет (обязателен до кодинга, слово Элвиса 04.09 19:30): **`docs/mockup-wf20.html`** — там живая примерка,
три варианта и шесть вопросов; ползунки в макете крутятся и красят окно теми же формулами, что уедут в код.
**Модели: все роли — Opus 5 max** (WORKFLOW.md 04.09 18:40: Fable экономим, Opus ниже `max` не брать) — батч E,
батч D, verify и гейт. Оркестратор — Fable high только если Элвис отдельно подтвердит, иначе тоже Opus max.
**Старт — только после слияния WF14 в `main`** (те же `MinimizeMenu`, `MenuModel`, тесты меню) и **не параллельно
с WF15/WF19**, которые тоже правят меню.

## Слова Элвиса (дословно)

> «Как тему свою сохранять, изменять, удалить — хочу такую возможность.» (04.09 вечер, из списка к WF14)
> Задача #5382: «Крутить свою тему ползунками, а не выбирать из готовых; предпросмотр.» · «все 10 делай» (20:40).

Раньше редактор цветов сознательно откладывали: WF5 («замороченное, не переносим: редактор тем, пипетка»),
WF6 п. 4 («„Мои темы“ — пара встроенная палитра + шрифт, редактора цветов нет»). WF20 закрывает этот долг.

## Что выяснено по коду (только чтение, 04.09; агенту заново не копать)

1. **Контракт `theme` менять не нужно вообще, и inject.js тоже.** Страница принимает ЛЮБУЮ палитру:
   `normalizeTheme` (`claude-patch/inject.js:424`) берёт шесть ключей `THEME_PALETTE_KEYS` (там же:332), гонит каждый
   через `normalizeHex`, а `id`/`name` — через `themeText` (:419, срезает `<>{};*\/"'`, 80 знаков). Готовая тема из
   каталога и «своя» отличаются только строкой id. Значит **WF20 — чисто Swift**, лоадер и страница не трогаются,
   «Поставить снова» команде не нужно.
2. **Предпросмотр уже есть и ровно тот, что нужен.** `preview:true` со слоями красит окно, не касаясь хранилищ
   (inject.js:1261), `preview:false` без слоёв возвращает сохранённое (:1269), закрепляющая команда сама гасит
   примерку по слоям (`endPreviewExcept`, :1231). Со стороны Swift — `ClaudeActions.sendPreview`
   (`ClaudeActions.swift:429`), `previewTheme` (:384), `endPreview` (:422). Примерка адресуется **заголовком окна**
   и фокуса не требует (:436) — панель поверх Claude ничего не ломает; **пустой заголовок → примерки нет вовсе**
   (там же `guard !title.isEmpty`).
3. **Вся цветовая математика уже написана — в автопокраске.** `AutoPaint.palette(hue:light:strength:)`
   (`AutoPaint.swift:225`) отдаёт готовые шесть цветов: фон с потолком насыщенности 60 % (`capped`, :260),
   текст подтянут до контраста 7 (`pulled`, :271), акцент уложен в полосу 4,5…6 (тёмная) / 4,5…7 (светлая)
   (`banded`, :291). Ручкам не нужен свой генератор — нужен один новый параметр: **отдельный hue акцента**
   (сейчас акцент берёт hue фона). Контраст и насыщенность считаются по hex (`saturation(hex:)` :361,
   `contrast(hex:hex:)` :346) — тем же способом их проверяет тест `testAutoPaintPaletteIsValidAndReadable`
   (`ClaudeAXTests.swift:1231`), его и повторяем для ручек.
4. **Обратный ход из палитры в ручки считается точно** для всего, что нарисовало приложение: hue берётся из
   `background` (кап трогает только насыщенность, тон сохраняется), сила — из светлоты фона (тёмная `l = 16 − 4·сила`,
   светлая `l = 96 − 4·сила`), сдвиг акцента — разница тонов `accent` и `background`, тёмная/светлая — из `type`.
   Для тем каталога и правленных руками восстановление приблизительное — панель об этом честно пишет.
5. **Файл своих тем расширяется без боли.** `MyThemesStore.parse` (`MyThemes.swift:90`) читает только знакомые ключи
   и **молча игнорирует незнакомые** — новое поле `knobs` старая версия приложения прочитает, потеряв только ручки.
   `json` (:114) пишет фиксированный порядок `id, name, type, palette, font, size, frame` — `knobs` дописывается
   последним, и проверка теста `contains("\"font\":null,\"size\":null,\"frame\":false")` (:1153) остаётся зелёной.
   `add` (:56) заводит новую запись всегда (WF14 учит его перезаписывать по имени); замены по id сейчас нет.
6. **Канал команд опрашивается раз в 500 мс и берёт ПОСЛЕДНЮЮ запись** (`CommandChannel.swift:37`), а `.preview`
   пишется мимо очереди сразу (:56, `deliver`). Значит ползунок без ограничения даст десятки атомарных записей
   в секунду (temp + `rename`), из которых лоадер увидит одну на 500 мс. **Троттлинг обязателен** — 300 мс
   и обязательная досылка последнего значения.
7. **Пока идёт примерка, окно не перекрашивается при смене чата**: `syncChatTheme` выходит на `previewing`
   (inject.js:1188). Панель, забытая открытой, «замораживает» тему окна до конца примерки — отсюда сторож окна
   и обязательный `endPreview` при закрытии.
8. **Окна в этом приложении уже делаются** — `HUD.swift:57` (NSWindow поверх всех, без фокуса),
   `SetupWindow`/`OperationWindow` в таргете приложения. Меню знает и окно (`AXUIElement`), и его рамку
   (`MinimizeMenu.show(for:at:)`, :125) — панели есть куда встать; координаты Quartz→AppKit через `Screens.flip`
   (`ClaudeApp.swift:13`), обрезка по `Screens.mainUsableFrame` (:21).
9. **Протокол `ClaudeAXControlling` (`ClaudeAX.swift`) не трогаем**: редактор открывается только из меню окна,
   в меню-бар не выносится — иначе пришлось бы менять контракт между модулями.

## Решения

### 1. Ручки (вариант А макета — панель; окончательный выбор за Элвисом)

Четыре ручки, все — поверх существующего генератора: **Цвет** (hue 0…359), **Акцент** (сдвиг −180…+180° от тона
фона), **Сила цвета** (0…100 %), тумблер **Тёмная / Светлая**. Плюс поле имени и кнопки «Сохранить» / «Отмена».
Гарантия читаемости остаётся жёсткой: `pulled`/`banded` никуда не деваются, нечитаемую тему ползунками не собрать
(вопрос 3 макета). Шрифт и размер — вопрос 2: если Элвис выберет «пять ручек», в панель добавляются ползунок по
каталогу шрифтов (`ClaudeActions.fonts`, порядок категорий `FontCategory`) и два ползунка кегля 11…24 (`Size.minPx/maxPx`),
и «своя тема» начинает собирать все четыре слоя разом — структура `MyTheme` это уже умеет (тема, шрифт, размер, рамка).

### 2. Генератор: один новый параметр

`AutoPaint.palette(hue:light:strength:accentHue:)` — `accentHue` по умолчанию `nil` (значит «как у фона»), поэтому
автопокраска и её пять тестов не меняются ни на строку. Акцент считается от `accentHue`, всё остальное — от `hue`;
`banded` работает как раньше. Ручки живут отдельной структурой `ThemeKnobs {hue: Int, accent: Int, strength: Int,
light: Bool}` (целые, чтобы `CommandValue` не пришлось учить дробям) с методами `palette()`, `theme(id:name:)`
и `ThemeKnobs.from(palette:type:)` — обратный ход из п. 4 «Что выяснено».

### 3. Панель (новый файл `app/Sources/ClaudeAX/ThemeEditor.swift`)

- `NSPanel`, стиль `[.titled, .closable, .utilityWindow, .nonactivatingPanel]`, `isFloatingPanel = true`,
  `level = .floating`, `hidesOnDeactivate = false`, `collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary]`,
  заголовок «Своя тема». Встаёт слева от окна Claude (нет места — справа), координаты через `Screens.flip`
  и обрезку по `Screens.mainUsableFrame`, чтобы не уехать за экран.
- **Панель одна на всё приложение** (`static weak var current`): открыли на другом окне — прежняя закрывается
  (со своим `endPreview`), новая перенацеливается.
- **Троттлинг примерки**: `previewInterval = 0.3` с; ползунок `isContinuous = true`, значения копятся, пишется одно
  на интервал, последнее значение досылается всегда (иначе окно застынет на предпоследнем цвете). Часы и таймер
  подставляются, как в `CommandChannel.init` — иначе тест не написать.
- **Заголовок окна перечитывается перед каждой записью** (`AX.string(window, kAXTitleAttribute)`): у главного окна он
  меняется вместе с чатом. Заголовка нет — примерка невозможна (п. 2 «Что выяснено»), панель пишет об этом строкой
  и остаётся рабочей (сохранить тему всё равно можно).
- **Сторож окна**: раз в 1 с проверяем, что окно живо; исчезло — `endPreview` уже некому, панель закрывается.
- **`endPreview` обязателен** на «Отмена», на крестик (`windowWillClose`), на закрытие панели сторожем и в `stop()`
  контроллера. Молчаливое залипание примерки — прямой запрет (п. 7 «Что выяснено»).

### 4. Сохранение и «изменить»

- Новая тема: id `MyThemesStore.makeID`, `add(name:…)` (после WF14 совпадение имени = перезапись — так и надо),
  затем `apply(myTheme:scope:"window", window:)` — обычная команда, она же гасит примерку.
- «Изменить»: панель открыта с id существующей темы → **новый `MyThemesStore.update(id:…)`**, который заменяет запись
  НА МЕСТЕ (id и позиция в списке сохраняются, галка в меню не съезжает), в том числе при переименовании.
  Это единственное отличие от «Сохранить как мою тему…», которое ключуется по имени.
- **`knobs` в `my-themes.json`** (вопрос 4 макета, рекомендую «да»): `"knobs":{"hue":262,"accent":0,"strength":50,"light":false}`
  последним полем записи. Есть — панель открывается ровно там, где её оставили; нет — ручки восстанавливаются из палитры
  (п. 4 «Что выяснено») и панель пишет «ручки подобраны по цветам».
- Слои шрифта/размера/рамки при сохранении берутся из уже применённых (`lastAppliedFont/Size/Frame`,
  `ClaudeActions.swift:322`) — то же поведение, что у нынешнего «Сохранить как мою тему…».

### 5. Меню (поверх структуры WF14, вариант А)

В «🎨 Оформление ▸», в нижнюю группу к своим темам: **«🎚 Своя тема…»** (открывает панель с ручками, подобранными по
нынешней теме окна) и **«✏️ Изменить мою тему ▸»** — список своих тем, пункт есть только когда они есть (как
у «🗑 Удалить мою тему ▸»). Порядок: 🎚 Своя тема… · ✏️ Изменить ▸ · 💾 Сохранить как мою тему… · 🗑 Удалить ▸ ·
🧹 Всё как у Claude. Предпросмотра по наведению у новых пунктов нет (они открывают панель, а не примеряют).

### 6. Не делаем в WF20

Пипетку и шесть цветов вручную (вариант В макета — отдельным воркфлоу, если Элвис захочет); тему на проект/папку
(WF15, задача #5370); плавную смену цветов (WF18, #5371); экспорт/импорт файла тем; ползунок в меню-баре;
правку `inject.js`, лоадера и контракта `theme`; перенос редактора в окно настроек приложения.

## Батчи

`claude-patch/inject.js` в этом воркфлоу не трогает НИКТО — правило «один агент на inject.js» соблюдено само собой.
Весь Swift — один батч: пункты меню, панель и тесты лежат в одних файлах, делить их между агентами нельзя
(правило проекта, тот же довод, что в WF14).

| Батч | Что | Файлы | Модель |
|---|---|---|---|
| E | `ThemeEditor.swift` (панель, ручки, троттлинг, сторож окна, `endPreview` на всех выходах), `ThemeKnobs` + обратный ход из палитры, `AutoPaint.palette(accentHue:)` (параметр со значением по умолчанию — автопокраска не меняется), `MyThemesStore.update(id:)` + поле `knobs` в записи, два пункта в «🎨 Оформление ▸», строки в `MenuModel`, шесть тестов. **Сборка волны у этого батча**: `swift build && swift test` | `app/Sources/ClaudeAX/{ThemeEditor,ThemeKnobs}.swift` (новые), `app/Sources/ClaudeAX/{AutoPaint,MyThemes,MinimizeMenu,MenuModel,ClaudeActions}.swift`, `app/Tests/ClaudeAXTests/ClaudeAXTests.swift` | Opus max |
| D | README (раздел «Меню» и «Мои темы») и `docs/TEAM.md` — что такое «Своя тема…», что ползунки не дают испортить читаемость, что «Изменить» правит тему на месте. Стартует ПОСЛЕ E | `README.md`, `docs/TEAM.md` | Opus max |
| V-E | verify по диффу, **только чтение, файлов не пишет**: контракт `theme` не изменился ни на байт и `inject.js` не тронут; `endPreview` есть на ВСЕХ выходах панели (отмена, крестик, сторож окна, `stop()`); троттлинг реально ограничивает записи и досылает последнее значение; заголовок перечитывается перед каждой записью; `accentHue` не поменял поведение автопокраски по умолчанию; `knobs` дописан последним полем и незнакомые поля по-прежнему игнорируются; `update(id:)` сохраняет позицию в списке; тесты меню WF14 переписаны, а не удалены | | Opus max |
| гейт | `swift build && swift test` (счёт поднять в `AGENTS.md`); `tools/bundle.sh release`; подмена без простоя (bundle → `pkill` → `rm -rf /Applications/PimpMyClaude.app` → `ditto` → `open`); живьём: (а) панель встаёт рядом с окном, ползунок красит окно за ~0,3–0,5 с; (б) «Сохранить» → тема в «МОИ ТЕМЫ» с галкой, в `my-themes.json` есть `knobs`; (в) «Отмена» и крестик возвращают прежний цвет (`probe.js` → `status().theme`, `preview:false`); (г) «Изменить мою тему ▸» открывает те же положения ручек; (д) окно закрыли при открытой панели — панель ушла, примерка не залипла; (е) панель на втором окне — первая закрылась, чужое окно не покрашено; коммит после каждого батча, слияние в `main` `--no-ff` | | Opus max |

**Тесты (шесть, поимённо):** `testThemeKnobsMakeReadablePalette` (сетка hue × сдвиг акцента × сила × тёмная/светлая:
текст ≥ 7, акцент в полосе, насыщенность фона ≤ 60 — по образцу `testAutoPaintPaletteIsValidAndReadable`);
`testThemeKnobsRoundTripThroughFile` (knobs записались и прочитались; файл без knobs даёт ту же палитру);
`testThemeEditorThrottlesPreview` (десять движений за 100 мс на подставленных часах → одна запись + досылка, все
записи `preview:true`, `scope:"window"`); `testThemeEditorSaveWritesMyThemeAndCommand`;
`testThemeEditorCancelEndsPreview` (`{"preview":false}` без слоёв); `testMenuHasThemeEditorItems`
(«🎚 Своя тема…» всегда, «✏️ Изменить ▸» — только при непустом списке). Тесты автопокраски, предпросмотра и меню
из WF12–WF14 обязаны остаться зелёными без правок, кроме ожиданий структуры меню.

## Вопросы Элвису (в макете, с рекомендациями)

1. Панель или ползунки прямо в меню — **рекомендую панель**. 2. Три ручки или пять (со шрифтом и размером) —
**рекомендую три**. 3. Держать читаемость силой — **рекомендую да**. 4. Запоминать положение ручек в файле —
**рекомендую да**. 5. Крестик = «Отмена» — **рекомендую да**. 6. Красить только это окно — **рекомендую да**.
Запуск — после ответа (хватит буквы варианта и «да/нет» по пунктам).

## Открытые риски

- **Пересечение по файлам.** `MinimizeMenu`, `MenuModel` и тесты меню правят WF14 (идёт), WF15 и WF19 (запланированы).
  WF20 стартует только после слияния WF14 и не в одной волне с WF15/WF19 — иначе два писателя на файл.
- **Троттлинг.** Без него ползунок даёт десятки атомарных записей в секунду, из которых лоадер видит одну на 500 мс;
  с ним окно отстаёт от ручки на треть секунды — это и есть цена, на глаз незаметная.
- **Залипшая примерка.** Пока панель открыта, окно не перекрашивается при смене чата (inject.js:1188). Лечится
  закрытием панели; на гейте проверяем `status().preview` после каждого выхода.
- **Заголовок окна меняется вместе с чатом** — адрес примерки перечитываем каждый раз; пустой заголовок (окно без
  имени чата) примерку отключает вовсе, панель об этом пишет.
- **Панель поверх Claude.** `.nonactivatingPanel` не должен уводить окно Claude на второй план и не должен закрывать
  крашеное окно собой — на гейте посмотреть глазами; при беде панель уезжает к краю экрана.
- **Восстановление ручек для чужих тем** (каталог, правка руками) приблизительное: цвет совпадает, сила может
  разойтись на несколько процентов. Панель пишет «подобрал по цветам»; после первого сохранения ручки точные.
- **`my-themes.json` правит и Элвис руками**: новое поле `knobs` не должно ронять разбор — тест на файл без него,
  с мусором в нём и со старым форматом обязателен.
- **Одинаковые имена своих тем.** «Сохранить как мою тему…» перезаписывает по имени (WF14), редактор — по id;
  переименовав тему в имя соседней, Элвис получит две одинаковые строки в списке. Принимаем: id разные, галка
  не путается; лечится удалением лишней.
