# WF43 — Чистка по Ревизору ⚪PimpMyClaude

Заведён 08.09.2026 11:00. Оркестратор и ВСЕ роли — **Opus 5 max** (Элвис: подписка Fable заканчивается,
Fable в этом чате не звать). Вход: `docs/revizor-wf42-2026-09-08.md` (выжимка) и `.json` (полные отчёты).
Ревизия a116786: 12 зон, средняя оценка **3,1 из 5**, 56 рисков, опровергнуть не удалось ни одного.
Находки заведены задачами [#5713](https://vlusnoff-production.up.railway.app/#t/5713)–[#5761](https://vlusnoff-production.up.railway.app/#t/5761).

Цель — довести зоны до 5 и НЕ добавить ни одной новой возможности. Правило раздувания: батч правит ровно
своё, «заодно» ничего не чинит, новых слоёв и флагов не заводит, чужое соседнее — диагнозом в отчёт.

## Что нельзя трогать (собрано из keepAsIs всех 12 зон)

Лоадер в `app.asar` (его смена = перепатч у всей команды); имя каталога `MyClaude`; `adoptedStyleSheets`
вместо `<style>`; `--cds-alpha-*`, `--cds-bg-neutral`, `--cds-text-danger`, `--danger-000` не переопределять;
хвост `:not(:is(.artifact-markdown-preview …))` у правил кода; вежливое владение `probe.js` (метка в первой
строке, 10 мин, 60 с молчания); пауза примерки 0,5 с; `NSApp.activate(ignoringOtherApps:)` перед `popUp`;
Carbon-хоткеи; побайтный порядок полей во всех командах и эталонах канала; тесты-сторожа упаковки и границ.
Полный список — раздел «НЕ ТРОГАТЬ при чистке» у каждой зоны в выжимке.

## Волна 1 — идёт без Элвиса (ничего из этого не требует его решения)

### Батч Д — документы (Opus 5 max)
Цель: паспорт перестаёт врать новому чату. Задачи #5752 #5753 #5754 #5755 #5756 #5751.
Файлы (только эти): `CLAUDE.md`, `README.md`, `docs/TEAM.md`, `docs/CHECKLIST.md`.
Готово, когда: лоадер везде назван v7; числа тестов 225/39/202; версия 1.4.0 (8); раздел «Текущее» описывает
сегодняшнее состояние, а не отменённую очередь; правило одного писателя названо для трёх полос, CLI в своей;
в чеклист добавлены шаги «плитки раскладок мышью» и «сохранить → закрыть окно → вернуть».
Проверки: `bash tools/test.sh` (сторожевые grep не должны покраснеть), глазами — ни одного `v6`, ни одного `1.3.0`.

### Батч П — канал «Пимп» и раскладки, Swift (Opus 5 max) · держит сборку волны
Цель: то, что написано ночью, перестаёт врать и залипать. Задачи #5728 #5729 #5730 #5731 #5732 #5733
#5680 #5681 #5682 #5684 #5688 #5689 #5690 #5691 #5556 #5557 #5558 #5560 #5595.
Файлы: `app/Sources/ClaudeAX/PimpChannel.swift`, `LayoutsStore.swift`, `ArrangeLayout.swift`, `Screens*.swift`,
`LayoutPicker.swift`, `ProjectPaint.swift` (только кэш #5595), `app/Tests/ClaudeAXTests/PimpChannelTests.swift`.
Готово, когда: снимок раскладки не пишет пустышку и не зовёт её «N окон»; безымянный попап не занимает `main`;
возврат обрывается, когда Claude пропал; `screen` в ответе называет настоящий экран; новое окно рождается на
экране окон, а не активного окна; `⌥⌘A` проверяет, влезает ли сетка; повторы `setFrame` не спят на главной нити.
Проверки: `swift test` зелёный и тестов не меньше 202; новые тесты на каждый пункт.

### Батч С — страница: темы, ключи, полоска, окна (Opus 5 max) · ЕДИНСТВЕННЫЙ писатель inject.js
Цель: цвет не теряется и не стирается, полоска не врёт. Задачи #5742 #5743 #5744 #5713 #5714 #5718 #5533 #5532.
Файлы: `claude-patch/inject.js`, `tests/*.test.mjs` только те, что этих разделов касаются.
Готово, когда: при живых цветах выбор темы не пропадает молча (страница не мешает решению Swift-половины);
«тема всем окнам» не уносит личные цвета без возврата; «Новое окно ▸ проект» кладёт цвет и под `id:`;
карточка сегмента не открывается сама; обычная фраза с ✅ не красит полосу «готово»; «Обкэшить» говорит,
когда отказал. `VERSION` поднят.
Проверки: `node --test tests/` зелёный; идемпотентность (число таблиц стилей не растёт).

## Волна 2 — после гейта волны 1 (тоже без Элвиса)

- **Батч Т — тесты и гейт**: #5683 #5749 #5757 #5758 #5759 #5760 #5761. Файлы: `tools/test.sh`, `tests/dom.mjs`,
  новые наборы, `app/Tests/PatcherTests/`. Плавающий тест, счёт CLI-тестов в сводке, тесты патчера перестают
  молча пропускаться, ручка над полем получает первые тесты, лоадер v7 проверяется хотя бы синтаксисом.
- **Батч А — приложение и патчер**: #5722 #5723 #5724 #5725 #5726 #5727.
- **Батч К — команды и меню**: #5734 #5735 #5736 #5737 #5715 #5716 #5535.
- **Батч Ц — проекты и темы окон**: #5738 #5739 #5740 #5741 #5534 #5537.
- **Батч П2 — CLI и скилл**: #5745 #5746 #5747 #5748 #5750 #5559 #5691(CLI-часть).

## Что ждёт Элвиса (в чистку не идёт)

- Релиз 1.4.0 команде (#5381) и инструкция от 1.2.0 — нужен он: нотаризация и рассылка.
- macOS 13–15 у команды — ответов нет ни одного.
- Разрез `inject.js` на модули (#5585) — это отдельная большая работа, решать после чистки.
- `status.json` на мегабайты (#5536) — правка лоадера = перепатч у всей команды.
- Всё, что проверяется только мышью: плитки раскладок, «запомни → верни», «Обкэшить», перезапуск Claude.

## Порядок и дисциплина

Ветка `wf43` от `main` (тег `wf43-start`). Один писатель на файл; `inject.js` — только батч С; сборку волны
держит батч П. Коммит на каждый батч делает гейт. Красное чинит гейт до отчёта. Полные тесты — на гейте,
не параллельно.
