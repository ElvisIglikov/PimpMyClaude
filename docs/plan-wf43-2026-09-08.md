# WF43 — Чистка по Ревизору ⚪PimpMyClaude

Заведён 08.09.2026 11:00, переписан 11:35 **по разносу критика** (Opus 5 max, отчёт — в run-файле).
Оркестратор и все роли — Opus 5 max (Элвис: подписка Fable заканчивается, Fable в этом чате не звать).
Вход: `docs/revizor-wf42-2026-09-08.md` (выжимка) и `.json` (полные отчёты, ревизия a116786).
12 зон, средняя оценка **3,1 из 5**, 56 находок, опровергнуть не удалось ни одной.
Задачи — [#5713](https://vlusnoff-production.up.railway.app/#t/5713)…[#5761](https://vlusnoff-production.up.railway.app/#t/5761) плюс старые хвосты.

Цель — довести зоны до 5 и **не добавить ни одной новой возможности**. Правило раздувания: батч правит ровно
своё, «заодно» ничего не чинит, новых слоёв и флагов не заводит, чужое соседнее — диагнозом в отчёт.

## ⚠️ Ловушки, названные критиком (читать до правки)

- **`CLAUDE.md` — симлинк на `AGENTS.md`** (git mode 120000). Править ТОЛЬКО `AGENTS.md`; инструмент, пишущий
  файл целиком по пути `CLAUDE.md`, заменит симлинк копией и разведёт паспорт надвое.
- **Живой `inject.js`** в `~/Library/Application Support/MyClaude/` во время волны НЕ обновлять: лоадер
  перечитывает его по mtime и прогоняет во всех открытых окнах Элвиса. Он за компьютером и работает.
- **`probe.js` не класть** — канал держит приложение; чужой файл на 10 минут глушит карту чатов, цвет проекта
  и `layout save`.
- **Живьём ничего не двигать**: ни `pimp.py arrange`, ни `layout restore`, ни ⌥⌘A, ни плитки — это окна Элвиса.
- **Приложение не пересобирать и не подменять** в `/Applications` в ходе волны (у него пропадёт меню на жёлтой
  кнопке). Только `swift build` / `swift test` в `app/.build`.
- **Сторожа по тексту исходника краснеют от безобидных правок**: `tools/test.sh:75` привязан к строке
  `dispose,`, `tests/size-half.test.mjs:84-91` — к трём кускам текста `inject.js`. Батч С правит их вместе с кодом.
- Копия `window-themes.json`, `claude.json`, `my-themes.json` снята до старта в scratchpad прогона.
- Заплатка #5683 наложена ДО волны (потолок плавающего теста 5 → 25 мс, коммит `edc7add`).

## Что нельзя трогать (из keepAsIs всех 12 зон)

Лоадер в `app.asar` (смена = перепатч у всей команды); имя каталога `MyClaude`; `adoptedStyleSheets` вместо
`<style>`; `--cds-alpha-*`, `--cds-bg-neutral`, `--cds-text-danger`, `--danger-000`; хвост
`:not(:is(.artifact-markdown-preview …))` у правил кода; вежливое владение `probe.js`; пауза примерки 0,5 с;
`NSApp.activate(ignoringOtherApps:)` перед `popUp`; Carbon-хоткеи; побайтный порядок полей всех команд и
эталонов; тень `chat:<имя>` рядом с `id:` (снос = попапы разом теряют цвет); тесты-сторожа упаковки и границ.

## Выброшено из чистки (по критику)

- **#5535** (поле `chat` остальным командам) — расширение контракта на двух концах, это новая возможность.
- **#5559** (`layers` по правде страницы) — новое поле в ответе probe; в волне остаётся только снятие мёртвой
  ветки в `pimp.py`.
- **#5532** (уборка карты тем) — находки под ней нет, боли нет, риск потерять цвета есть.
- **#5595** (кэш `ProjectPaint.status`) — условие «если строку начнут читать часто» не наступило.
- **#5537** (`row` = null) — диагностическая мелочь, в рисках ревизии её нет.
- **#5743** («тема всем окнам» стирает личные цвета) — это выбор Элвиса и смена смысла; ушло к нему вопросом
  (рекомендую: общий цвет накрывает только окна без своего). Кодить в этой волне нельзя.
- **#5751** в части текста подсказки — новое видимое сообщение, нужен макет; числовая половина (умолчание
  280) идёт, но действует только с релизом 1.4.0.

## Волна 1 — идёт без Элвиса

### Батч П1 — канал «Пимп», раскладки, CLI, скилл, эталоны (Opus 5 max) · держит сборку
Задачи: #5728 #5729 #5730 #5732 #5680 #5681 #5682 #5688 #5689 #5690 #5691 #5556 #5558 #5745 #5746 #5747 #5748 #5750.
Файлы: `PimpChannel.swift`, `LayoutsStore.swift`, `ArrangeLayout.swift`, `LayoutPicker.swift`,
`app/Tests/ClaudeAXTests/PimpChannelTests.swift`, `tools/pimp.py`, `tools/release.sh`, `tests/pimp_cli_test.py`,
`tests/fixtures/pimp/*`, `skills/pimp/SKILL.md`.
**Эталоны канала переписываются ПЕРВЫМ шагом батча, до кода** — на них стоят оба конца.
Готово, когда: снимок раскладки не пишет пустышку и не зовёт её «N окон»; безымянный попап в раскладку не идёт;
возврат обрывается, когда Claude закрыт, и говорит, что закончил; `screen` называет настоящий экран (текст CLI
и 14 эталонов правятся разом); голая «расставить» повторяет последнюю раскладку; «Claude не запущен» не врёт
при свёрнутых окнах; `release.sh` не сносит розданный архив до сборки; в скилле нет пути `/Users/elvis`.
Проверки: `swift test` (тестов не меньше 202), `python3 -m unittest tests/pimp_cli_test.py`.

### Батч П2 — окна, экраны, меню (Opus 5 max) · ПОСЛЕ П1, тот же владелец Swift
Задачи: #5731 #5733 #5684 #5557 #5742 #5560.
Файлы: `ClaudeActions.swift`, `ClaudeAXController.swift`, `ClaudeApp.swift` (enum `Screens`),
`MinimizeMenu.swift`, `MenuModel.swift`, `app/Tests/ClaudeAXTests/ClaudeAXTests.swift`.
Готово, когда: новое окно рождается на экране, где стоят окна; ⌥⌘A проверяет, влезает ли сетка, и не влезло —
кладёт лентой с плашкой; повторы `setFrame` не спят на главной нити; после переезда окон «расставить» не
отвечает «окон нет»; при живых цветах пункты цвета/шрифта/размера погашены с подсказкой (как у «Раскрасить
по кругу»); «посередине» при рядах — середина первого ряда.

### Батч С — страница (Opus 5 max) · ЕДИНСТВЕННЫЙ писатель `inject.js`
Задачи по порядку: **#5744 → #5533** (сперва писать оба ключа, потом сбор слоёв по приоритету), затем
#5713 #5714 #5718 и #5740 (бэкап зеркала тем — сеть безопасности ставится ДО правок пути чтения).
Файлы: `claude-patch/inject.js`, `tests/*.test.mjs` своих разделов, `tools/test.sh:75` и
`tests/size-half.test.mjs:84-91` (хрупкие сторожа — правятся вместе с кодом), `WindowThemeStore.swift`
только по #5740 — **этот файл в волне больше ничей**.
Заодно одной строкой: в люк `__myclaudeTest` добавить `setStage`, `collapseTargets`, `clampHeight`,
`storedLayer` — иначе батч Т волны 2 станет вторым писателем `inject.js`.
Готово, когда: «Новое окно ▸ проект» кладёт цвет и под `id:`; запись `id:` не закрывает более полную тень;
карточка сегмента сама не открывается; обычная фраза с ✅ не красит полосу «готово» (допуск ведущих пробелов,
оба значка ✋ и ⚠, тест на настоящих строках Элвиса); «Обкэшить» говорит, когда отказал; `VERSION` поднят.
Не трогать: объект `api` (`inject.js:5912`) — сторож привязан к строке `dispose,`.

### Проверяющий волны 1 (Opus 5 max, отдельный агент)
Сверка «сделано vs описано» по диффу, verify по коду, полный `bash tools/test.sh`. Красное чинит гейт до отчёта.

## Волна 2 — после гейта волны 1

- **Батч Т — тесты и гейт** (единственный писатель `inject.js` в волне): #5683 (настоящая починка) #5749
  #5757 #5758 #5759 #5760 #5761.
- **Батч А — приложение и патчер**: #5722 #5723 #5724 #5725 #5726 #5727 + умолчание 280 (#5751, числовая часть).
- **Батч К — команды и меню**: #5734 #5735 #5736 #5715 #5716.
- **Батч Ц — проекты и темы окон**: #5738 #5739 #5741 #5534 (корень: `ProjectPaint.windowKey(forTitle:)`
  отдаёт `main` любому окну с заголовком «Claude» — после этого обход из П1 снимается).
- **Батч Д — документы** (делает гейт): #5752 #5753 #5754 #5755 #5756. Правится `AGENTS.md` (не `CLAUDE.md`),
  `HANDOFF.md`, `README.md`, `docs/TEAM.md`, `docs/CHECKLIST.md`. Числа тестов проставляет гейт по последнему
  прогону — не батч.

## Что ждёт Элвиса (в чистку не идёт)

- **#5743** «тема всем окнам»: подтверждение или подложка — вариант «б» рекомендован.
- Релиз 1.4.0 команде (#5381) и инструкция от 1.2.0.
- macOS 13–15 у команды — ответов ноль (#5386).
- Разрез `inject.js` на модули (#5585) — после чистки, отдельной работой.
- `status.json` на мегабайты (#5536) — правка лоадера = перепатч у всей команды.
- Всё мышью: плитки раскладок, «запомни → верни», «Обкэшить» из попапа, перезапуск Claude, цвет чата
  после переименования.

## Дисциплина

Ветка `wf43` от `main`, тег `wf43-start` (`33139e0`). Один писатель на файл; `inject.js` — только батч С;
Swift — только П1, затем П2. Полные тесты и коммиты — на гейте. Сторож — крон в чате оркестратора.
