**Вердикт: fix-needed** — код WF19 (A-js + A-swift) сходится с планом полностью; чинить нужно доки батча D и процессные хвосты.

## Что сошлось (проверено по диффу и коду)

| Проверка | Итог |
|---|---|
| Контракт `size` побайтно | ✅ `SizeLayer.commandValue` (`FontCatalog.swift:150–166`): `.keep`→поля нет, `.reset`→`null`, `.halves`→объект, `answer` первым, снятая половина `null`, клэмп 11…24. `«🧹 Всё как у Claude»` шлёт `.reset` (`MinimizeMenu.swift:518`) → `,"size":null}` байт в байт |
| Половина `null` не утекает в хранилище | ✅ чистка ровно в одном месте, `inject.js:1301–1312` — после слияния и ДО `applyLayers`/`writeLayers`/`storeSessionLayers`/`pending`; вторая сетка — `applySize` гоняет значение через `normalizeSize` (`:1064`); Swift хранит `Size`, нулевых половин там нет |
| `normalizeSize` / `LAYER_NORMALIZE` не тронуты (В1) | ✅ особый случай только в `runThemeCommand` (`inject.js:1283–1289`) |
| Строгий дискриминатор (В2) | ✅ `value[half] === null`, мусор идёт через `sizePx` = «поля нет» |
| Пустой результат = полный сброс | ✅ `Object.keys(merged).length ? merged : null` |
| В4 / В5 | ✅ `ClaudeActions.windowSize(after:window:all:)` (`:503`, база `window ?? all`), `lastAppliedSize = size.applied(to:)` (`:494`) |
| Галка по половине | ✅ уже от WF14, `MinimizeMenu.swift:806–808`; A-swift верно ничего не правил |
| «Случайно» | ✅ `AutoPaint.nextLight` (`AutoPaint.swift:170`), в `paint` читается ДО `autoPaintStore.remember`; `prefersLightWindows` удалён, висячих ссылок нет |
| «Ещё раз» | ✅ `avoiding:` (`AutoPaint.swift:153–166`), выбор из трёх, мусор клэмпится |
| Шов — статиками в `AutoPaint` (Б3) | ✅ свойств на `ClaudeActions` не заведено |
| Тесты переписаны, не удалены | ✅ 61 → 64 функции, удалённых нет; переписаны ровно `testSizeAndFramePayloadMatchContract` и `testThemeStoreRemembersSizeAndFrame`, у `testAutoPaintRemembersPresetStartAndScheme` — только комментарий (В7) |
| `VERSION` | ✅ `inject.js:41` = `wf19-a-1` |
| Лоадер, `themes.json`, `Patcher`, `Resources`, `tools` | ✅ не тронуты (`git diff --stat` пуст) |
| `node --check claude-patch/inject.js` | ✅ синтаксис ок |

## Находки

**1. [важно] `docs/TEAM.md:36` — ⌥⌘N приписана «В отдельное окно», а у неё клавиши нет.**
«И этот пункт, и клавиша ⌥⌘N теперь слушаются» стоит в буллете «🪟 В отдельное окно». По `MenuModel.swift:96–99` ⌥⌘N — это `.newWindow` (`registersHotkey: true`), у `.popoutWindow` `key: nil`. README честен (`README.md:107` «клавиши у пункта нет»). Команда нажмёт ⌥⌘N вместо выноса чата и получит новый чат новым окном.
*Правка:* в `TEAM.md:36` убрать клавишу — «Пункт теперь слушается, с какого окна ни нажми…»; про ⌥⌘N сказать в буллете «Новое окно», где она и живёт.

**2. [важно] `docs/TEAM.md:37–38` — плашка «Сначала открой чат» выдана за новую, а настоящая новая не названа.**
«раньше в таких случаях пункт просто молчал» — неверно: ветка `no-chat` с `NEW_WINDOW_NOTE_CHAT` была до WF19 и в диффе не менялась. WF19 добавил ровно одну плашку — «Этот чат уже в отдельном окне» (`inject.js:3596`, `:4275`), и её в TEAM.md нет вовсе.
*Правка:* переписать на «Нажал в уже вынесенном окне — оно скажет „Этот чат уже в отдельном окне“»; фразу про «раньше молчал» применительно к пустому экрану снять.

**3. [важно, PLAUSIBLE — нужен живой гейт] `ClaudeActions.swift:187 + :210` — «Новое окно» с попапа адресовано главному окну, а ⌘N уходит попапу.**
`match = mainWindowMatch()` направляет команду главному окну, но `self.after(newWindowKeyDelay) { self.newChat(window) }` (`:210`) и сам `newChat` (`:799–805`) фокусируют и бьют ⌘N по тому окну, из чьего меню нажали. Страница главного окна ждёт `/epitaxy`; создастся ли там чат — зависит от того, ловит ли Electron ⌘N акселератором приложения. Отчёт A-swift этого не проверял, живых проверок не было.
*Правка:* на гейте прогнать сценарий «Новое окно с попапа» живьём; не заработало — слать ⌘N главному окну (или, минимум, записать ограничение в README/TEAM и завести задачу).

**4. [мелочь] `ClaudeActions.swift:316` — `var mainWindowMatch` никем не присваивается.**
Комментарий обещает, что `ClaudeAXController` подставит сюда свой `ProjectIndex.mainWindow()` (он умеет и случай двух страниц). `grep` по `app/Sources` даёт только объявление и два вызова — присваивания нет, статик молчит при `pages.count != 1`. Шов мёртвый, комментарий вводит в заблуждение.
*Правка:* либо присвоить в `ClaudeAXController` (`index.mainWindow()?.match`), либо снять из комментария обещание про контроллер.

**5. [мелочь] Запас из дополнения 01:30 («`title:""` = окно в фокусе») не реализован.**
`match == nil` (главное окно на обычном чате claude.ai или `status.json` пропал) — команда по-прежнему уходит с AX-заголовком попапа, то есть ровно в старое молчание. README это описывает как «адресация остаётся прежней», TEAM.md — нет.
*Правка:* решить на гейте — либо реализовать `title:""`, либо снять пункт из дополнения плана как отменённый.

**6. [мелочь] Плашка «Этот чат уже в отдельном окне» достижима только на запасном пути.**
`addressed()` (`inject.js:1254`) при наличии `match` сверяет только `location.pathname`, попап (`about:blank`) не адресован и молчит; в штатном случае чат выносит главное окно. `README.md:115` это оговаривает честно — расхождения с кодом нет, но пункт «плашки popout-window» из списка проверок закрыт лишь частично.

**7. [мелочь] `inject.js:4018` — у `new-window` слой размера по-прежнему через `LAYER_NORMALIZE`.**
Половина `null` в `new-window` прочтётся как полный сброс. Сегодня недостижимо (`SizeLayer(Layer<Size>)` даёт только keep/reset/set), но контракты команд разъехались.
*Правка:* строкой комментария зафиксировать, что у `new-window` половин нет, либо позвать `normalizeSizeCommand` и там.

**8. [мелочь, процесс] Батчи закоммичены под чужими заголовками, `MenuModel.swift` не закоммичен.**
Код WF19 лежит в `db876d9` и `0958822` с сообщениями «WF20: дополнение — …»; `app/Sources/ClaudeAX/MenuModel.swift` (файл A-swift, правка только в doc-комментарии `sizeResetTitle`) остался в рабочем дереве незакоммиченным вместе с файлами батча D. Resume и откат по батчам по такой истории не собрать.
*Правка:* на гейте развести коммиты по батчам либо явно записать в run-файл, где что лежит; `MenuModel.swift` внести в коммит A-swift, а не D.

**9. [мелочь, гейт] `AGENTS.md` → «Контракты» не обновлён.**
План просит дописать половину `null` у `size`; кроме этого поменялся порядок полей `new-window`/`popout-window` — теперь `scope, title, match, x, y, …` (`ClaudeActions.swift:280–300`), а в `AGENTS.md` записан старый порядок без `match`.

**10. [наблюдение, не баг] Галка после снятия половины у окна с записью «всем окнам».**
Окно записывает `{question:12}`, страница дальше по цепочке не идёт, экран — «как у Claude» для ответов, а `resetState` требует ещё и `allEmpty` (`MinimizeMenu.swift:806–808`) → в «Размер ответов ▸» не отмечено ничего. Это ровно то поведение, которое план объявил ожидаемым (проверка гейта 2: «у окна не отмечено ничего, галка — в „Всем окнам ▸“») и о котором предупреждает М5/п. 11 — ложным багом не считаю, фиксирую для гейта.

## Чужое (не мои файлы, в отчёт)

- `docs/TEAM.md`, `README.md` — батч D, не закоммичены; находки 1, 2, 5.
- `app/Sources/ClaudeAX/MenuModel.swift` — не закоммичен, находка 8.
- `docs/plan-wf20-2026-09-04.md` — прирос в тех же двух коммитах, что код WF19.

## Follow-up

Живьём не проверено ничего (гейт впереди): сценарии (а)–(з) плана, «Новое окно» с попапа (находка 3), плашка «уже в отдельном окне», доставка `cp inject.js` ДО подмены приложения.