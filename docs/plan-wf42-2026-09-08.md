# WF42 — Ревизор ⚪PimpMyClaude: карта функций и план чистки

Заведён 08.09.2026 09:50. Оркестратор — **Opus 5 max** (Элвис: «Fable подписка заканчивается, в этом чате
оркестратором будет Опус 5 Макс, Fable не запускай»). По WORKFLOW.md «Fable кончился → все клетки Fable → Opus max»
— так и делаем во ВСЕХ ролях этого прогона. Методика — `SkilZZZ/docs/revizor-method.md` (ред. 2 от 08.09), скилл
`~/.claude/skills/revizor`.

Задачи: [#5662](https://vlusnoff-production.up.railway.app/#t/5662) (Ревизор), [#5709] (рефакторинг),
[#5710] (карта функций «работает / не работает»).

## Зачем сейчас

41 воркфлоу за пять дней, десятки агентов, `inject.js` вырос с 5250 до 6107 строк, Swift — до 13 311.
Правило AGENTS.md: перед большой фичей и после серии из 3+ воркфлоу — Ревизор и чистка, а не следующая фича.
Элвис прямо просит рефакторинг и спрашивает, не важнее ли сперва знать, что работает. **Важнее** — поэтому
карта функций («работает / сломано / живьём не проверялось») — обязательная зона ревизии, а не отдельная работа.

## Снимок

`git worktree add --detach <scratchpad>/revizor-snapshot HEAD` на ревизии **a116786** (main, чисто).
Ревизоры читают ТОЛЬКО там, ничего не пишут в репозиторий. Живые прогоны не останавливаем — их и нет.
Перед планом чистки важные находки пересверяются с текущим HEAD.

## Состояние на входе (числа сняты командами 08.09 09:45)

- `bash tools/test.sh` — зелёный: JS 18 файлов / 225 проверок, Python CLI 39, Swift 202. Код возврата 0.
- `claude-patch/inject.js` — 6107 строк, один файл, 17 разделов, 193 функции верхнего уровня.
- Swift — 13 311 строк в 47 файлах; самые большие: MinimizeMenu 1311, ClaudeActions 1221, PimpChannel 1081.
- Тесты — 13 244 строки (JS 5071 + Swift 8173 + Python 425): **тестов больше, чем боевого кода Swift**.
- `docs/` — 98 файлов, 17 279 строк (планы, run-файлы, макеты).
- Приложение у Элвиса — 1.4.0 (8), собрано из main; в `dist/` лежит 1.3.0 — команда сидит на старом.
- Открытых задач по проекту в 🟣Trelvis — 60 из 89 в слаге `other`.

## Зоны (12 ревизоров, каждый — Opus 5 max, только чтение)

Каждому: снимок, паспорт `CLAUDE.md`, `AGENTS.md`, `SkilZZZ/AGENTS.md`, `SkilZZZ/WORKFLOW.md`, схема отчёта,
запрет выдумывать проблемы ради объёма и выдавать известное за новое.

| # | Зона | Файлы | Строк |
|---|---|---|---|
| 1 | Страница: темы, цвета, ключи чата | inject.js 204–1707, 2961–3259 | ~1800 |
| 2 | Страница: полоска, карточка, ручка, раскладка, прокрутка | inject.js 1708–2960, 3260–4113, 5534–5717 | ~2300 |
| 3 | Страница: окна и чаты (Обкэшить, Workflow, новое окно, кто этот чат, команды, подписки) | inject.js 4114–5533, 5718–6107 | ~1800 |
| 4 | Swift: меню и оформление | MinimizeMenu, MenuModel, LayoutPicker, ThemeEditor(+Model), MyThemes, ThemeCatalog, ThemeKnobs, FontCatalog | ~3355 |
| 5 | Swift: команды странице, авто-Allow, хоткеи, StatusFeed, живые цвета | ClaudeActions, CommandChannel, LiveColors, LiveStyle, WorkflowKit, StatusFeed, HUD, Hotkeys, AutoAllow, AX | ~2735 |
| 6 | Swift: канал «Пимп», раскладки, стор проектов | PimpChannel, LayoutsStore, ArrangeLayout, ProjectsStore, Screens | ~1600 |
| 7 | Swift: проекты, темы окон, probe, автопокраска | ProjectPaint, ProjectIndex, ProjectSettings, WindowThemeStore, ChatProbe, AutoPaint | ~2850 |
| 8 | Swift: приложение, патчер, лоадер, установка | AppDelegate, SetupWindow, OperationWindow, Notifier, ClaudeAXController, ClaudeApp, Patcher, Asar, Loader | ~2800 |
| 9 | CLI, скилл, сборка, релиз | tools/pimp.py, bundle.sh, release.sh, test.sh, install-skill.sh, skills/pimp | ~1330 |
| 10 | Сами тесты (обязательная зона) | tests/*.mjs, tests/dom.mjs, tests/pimp_cli_test.py, app/Tests/* | ~13 244 |
| 11 | Понятность и параллельная работа + документы | CLAUDE.md, AGENTS.md, README, docs/TEAM.md, docs/CHECKLIST.md, docs/PROGRESS.md, планы | — |
| 12 | **Карта функций глазами Элвиса и команды** — что работает, что сломано, что живьём не проверялось | весь снимок + CHECKLIST + Trelvis | — |

Сквозные сценарии (поперёк зон, спрашиваются у зон 1–8): (а) Элвис жмёт плитку раскладки → меню → AX-рамки →
экран; (б) чат говорит «Пимп, пять в ряд» → CLI → `pimp/` → приложение → окна → ответ; (в) Claude
перезапустился → поколение → `themes-restore` → страница → цвет остался.

## Что ревизоры НЕ делают

Не чинят, не пишут фич, не трогают живые файлы в `~/Library/Application Support/MyClaude/`, не кладут
`probe.js` (канал занят приложением — иначе у Элвиса замрёт карта чатов), не собирают приложение,
не запускают `arrange`/`open` (окна Элвиса на месте — он работает).

## Гейт синтеза (оркестратор, Opus 5 max)

1. Отчёты зон → `docs/revizor-wf42-2026-09-08.json` + выжимка `docs/revizor-wf42-2026-09-08.md`.
2. Дедуп между зонами, пересверка важного с текущим HEAD.
3. Один свежий проверяющий (Opus 5 max — вместо Fable high по слову Элвиса) пытается опровергнуть важные находки.
4. Каждая находка — задача в 🟣Trelvis одной командой `save-json`.
5. HTML владельцу по `HTML.md` со списком «оставить как есть».
6. План чистки WF43 батчами с границами файлов → критик → одобрение Элвиса → кодинг.

## Критерии готовности WF42

- 12 отчётов по схеме, сохранены в репозиторий.
- Карта функций: у КАЖДОЙ возможности стоит «работает / сломано / живьём не проверялось» и чем проверяется.
- Найденное — в Trelvis; дублей с уже открытыми 60 задачами нет.
- План чистки WF43 написан и раскритикован; кодинг не начат без слова Элвиса, кроме безопасных правок
  (см. WF43: «что можно делать без Элвиса»).
