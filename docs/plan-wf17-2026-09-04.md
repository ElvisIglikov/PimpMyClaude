# План WF17 — «Релиз 1.4.0 команде»: нотаризация, zip, живой «Снять → Поставить», обновления Claude

Дата: 04.09.2026 21:10. Проект ⚪PimpMyClaude. Задача 🟣Trelvis [#5381](https://vlusnoff-production.up.railway.app/#t/5381)
(одобрена Элвисом 04.09 20:40 — «все 10 делай»). Ветка `wf17`, тег `wf17-start` (от `main` ПОСЛЕ слияния всех воркфлоу,
которые правят `README.md`, `docs/TEAM.md`, `AppDelegate.swift` — как минимум WF14). Run-файл `docs/run-wf17-2026-09-04.json`.
Модели: **все роли — Opus 5 max**, гейт тоже Opus max (слово Элвиса 04.09 18:40: Fable экономим, Opus ниже `max` не брать).
Макета нет — интерфейса WF17 почти не добавляет (один пункт меню, текст диалога — в плане дословно).
**Запуск — только после ответов Элвиса на вопросы в конце (в первую очередь В1 и В2).**

## Слова Элвиса

> «все 10 делай (п. 4)» — 04.09 20:40, одобрение списка WF15–WF24, где WF17 — релиз.

Раньше, при постановке WF4 (03.09): «нужно, чтобы у команды просто открылось и работало, без плясок с правым кликом».
Отсюда нотаризация и zip, а не «скачай и разреши в настройках».

## Что выяснено (04.09 живьём и по коду; агенту заново не копать)

1. **Обновления Claude после патча НЕ ставятся — подтверждено живьём, в логе Элвиса.** Claude Desktop обновляется
   Squirrel.Mac (`Contents/Frameworks/Squirrel.framework`, кэш `~/Library/Caches/com.anthropic.claudefordesktop.ShipIt`,
   фид `https://api.anthropic.com`, проверка раз в час). В `~/Library/Logs/Claude/main.log`:
   ```
   19:09:59 [info]  [updater] Found an update, downloading
   19:18:06 [error] [updater] Auto-update error: [Error: Code signature at URL file:///Users/elvis/Library/Caches/
            com.anthropic.claudefordesktop.ShipIt/update.hTN7z3T/Claude.app/ did not pass validation:
            не удалось удовлетворить требованию (требованиям) к коду]
   ```
   Squirrel проверяет подпись скачанного обновления по требованию, снятому с **работающего** приложения. После нашего
   патча работающий Claude подписан **ad-hoc** (`codesign -dv`: `flags=0x2(adhoc)`, `TeamIdentifier=not set`), а
   обновление подписано Apple/Anthropic — требование не сходится, обновление отбрасывается. Папку `update.*` ShipIt
   после отказа удаляет (кэш пуст, mtime 19:18) — переиспользовать скачанные байты нельзя.
   **Следствие для команды: патч не «слетает от обновления» — обновление просто никогда не приходит, Claude тихо
   застывает на той версии, на которой его запатчили.** В UI ничего не видно, только строка в логе.
2. **Это ломает обещание в текстах.** `README.md:40` и `docs/TEAM.md` («Claude обновился — патч слетает, нажми
   „Поставить снова“») описывают сценарий, который бывает только при РУЧНОЙ переустановке Claude. Правдивый текст
   обязателен в релизе 1.4.0.
3. **«Снять» подпись Apple не возвращает.** `ClaudePatcher.restore` кладёт из бэкапа оригинальные `app.asar` и
   `Info.plist` и снова подписывает ad-hoc (`Patcher.swift`, `signAndVerify` → `codesign --sign -`). Значит после
   «Снять» обновления по-прежнему заблокированы. Единственный честный возврат — **скачать Claude заново с
   claude.ai/download и положить поверх**; тогда обновления оживают, а патч ставится заново кнопкой «Поставить».
4. **Обновление самого PimpMyClaude Claude не закрывает.** В `install()` есть ветка `.alreadyInstalled`: лоадер уже v6,
   хэш и подпись сходятся → приложение обновляет только живые файлы (`inject.js`, `claude.css`, `workflow-kit`) и
   Claude не трогает. То есть апгрейд команды с 1.3.0 на 1.4.0 — это «Выйти → заменить .app → открыть → Поставить»,
   без закрытия Claude и без повторных вопросов macOS про микрофон. Это надо написать в TEAM.md явно.
5. **`tools/release.sh` готов и проверен на 1.3.0**: bundle (Developer ID + hardened runtime) → проверки подписи →
   `notarytool submit --wait` (профиль keychain `pimpmyclaude`) → `stapler` → `dist/PimpMyClaude-<версия>.zip` →
   распаковка и `spctl --assess` ровно того zip, который уедет. Две мины: (а) скрипт в начале делает
   `rm -f dist/PimpMyClaude-*.zip` — предыдущий релиз 1.3.0 стирается, отката у команды не останется, если заранее не
   скопировать; (б) `dist/` в `.gitignore` — zip в репозиторий не попадает, живёт на рабочем столе Элвиса.
6. **Версия.** В репозитории сейчас `1.3.0 (7)`; батч B WF14 поднимает `Info.plist` до `1.4.0 (8)`. WF17 версию
   **не трогает**, только проверяет на гейте, что она поднята и совпадает с именем zip.
7. **Бэкапы чистые.** `~/Library/Application Support/MyClaude/backups/{1.40609.1,1.46388.1}/app.asar` — без маркера
   `[MyClaude:v*:start]`, то есть в бэкапе лежит настоящий оригинал. Риск на будущее: `makeBackup` сохраняет то, что
   лежит в бандле, а `Asar.patch` перед вставкой срезает чужой лоадер — значит если кто-то поставит наш патч поверх
   СТАРОГО лоадера (v5 из `patch-claude.mjs`), в бэкап уедет уже пропатченный asar. У команды такого быть не должно
   (Claude приходит чистым), у Элвиса проверено — чисто. На гейте проверить одной командой, у себя и на чистом
   пользователе.
8. **Владелец бандла важен.** Claude пишет в лог `App bundle ownership: uid=501 gid=80 (/Applications/Claude.app)`.
   Патчер умеет различать: `EPERM` → системный промпт «Управление приложениями», `EACCES` → сообщение «чужой владелец,
   зайди под админом или переустанови Claude в ~/Applications». На чистом пользователе macOS Claude из `/Applications`
   принадлежит Элвису (uid 501) — тест туда и упрётся, если не поставить Claude заново от имени нового пользователя.
   **Значит сценарий чистого пользователя обязан начинаться с установки Claude начисто.**
9. **Приложение уже следит за состоянием патча**: `AppDelegate.refreshState` раз в 60 с и при активации Claude,
   `ClaudeState.lost(reason:)` («Claude обновился» / «старый лоадер» / «хэш не сходится»), красный «!» в строке меню и
   одно уведомление с кнопкой «Поставить снова» (`Notifier.postPatchLost`). Состояния «Claude хочет обновиться, но не
   может» в этом наборе нет — его и добавляет батч S.
10. **Тесты**: 51 (`app/Tests/ClaudeAXTests`, `app/Tests/PatcherTests`); патчер покрыт пятью
    (`testInstallPutsLoaderAndMatchingHash`, `testInstallIsIdempotent`, `testInstallLiveFilesCopiesWorkflowKit`,
    `testRestoreReturnsOriginalBytes`, `testLoaderMarkersAreVersionSix`).

## Решения

### 1. Что едет в релиз 1.4.0

Всё, что слито в `main` на момент старта WF17: WF13 «Новое окно»/«В отдельное окно», блоки кода в цвет темы, WF14
(меню «Оформление», ползунок полей, ровная рамка, галки, свои темы), плюс тексты и мини-код WF17. Всё, что сольётся
после старта, — уже 1.4.1 (вопрос В7).

### 2. Правда про обновления Claude — в TEAM.md и README

Дословный текст для TEAM.md (батч R вставляет как есть, менять только орфографию):

> **Про обновления Claude.** Пока стоит патч, Claude сам не обновляется: он скачивает обновление и отказывается его
> ставить, потому что после патча у него локальная подпись. Ничего не ломается — Claude просто остаётся на той версии,
> которая есть. Когда захочется свежий Claude: значок 🅿️ → «Обновить Claude…» и дальше по шагам (скачать с
> claude.ai/download, перетащить в «Программы» с заменой, нажать «Поставить»). Занимает пару минут, чаты и настройки на
> месте. «Снять» подпись Apple не возвращает — обновления оживают только после переустановки самого Claude.

В README — та же правда вместо строки 40 плюс абзац «Как выпускать релиз» (порядок: версия → `tools/release.sh` →
копия предыдущего zip → TEAM.md → раздача).

### 3. Мини-код (батч S) — рекомендуемый вариант А, см. вопрос В1

- Новый файл `app/Sources/Patcher/ClaudeUpdates.swift`: читает **хвост** `~/Library/Logs/Claude/main.log` (последние
  ~256 КБ, файл может быть на 2 МБ+), ищет строки `[updater] Found an update` и `Auto-update error` + `did not pass
  validation`; возвращает `.blocked(at: Date)` / `.none`. Файла нет, права не дают, строки битые — тихо `.none`.
  Ничего никуда не отправляет, лог наружу не выносится (в интерфейсе — только дата).
- В `AppDelegate`: рядом с `refreshState` (тот же таймер 60 с) — состояние «Claude хочет обновиться, патч мешает»:
  подсказка в строке состояния меню и **одно** уведомление, как у «патч слетел» (флаг, повторов нет).
- Пункт меню значка **«⬆️ Обновить Claude…»** (после «Статус…»): диалог с тремя шагами — «1. Скачаю страницу загрузки
  (кнопка открывает claude.ai/download) 2. Перетащи новый Claude в «Программы» с заменой 3. Вернись и нажми
  «Поставить»» и кнопкой «Открыть claude.ai/download». Ничего сам не качает и не удаляет (см. В3).
- Тесты: парсер лога на фикстурах (пусто, только `Found an update`, ошибка валидации, битая кодировка, файла нет).

### 4. Живой «Снять → Поставить» — только руками Элвиса

«Поставить» закрывает Claude, а с ним чат оркестратора, поэтому это шаги Элвиса. Порядок и что считать зелёным — в
чеклисте гейта ниже. Перед «Снять» — убедиться, что бэкап текущей версии на месте
(`~/Library/Application Support/MyClaude/backups/<версия>/app.asar`), иначе «Снять» честно откажется.

### 5. Чистый пользователь macOS

Новый пользователь на Маке Элвиса (Системные настройки → Пользователи и группы), НЕ администратор — так ближе к
команде. Порядок: войти → поставить Claude начисто с claude.ai/download в `~/Applications` (иначе упрёмся в чужого
владельца, см. «Что выяснено» п. 8) → скачать zip **браузером** (чтобы был карантин, как у команды; если zip приехал
файлом — повесить карантин вручную: `xattr -w com.apple.quarantine "0083;0;;" ~/Downloads/PimpMyClaude-1.4.0.zip`) →
распаковать двойным кликом → перетащить в «Программы» → открыть → пройти окно настройки → «Поставить». Зелёное:
двойной клик не ругается «повреждено», Gatekeeper молчит, оба разрешения спрашиваются по одному разу, патч встаёт,
меню на жёлтой кнопке работает.

### 6. Первый из команды и отзыв

Отдаём **одному** человеку, а не всем сразу (вопрос В2): zip + `docs/TEAM.md` + короткий текст в чат. Анкета — семь
вопросов, ответ словами в чат, без формы:
1. Открылось с первого раза или macOS ругалась?
2. Сколько раз спросили разрешения и понятно ли было, что нажимать?
3. Claude закрылся и открылся сам?
4. Меню на жёлтой кнопке нашлось?
5. Что не заработало или выглядит странно?
6. Что попросил бы добавить первым делом?
7. Сколько всего заняло от zip до работающего меню?

### 7. Чего в WF17 НЕ делаем

Свой загрузчик Claude (скачивание и установка новой версии из приложения), полный бэкап Claude.app ради возврата
подписи Apple, авто-обновление самого PimpMyClaude (Sparkle), чистка старых бэкапов, правки `inject.js` и лоадера,
любые новые фичи меню. Всё это — задачами в 🟣Trelvis по итогам (см. риски).

## Батчи

| Батч | Что | Файлы | Модель |
|---|---|---|---|
| R | Тексты релиза: `docs/TEAM.md` — версия 1.4.0, проверено на Claude 1.46388.1, раздел «Про обновления Claude» (п. 2 дословно), «Как обновить само приложение» (Выйти → заменить → открыть → Поставить, Claude не закрывается), что значит красный «!», честный откат («Снять» ≠ подпись Apple); новые пункты меню WF13/WF14 берутся как есть из батча D WF14, заново не переписывать. `README.md` — правка неверной строки про «патч слетает», раздел «Как выпускать релиз», счёт тестов. Текст в чат команде + анкета из семи вопросов | `docs/TEAM.md`, `README.md` | Opus max |
| S | Swift, маленький: `ClaudeUpdates.swift` (парсер хвоста лога Claude), состояние «Claude хочет обновиться, патч мешает» в меню-баре + одно уведомление, пункт «⬆️ Обновить Claude…» с диалогом и кнопкой на claude.ai/download, тесты на фикстурах лога. **Сборка волны у этого батча**: `swift build && swift test`. Версию в `Info.plist` НЕ трогать (её поднял WF14) | `app/Sources/Patcher/ClaudeUpdates.swift` (новый), `app/Sources/PimpMyClaude/{AppDelegate,Notifier}.swift`, `app/Tests/PatcherTests/PatcherTests.swift` | Opus max |
| V | verify по диффу, только чтение: тексты не расходятся с фактами раздела «Что выяснено» (особенно про обновления и про «Снять»), парсер лога не падает на отсутствующем файле/битых байтах и не читает файл целиком, лог никуда не отправляется, уведомление не повторяется, нет правок `inject.js`, лоадера, `Info.plist` и меню WF14; тесты переписаны, а не удалены | | Opus max |
| гейт | Чеклист ниже целиком | | Opus max |

## Чеклист гейта — с ролями

| # | Кто | Что делает | Зелёное |
|---|---|---|---|
| 1 | оркестратор | `swift build && swift test` в `app/`; `node --check claude-patch/inject.js` | все тесты зелёные (до WF14 был 51 + тесты WF14 и S; точный счёт сверить по факту и записать в README) |
| 2 | оркестратор | Проверить `Info.plist`: `1.4.0` / `8`; сверить с тем, что слито в `main` | версия совпадает с планом релиза |
| 3 | **Элвис сам** | Скопировать `dist/PimpMyClaude-1.3.0.zip` на рабочий стол (release.sh его сотрёт) | копия лежит |
| 4 | оркестратор (Элвис рядом) | `tools/release.sh` | «✅ готово: dist/PimpMyClaude-1.4.0.zip», нотаризация принята, `spctl` — «Notarized Developer ID» |
| 5 | оркестратор | Положить рядом с zip свежий `docs/TEAM.md`; проверить, что версия в TEAM.md = 1.4.0 | оба файла на рабочем столе |
| 6 | **Элвис сам** | Значок 🅿️ → «Снять…» на своём Маке | Claude закрылся и открылся, меню на жёлтой кнопке пропало, «Статус…» говорит «Патч не стоит» |
| 7 | **Элвис сам** | Значок 🅿️ → «Поставить…» | Claude закрылся и открылся, меню и полоска на месте, темы применились, «Патч стоит · v6» |
| 8 | оркестратор | `strings` по `backups/<версия>/app.asar` — нет маркера `[MyClaude:v*:start]` | бэкап чистый |
| 9 | **Элвис сам** | Завести нового пользователя macOS и пройти сценарий из решения 5 | Gatekeeper молчит, патч встал, меню работает |
| 10 | оркестратор | Через 2–3 часа работы Claude сосчитать в `~/Library/Logs/Claude/main.log` строки `Found an update, downloading` и `did not pass validation` | известно, сколько раз в сутки Claude качает обновление впустую (это трафик команды) |
| 11 | **Элвис сам** | Посмотреть в самом Claude: есть ли где-то «Restart to update» или значок обновления | знаем, видит ли команда хоть что-то |
| 12 | оркестратор | Проверить пункт «⬆️ Обновить Claude…»: открывает claude.ai/download, ничего не трогает без спроса | диалог и кнопка работают |
| 13 | **Элвис сам** | Отдать zip + TEAM.md первому из команды, задать семь вопросов | ответы получены |
| 14 | оркестратор | По ответам: правки текстов сразу, всё остальное — задачами в 🟣Trelvis; коммит после каждого батча, слияние `main --no-ff`, run-файл в `финиш`, сторож снят | ни одного «потом разберёмся» |

## Вопросы Элвису (до запуска)

1. **Код в WF17 или чистый релиз?** *(рекомендую: сделать маленький батч S)* — иначе через день-два у всей команды
   Claude встанет на текущей версии, никто не поймёт почему, и вопросы придут тебе. Вариант Б: только тексты, а
   детектор и «Обновить Claude…» — отдельным воркфлоу позже.
2. **Кому отдаём первым?** *(рекомендую: одному человеку, который сегодня-завтра ответит на семь вопросов, и лучше
   тому, чей экран можно посмотреть)* — назови имя; всей команде сразу отдавать не стоит, первый прогон всегда что-то
   вскрывает.
3. **Как быть с обновлениями Claude дальше?** *(рекомендую: сейчас — честный текст + пункт «Обновить Claude…» с ручными
   шагами; полноценный загрузчик Claude внутри приложения — отдельной задачей в 🟣Trelvis)*. Вариант Б: сразу учить
   приложение качать и ставить Claude само — это плюс воркфлоу и риск сломать чужой Claude.
4. **Бэкапить весь Claude.app перед патчем (832 МБ), чтобы «Снять» возвращал подпись Apple и обновления?**
   *(рекомендую: нет)* — 832 МБ на каждую версию в Application Support, а обновляться всё равно придётся скачиванием
   свежего Claude.
5. **Чистый пользователь: заводить нового на твоём Маке или проверить сразу на чужом?** *(рекомендую: новый
   пользователь у тебя — 15 минут, и ошибка не достаётся живому человеку)*.
6. **Кто жмёт `release.sh`?** *(рекомендую: я, пока ты рядом)* — может всплыть запрос keychain на профиль нотаризации;
   «Снять→Поставить» и чистый пользователь — в любом случае ты.
7. **Если WF15/WF16 успеют слиться до релиза — они едут в 1.4.0 или ждут 1.4.1?** *(рекомендую: всё, что слито до
   старта WF17, — 1.4.0; что позже — 1.4.1, чтобы команда не получала два разных «1.4.0»)*.

## Открытые риски

- **Нотаризация зависит от Apple.** `notarytool --wait` иногда думает 20–30 минут; профиль `pimpmyclaude` живёт в
  keychain — если он потерялся, нужен пароль приложения с appleid.apple.com (заводит Элвис, агент не может).
- **Команда встанет на старом Claude.** Даже с честным текстом кто-то не прочитает и будет полгода сидеть на старой
  версии; отсюда рекомендация делать батч S (детектор + напоминание).
- **Пустой трафик.** Claude, похоже, качает отвергаемое обновление при каждой проверке (раз в час). Пункт 10 чеклиста
  это измерит; если так — писать в TEAM.md отдельной строкой и заводить задачу «глушить проверку обновлений на время
  патча» (возможно, безопаснее её не трогать вовсе).
- **Чтение чужого лога.** Парсер лезет в `~/Library/Logs/Claude/main.log` — файл пользователя. Читать только хвост,
  ничего не пересылать, в интерфейсе показывать только факт и дату. Verify обязан это проверить.
- **Разные macOS у команды.** Всё проверено на macOS 26.5; на 13–15 радиус рамки другой (WF14 это учитывает), а
  промпт «Управление приложениями» ведёт себя иначе. Остаточный риск: первый отзыв как раз и должен прийти с другой
  версии — при выборе тестировщика это плюс.
- **Конфликт по файлам.** `README.md`, `docs/TEAM.md` и `AppDelegate.swift` пишет и WF14, и (позже) WF15/WF16.
  WF17 стартует только после их слияния в `main`; параллельно с ними не запускать.
- **Старые бэкапы копятся** (~40 МБ на версию Claude) — чистки нет; при частых обновлениях за год наберётся заметно.
  Задача в 🟣Trelvis, не в этот воркфлоу.
- **Откат у команды.** Если 1.4.0 окажется хуже 1.3.0, вернуть можно только копией старого zip (шаг 3 чеклиста) —
  без неё отката нет вообще.
