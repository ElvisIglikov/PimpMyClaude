# Проверка WF29 (батч V, 06.09.2026)

Сверка по диффу `wf29-start..рабочее дерево` (батчи A и B ещё не закоммичены), не по отчётам.
Документы не смотрел — их пишет батч D, сверяет оркестратор на гейте.

## Тесты

`bash tools/test.sh` — **зелёное, exit 0**: `JS: 12 файлов, 142 проверки · Swift: 121 тест`
(база была 11/127 и 104). Внутри прошли сторожа: `node --check`, запрет статического ESM/`require`,
приметы идемпотентности, `VERSION: main «wf20-g-2» → «wf29-a-1»`, разбор `claude.json`/`themes.json`.
Динамических `import(` в inject.js стало 2 (боевой и запасной в `setModuleImporter`) — сторож их только
считает, порогом не является (`tools/test.sh:66-67`).

Тесты плана поимённо на месте все 32: A-1…A-11 — `tests/chat-id.test.mjs`, A-12…A-14 —
`tests/addressed.test.mjs:69-117`, A-15 — прежний `tests/idempotent.test.mjs` (правок не потребовал),
B-16…B-19 — `app/Tests/ClaudeAXTests/ChatProbeTests.swift`, B-20…B-32 — `ProjectTests.swift:1257-1616`.

## Границы соблюдены

- Лоадер не тронут: `git diff wf29-start -- claude-patch/patch-claude.mjs app/Sources/Patcher/` пуст,
  строка `LOADER` и `Loader.swift` без изменений.
- `command.json`/`CommandChannel.swift` не тронуты.
- Правленых файлов ровно 11 + 3 новых — все из таблицы «Батчи», чужих писателей нет.
- Живые файлы в `~/Library/Application Support/MyClaude/` не трогались: `ClaudeAXController` (единственный,
  кто создаёт `ChatProbe()` с `onDisk`) в тестах не собирается — `grep -rn "ClaudeAXController(" app/Tests/`
  пуст, так что `swift test` в живой канал не пишет.

## Что сверено по коду

| Пункт плана | Где | Итог |
|---|---|---|
| Контракт `chat` (решение 6), порядок `scope,title,match?,chat?,preview?` | `ClaudeActions.swift:396-407`, тест 26 побайтно | сходится |
| `addressed()`: `match` → `chat` → заголовок | `claude-patch/inject.js:1268-1276` | сходится |
| Контракт `chats()` (решение 4) | `inject.js:4504-4540` | сходится (об отступлении по `path` ниже) |
| Тумблер выключен — probe молчит | `ChatProbe.swift:190-193`, тест 18 | сходится |
| Вежливое владение каналом (побайтно / метка / 10 мин / молчание 60 с) | `ChatProbe.swift:254-285`, тест 17 | сходится |
| Один круг за раз (30 с), пол 15 с, переспрос неизвестного 60 с | `ChatProbe.swift:212-225`, тест 18 | сходится |
| Чтение `probe-result.json` только по смене отпечатка | `ChatProbe.swift:239-249` | сходится |
| Деградация по заголовку при несвежей карте и `store != "ok"` | `ProjectPaint.swift:249-272`, тест 23 | сходится |
| «Не определён» у попапа: команды нет, папка главного окна не подставляется | `ProjectPaint.swift:251-263`, тест 22 | сходится |
| Один ключ на окно, перенос отпечатка | `ProjectPaint.swift:317-330`, тест 30 | сходится |
| Кэш разбора `status.json` по отпечатку (7а) | `ProjectIndex.swift:288-295`, `:342-356`, тест 31 | сходится |
| `revision` по кортежу, `lastFocusedAt` его не двигает | `ProjectIndex.swift:273-279`, `:306-310`, тест 20 | сходится |
| Хвосты WF20: `guard scope == themeScopeWindow`, `isBroken` не-UTF-8 | `ClaudeActions.swift:554`, `ProjectSettings.swift:268-270` | сходится |

## Находки

### 1. [средне] Смена чата в главном окне: цвет поедет не за 4 с, а через круг probe

`ProjectPaint.swift:238` берёт ответ страницы про главное окно, пока ему меньше 60 с
(`ChatProbe.mainChatSeconds`, `ChatProbe.swift:92`), и этот ответ ПЕРЕБИВАЕТ свежий `status.json`.
Лоадер переписывает `status.json` раз в 2 с (`patch-claude.mjs:191`), а карта probe обновляется не чаще
раза в 15 с. Значит после переключения чата в главном окне цвет держится старым, пока не приедет новый
круг: обычно 6–8 с, в худшем ~17–19 с.

Гейт п. 7 плана ждёт «за 4 с главное окно перекрашивается» — это ожидание не сбудется.
Правка: либо поправить ожидание гейта, либо опустить `ChatProbe.mainChatSeconds` (одна константа,
`ChatProbe.swift:92`) до ~10 с. Конечное состояние верное в обоих случаях, речь только о задержке.

### 2. [средне] Главное окно: чат, которого нет в индексе, откатывается к `status.json`

`ProjectPaint.swift:246`:

```swift
folder: session.map(index.folder(of:)) ?? main.folder
```

Страница назвала чат, а `index.session(for:)` его не знает → `session == nil` → берётся `main.folder`,
то есть папка чата из `status.json`. Это ровно то, что запрещает комментарий тремя строками выше
(`:239-241`) и что отчёт батча B числит среди отступлений («к чату из `status.json` НЕ откатываемся»).
Проверено отдельно: `Optional.map(...) ?? fallback` при `nil` даёт fallback.

Живой вред узкий (окно расхождения ≈ 2 с, и чаще откат даёт как раз верный ответ), но правило
#5455 в коде не записано, а отчёт утверждает обратное. Правка в одну строку:

```swift
folder: named == nil ? main.folder : named.flatMap { index.session(for: $0) }.map(index.folder(of:))
```

Тестом это не закрыто: `testMainChatPrefersPageAnswer` (`ProjectTests.swift:1473`) гоняет только случай
«чат страницы есть в индексе».

### 3. [мелко] Находка 5 WF20 закрыта только для цели `main`

`paintableTitles()` отдаёт заголовки ВСЕХ окон на экране, включая главное
(`ClaudeActions.swift:714-730`), поэтому главное окно даёт две цели: `main` и `w:<настоящий заголовок>`
(`ProjectPaint.swift:264-272`). Новая сверка владения примеркой идёт КЛЮЧОМ
(`ProjectPaint.ownedByEditor`, `:336-339`) и защищает только `main`; дубль `w:` раньше защищался
заголовком, теперь не защищён ничем — при смене вида проекта с открытой панелью «Своя тема» тик может
погасить примерку через него.

Правка одной строкой, старые тесты от неё не краснеют:

```swift
if let key = ClaudeActions.themeEditorKey { return key == target.key || ClaudeActions.themeEditorTitle == target.title }
```

### 4. [мелко] Ничья заголовков считается не совсем ничьёй

`ChatProbe.chat(forTitle:)` (`ChatProbe.swift:166-177`) пропускает страницы с `chat == nil`
(`guard let chat = page.chat else { continue }`). Два попапа с ОДНИМ заголовком, где id назвал только
один, отдают этот id — и ручной выбор темы в неопознанном окне уедет в чужой проект
(`ProjectPaint.writableFolder`, `:499`). План (тест 19) требует на ничью `nil`. Тест 19
(`ChatProbeTests.swift:293`) закрывает только случай «оба назвали id».

## Отступления батчей — проверены, возражений нет

- `path: ""` на чужой странице вместо `location.pathname` (`inject.js:4380`) — в контракте чужая страница
  не описана вовсе, а `data:`-адрес артефакта и есть причина файлов на 3,9 МБ. Согласен.
- `store: "none"` у попапа с заголовком-заглушкой (`inject.js:4485`) — «ok» значит «канал сработал»,
  здесь он не работал; приложение честно уходит на старый путь. Согласен.
- `dom.modules(...)` (`tests/dom.mjs:606-613`) и `ProjectIndex.readStatus` (`:113`) — сиденья ради тестов,
  боевого поведения не меняют.
- `ProjectIndex.fileInfo` со свежим `URL` (`:351`) — без этого кэш 7а и канал probe не протухали бы
  никогда (URL кэширует значения ресурсов). Правки в плане нет, но она обязательна и покрыта тестом 31.
- `ownedByEditor` с откатом на заголовок при пустом `themeEditorKey` — честная деградация (см. находку 3).

## Вердикт

**fix-needed** — четыре находки, все правятся строками в файлах батча B, кроме находки 1
(она либо константа, либо строка в гейте плана). Тесты и границы чистые.
